# Definition of Done: Default executor provider to Anthropic (production hotfix)

**PR:** commit `8c21dcd` (retroactive story — implemented as a production hotfix during `s4.1` execution, no dedicated PR) | **Merged:** 2026-06-29
**Story:** artefacts/2026-06-22-wuce-multi-tenancy/stories/s4.3.md
**Test plan:** artefacts/2026-06-22-wuce-multi-tenancy/test-plans/s4.3-test-plan.md
**Assessed by:** Claude (agent) — retroactive DoD backlog pass, 2026-08-17
**Date:** 2026-08-17

---

## AC Coverage

| AC | Satisfied? | Evidence | Verification method | Deviation |
|----|-----------|----------|---------------------|-----------|
| AC1: default provider is Anthropic when env var unset | ✅ | `src/modules/skill-turn-executor.js` line 625: `const provider = (process.env.SKILL_EXECUTOR_PROVIDER \|\| 'anthropic').toLowerCase();` | Direct code inspection, 2026-08-17 | None |
| AC2: Copilot works when explicitly opted in | ✅ | Covered by `check-wuce26-per-answer-model-response.js` T9/T10 (copilot-path tests) | Automated test, re-run fresh 2026-08-17 — 14/14 passing | None |
| AC3: `.env.example` documents the new default | ✅ | `.env.example` line 67: `SKILL_EXECUTOR_PROVIDER=anthropic`, with a preceding comment noting Copilot is opt-in | Direct file inspection, 2026-08-17 | None |
| AC4: copilot-path tests pin the provider explicitly | ✅ | `check-wuce26-per-answer-model-response.js` T9/T10 set `process.env.SKILL_EXECUTOR_PROVIDER = 'copilot'` and restore in `finally` | Direct test-file inspection + fresh run, 2026-08-17 | None |

---

## Scope Deviations

None. This story is explicitly documented as a retroactive artefact for a production hotfix applied mid-execution of `s4.1` — the artefact-first gap (code before story) is itself already acknowledged in the story's own header ("Status: Definition-of-Done (retroactive...)"), consistent with this repo's own retrospective-story convention.

---

## Test Plan Coverage

**Tests passing in CI:** No dedicated test file for this story (per its own AC4, coverage lives inside `check-wuce26-per-answer-model-response.js`'s existing T9/T10) — 14/14 assertions in that shared file pass fresh on current master 2026-08-17.
**Gaps:** None identified.

---

## NFR Status

No red flags found in this pass. This hotfix directly unblocked `s4.1`'s own AC4/AC5 verification (journey persistence only fires after a successful model response) — its metric linkage to M1/M2 (per the story's own header) is real and already indirectly validated by `s4.1`'s own DoD.

---

## Metric Signal

**M1 (Authorization coverage rate)** and **M2 (Cross-tenant journey data leakage prevention)** — both named in this story's own Metric Linkage section as blocked without this fix.
Signal: on-track
Evidence: `s4.1`'s own DoD (already complete, `s4.1-dod.md`) confirms the production smoke test passed after this hotfix landed — direct evidence the blocking issue this story fixed is resolved.
Date measured: 2026-06-29 (cross-referenced from `s4.1`'s own DoD date)

---

## Outcome

**COMPLETE**

**Follow-up actions:** None.

---

## DoD Observations

1. **This story was missed in the initial batch pass of this cluster** — the operator's own triage correctly identified 9 stories in this feature needing DoD attention, but this agent's own working list only carried forward 8 of them (5 already-complete-artefact syncs + 3 fresh write-ups), silently dropping `s4.3`. Caught only because the integrity checker's failure count didn't fully match expectations after the batch write, prompting a re-check. **Process takeaway**: when working from a triage list with N items, explicitly check off each item against the final state write rather than trusting a mental tally — this is exactly the kind of small, easy-to-miss error that compounds across a 161-story backlog if not caught early.
2. ~7 weeks live in production (hotfix landed mid-`s4.1`, 2026-06-29), no incidents reported.
