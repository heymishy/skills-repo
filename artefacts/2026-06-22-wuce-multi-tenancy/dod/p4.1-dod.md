# Definition of Done: p4.1 — Prompt-cache key scoping + per-tenant rate-limit isolation

**PR:** #396 | **Merged:** 2026-06-24
**Story:** artefacts/2026-06-22-wuce-multi-tenancy/stories/p4.1.md
**Test plan:** artefacts/2026-06-22-wuce-multi-tenancy/test-plans/p4.1-test-plan.md
**DoR artefact:** artefacts/2026-06-22-wuce-multi-tenancy/dor/p4.1-dor.md
**Assessed by:** Claude Sonnet 4.6 (automated)
**Date:** 2026-06-24

---

## AC Coverage

| AC | Satisfied? | Evidence | Verification method | Deviation |
|----|-----------|----------|---------------------|-----------|
| AC1 — Cache keys distinct for same sessionId across different tenants | ✅ | `AC1: keys distinct for org-a and org-b with same sessionId` — PASS | Automated unit test (`check-p4.1-tenant-cache-ratelimit.js`) | None |
| AC2 — Cache key format `${tenantId}-${sessionId}` | ✅ | `AC2: key is tenantId-sessionId` — key === `'my-org-xyz123'` | Automated unit test | None |
| AC3 — Cache key falls back to sessionId when tenantId absent | ✅ | `AC3a/b/c` — null, undefined, and missing tenantId all return sessionId alone | Automated unit test (3 sub-cases) | None |
| AC4 — Per-tenant rate-limit counters isolated | ✅ | `AC4: org-a hitting limit does not affect org-b counter` — org-b next() called after org-a saturated | Automated unit test | None |
| AC5 — Rate-limit key includes tenantId when available | ✅ | `AC5` and `AC5b` — key includes `'my-org'`; tenantId takes precedence over IP | Automated unit test (2 sub-cases) | None |
| AC6 — Rate-limit key falls back to IP when tenantId absent | ✅ | `AC6a/b` — null and missing tenantId both yield IP-based key | Automated unit test (2 sub-cases) | None |
| AC7 — Test suite passes | ✅ | `node tests/check-p4.1-tenant-cache-ratelimit.js` → 11 run, 11 passed, 0 failed, exit 0 | Automated test suite run | None |

**Deviations:** None.

---

## Scope Deviations

None. The single commit (`027dbba`) touches exactly the four files in the DoR contract: `src/web-ui/adapters/cache-key.js` (new), `src/web-ui/middleware/rate-limiter.js` (modified), `src/modules/skill-turn-executor.js` (modified), `tests/check-p4.1-tenant-cache-ratelimit.js` (new). No changes to journey.js, session-store.js, journey-access.js, auth.js, or server.js.

---

## Test Plan Coverage

**Tests from plan implemented:** 11 / 11
**Tests passing:** 11 / 11

| Test | Implemented | Passing | Notes |
|------|-------------|---------|-------|
| buildCacheKey — keys distinct for same sessionId across tenants (AC1) | ✅ | ✅ | Safety-critical case |
| buildCacheKey — format `tenantId-sessionId` (AC2) | ✅ | ✅ | |
| buildCacheKey — fallback: no tenantId (AC3a) | ✅ | ✅ | |
| buildCacheKey — fallback: tenantId null (AC3b) | ✅ | ✅ | |
| buildCacheKey — fallback: tenantId undefined (AC3c) | ✅ | ✅ | |
| Rate-limit counters isolated per tenant (AC4) | ✅ | ✅ | org-a saturated; org-b unaffected |
| buildRateLimitKey — includes tenantId (AC5) | ✅ | ✅ | |
| buildRateLimitKey — tenantId takes precedence (AC5b) | ✅ | ✅ | |
| buildRateLimitKey — IP fallback: no tenantId (AC6a) | ✅ | ✅ | |
| buildRateLimitKey — IP fallback: tenantId null (AC6b) | ✅ | ✅ | |
| buildCacheKey is synchronous — not a Promise (NFR) | ✅ | ✅ | |

**Gaps:** None. All test plan scenarios implemented.

---

## NFR Status

| NFR | Addressed? | Evidence |
|-----|------------|---------|
| Key construction is synchronous — no I/O, negligible latency impact | ✅ | NFR test in `check-p4.1-tenant-cache-ratelimit.js` asserts `buildCacheKey` does not return a Promise; 1000 calls complete in negligible time |
| In-process rate-limit counters reset on deploy — acceptable | ✅ | Documented in story NFR and test file comment. No test assumes counter persistence across restarts. W1 RISK-ACCEPT logged in decisions.md (D7). |

---

## Metric Signal

| Metric | Signal | Evidence | Date measured |
|--------|--------|----------|--------------|
| M2 — Cross-tenant journey data leakage prevention | not-yet-measured | p4.1 closes the inference-layer isolation gap: tenant-scoped prompt-cache keys prevent cross-tenant cache hits; per-tenant rate-limit counters prevent quota exhaustion by one tenant affecting another. Full M2 measurement requires live multi-tenant deployment (not yet active). Signal carries forward as `not-yet-measured` — consistent with post-p2.2 state. | null |

**Note:** p4.1 is a contributing story for M2 (metric linkage confirmed in story artefact). The `contributingStories` array for M2 in pipeline-state.json has been updated to include `p4.1`.

---

## Outcome

**COMPLETE**

ACs satisfied: 7/7
Deviations: None
Test gaps: None
Follow-up actions: None (W2 cache-scope session threading deferred to a follow-up — RISK-ACCEPT logged in decisions.md D8, not a DoD blocker).

---

## DoD Observations

1. **W2 session threading deferred (not a blocker):** `_anthropicSystem()` in `skill-turn-executor.js` now accepts an optional `session` parameter and embeds the cache-scope comment when `session.tenantId` is present. However, the two call sites (lines 79 and 166) still call `_anthropicSystem(systemPrompt)` without passing session — they pass `undefined`, which is handled gracefully. The scoping logic is implemented and exercised by unit tests; activation requires threading `session` through the call chain. This is a known deferred item (decisions.md D8), not a post-merge regression.

2. **M2 contributingStories gap corrected:** p4.1 was linked to M2 in its story artefact but absent from M2's `contributingStories` array in pipeline-state.json. Corrected in this DoD's state write.
