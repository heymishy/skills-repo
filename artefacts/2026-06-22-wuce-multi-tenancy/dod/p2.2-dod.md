# Definition of Done: p2.2 — Tenant-isolation test suite

**PR:** https://github.com/heymishy/skills-repo/pull/395 | **Merged:** 2026-06-24
**Story:** artefacts/2026-06-22-wuce-multi-tenancy/stories/p2.2.md
**Test plan:** artefacts/2026-06-22-wuce-multi-tenancy/test-plans/p2.2-test-plan.md
**DoR artefact:** artefacts/2026-06-22-wuce-multi-tenancy/dor/p2.2-dor-contract.md
**Assessed by:** /definition-of-done (agent)
**Date:** 2026-06-24

---

## AC Coverage

| AC | Satisfied? | Evidence | Verification method | Deviation |
|----|-----------|----------|---------------------|-----------|
| AC1 — 9 cross-tenant 404s | ✅ | AC1.1–AC1.9 all PASS: handleGetJourney, handleGetJourneyById, handleGetJourneyState, handleGetJourneyViewers, handleGetJourneyStageView, handleGetStageControls, handlePostSideTripClarify, handleDeleteSideTrip, handleGetTrace each return 404 for bob→alice cross-tenant call | Automated test in `tests/check-p2.2-tenant-isolation.js` | None |
| AC2 — 2 cross-tenant 403s | ✅ | AC2.1–AC2.2 PASS: handlePostJourneyRecommit and handlePostJourneyStageCommit return 403 for bob→alice cross-tenant call (POLICY.OWNER) | Automated test | None |
| AC3 — filesystem isolation | ✅ | AC3.1–AC3.4 PASS: org-a tree contains no org-b marker content or paths; org-b tree contains no org-a marker content or paths. Verified via recursive directory scan with UUID-keyed content markers | Automated integration test | None |
| AC4 — session store isolation | ✅ | AC4.1 PASS: readSession('sess-alice', 'org-a') returns object. AC4.2 PASS: readSession('sess-alice', 'org-b') returns null. AC4.3 PASS: accessToken absent from stored session file (NFR-sec-no-accesstoken-disk) | Automated integration test | None |
| AC5 — same-tenant not broken | ✅ | AC5.1–AC5.8 PASS: alice calling her own journey on 8/9 read handlers returns non-4xx. handlePostSideTripClarify excluded (requires session infrastructure adapter wiring not available in unit test context; access guard is proven by AC1.7) | Automated test; 1 handler excluded with documented rationale | AC5-excl (minor — see below) |
| AC6 — suite passes | ✅ | `node tests/check-p2.2-tenant-isolation.js` → 27 run, 27 passed, 0 failed, exit 0 | Automated test execution | None |
| AC7 — no regression | ✅ | check-p2.1-tenant-storage.js (16/16), check-p0.1-journey-access.js (16/16), check-p0.2-journey-guard-wiring.js (13/13), check-p1.2-tenant-session-journey.js (12/12) all PASS. Pre-existing ougl1–6 failures confirmed unchanged from master before this branch was created | Automated test execution + master baseline comparison | None |

---

## Scope Deviations

**AC5-excl (minor):** AC5 tests 8 of 9 read-route handlers. handlePostSideTripClarify is excluded because it calls `getRegisterHtmlSession()` after the access guard passes — this injectable adapter requires session infrastructure not wired in the unit test context. The access guard itself is proven via AC1.7 (cross-tenant call to handlePostSideTripClarify correctly returns 404), which validates the guard is in place. The positive same-tenant path for this one handler is not covered by an automated test. Not blocking.

All other out-of-scope items (production code changes, concurrent/stress testing, adversarial tenantId injection, Postgres/Redis isolation) were not touched — exactly per the DoR contract.

---

## Test Plan Coverage

**Tests from plan implemented:** 27 / 14 planned (13 additional: AC3 expanded to 4 subtests with UUID markers; AC4 expanded with accessToken NFR subtest; AC5 expanded across 8 handlers)
**Tests passing:** 27 / 27

| Test group | Planned | Implemented | Passing |
|-----------|---------|-------------|---------|
| AC1 — 9 cross-tenant 404s | 9 | 9 | 9 |
| AC2 — 2 cross-tenant 403s | 2 | 2 | 2 |
| AC3 — filesystem isolation | 1 | 4 | 4 |
| AC4 — session store isolation | 1 | 3 | 3 |
| AC5 — same-tenant not broken | 1 | 8 | 8 |
| NFR — timing | 0 | 1 | 1 |

**Gaps:** None from the test plan. The additional tests increase confidence without introducing new scope.

---

## NFR Status

| NFR | Addressed? | Evidence |
|-----|------------|---------|
| NFR-sec-no-accesstoken-disk: accessToken not persisted | ✅ | AC4.3 PASS: parsed session file has no `accessToken` key |
| Suite timing < 10000ms | ✅ | NFR test PASS: elapsed measured end-to-end; consistently < 1000ms in practice |
| tmpDir cleanup (no test pollution) | ✅ | try/finally block in main(); fs.rmSync with recursive+force; env vars deleted in finally |
| No production code modified | ✅ | `git diff master --stat` on branch shows only `.github/pipeline-state.json` (bookkeeping) and the new test file |

---

## Metric Signal

| Metric | Signal | Evidence | Date measured |
|--------|--------|----------|--------------|
| M2 — Cross-tenant journey data leakage prevention | active | p2.2 proves the two-tenant HTTP isolation boundary: 9 cross-tenant read routes return 404, 2 mutating routes return 403, filesystem trees are confirmed non-overlapping, session store cross-reads return null. The full M2 measurement baseline is now established. | 2026-06-24 |

---

## Outcome

**COMPLETE WITH DEVIATIONS ✅**

**ACs satisfied:** 7 / 7
**Deviations:** 1 recorded (AC5-excl — handlePostSideTripClarify same-tenant positive test excluded; non-blocking)
**Test gaps:** None

**Follow-up actions:**
- AC5-excl: Add a wired integration test for handlePostSideTripClarify same-tenant positive path (requires registerHtmlSession + linkSessionToJourney stubs) — low priority, access guard is already covered by AC1.7.

---

## DoD Observations

1. p2.2 is test-only (zero production code changes). All 7 ACs verified by the 27-test suite without touching any source file. This is the lightest possible delivery: the implementation debt (p2.1) was already paid; p2.2 just proves it.

2. Merge of PR #395 produced a three-way conflict in pipeline-state.json (master had 7 local commits ahead of origin). Resolved by keeping HEAD's branch-setup fields over the incoming definition-of-ready fields, then applied post-merge advances on master per the B2 epic-nested bookkeeping rule.

3. M2 metric is now active. p2.1 established the storage isolation; p2.2 confirms it via automated two-tenant scenario. Phase 3 (Postgres/Redis persistence) is deferred. Phase 4 (p4.1 — prompt-cache key scoping) and Phase 5 (p5.1 — adversarial path-traversal audit) are next eligible stories, both at test-plan stage.
