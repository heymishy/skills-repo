# Definition of Done: Thread session into prompt-cache scoping (activate Decision 8)

**PR:** https://github.com/heymishy/skills-repo/pull/475 | **Merged:** 2026-07-14
**Story:** artefacts/2026-06-22-wuce-multi-tenancy/stories/s6.1-thread-session-into-cache-scope.md
**Test plan:** artefacts/2026-06-22-wuce-multi-tenancy/test-plans/s6.1-test-plan.md
**DoR artefact:** artefacts/2026-06-22-wuce-multi-tenancy/dor/s6.1-dor.md
**Assessed by:** Claude (agent) — retroactive DoD backlog pass, 2026-08-17
**Date:** 2026-08-17

---

## AC Coverage

| AC | Satisfied? | Evidence | Verification method | Deviation |
|----|-----------|----------|---------------------|-----------|
| AC1–AC4 (tenant-scoped cache-key divergence, non-streaming + streaming, no-tenantId fallback) | ✅ | `check-s6.1-cache-scope-session-threading.js`, 4/4 assertions | Automated test, re-run fresh on current master 2026-08-17 | None |
| AC5 (`buildCacheKey()`/`_anthropicSystem()` bodies unchanged — only params threaded through) | ✅ | `gh pr view 475 --json files` confirms `src/web-ui/adapters/cache-key.js` was NOT touched by this PR — only `skill-turn-executor.js` and `skills.js` (route wiring) | Direct PR diff inspection, 2026-08-17 | None |
| AC6 (5 named regression suites unaffected: `wuce26`, `p4.1-tenant-cache-ratelimit`, `mfc1`, `bri-s3.1`, `pla-s2`) | ✅ | Re-ran all 5 fresh: `wuce26` 14/14, `p4.1` 11/11, `bri-s3.1` 20/20, `pla-s2` clean, `mfc1` 21/25 with the 4 failures all pre-existing/environmental (3× missing local `ANTHROPIC_API_KEY`, 1× already listed in `tests/known-baseline-failures.json`) | Automated, re-run fresh 2026-08-17 | None — pre-existing failures confirmed unrelated |
| AC7 (own test suite passes) | ✅ | Same as AC1-4 | Automated | None |

This closes the `acVerified: 6/7` gap recorded in `pipeline-state.json` at merge time — AC5 was the unverified one; confirmed here via direct PR diff inspection.

---

## Scope Deviations

None. This story's own DoR contract flagged a genuine ambiguity (whether it needed to touch `server.js`) which was resolved and documented at implementation time — see `decisions.md` Decision 12 and the story's own `notes` field ("Fix-forward: activates Decision 8 (RISK-ACCEPT, 2026-06-24)").

---

## Test Plan Coverage

**Tests passing in CI:** 4/4 (own suite) + 5 regression suites re-confirmed, re-run fresh 2026-08-17.
**Gaps:** None. `mfc1`'s 4 pre-existing failures are documented in `tests/known-baseline-failures.json` and are unrelated to this story (verified: 3 are local-environment `ANTHROPIC_API_KEY` absence, not code issues; the 4th, `T3.2`, is a separately-known baseline flake).

---

## NFR Status

No red flags found in this pass. This story closes a real tenant cross-contamination risk in prompt-cache scoping (two tenants with identical prompts would otherwise share a cache entry) — security-relevant and correctly verified.

---

## Metric Signal

No formal benefit-metric artefact exists for this feature. No metric signal to record.

---

## Outcome

**COMPLETE**

**Follow-up actions:** None.

---

## DoD Observations

1. ~5 weeks live in production, no incidents reported. This story's own `acVerified: 6/7` state (one AC unconfirmed since merge) was closed out in this retroactive pass via direct PR diff inspection — a good example of a DoD gap that was genuinely small and quick to close once looked at, not requiring any new code or test work.
2. Closes out the 9-story `2026-06-22-wuce-multi-tenancy` retroactive DoD batch. See `p1.1-dod.md` for the note on the 5 stories (`s3.1`, `s3.2`, `s4.1`, `s4.2`, `s5.1`) whose DoD artefacts already existed and only needed a `pipeline-state.json` bookkeeping sync.
