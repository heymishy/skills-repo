# Definition of Done: s4.1 — Production deployment and end-to-end smoke test

**Operator:** Hamish King | **Date:** 2026-06-29
**Story:** artefacts/2026-06-22-wuce-multi-tenancy/stories/s4.1.md
**Test plan:** artefacts/2026-06-22-wuce-multi-tenancy/test-plans/s4.1-test-plan.md
**DoR artefact:** artefacts/2026-06-22-wuce-multi-tenancy/dor/s4.1-dor.md

---

## AC Coverage

| AC | Satisfied? | Evidence | Verification method | Deviation |
|----|-----------|----------|---------------------|-----------|
| AC1 | ✅ | `fly secrets list --app skills-framework` confirms all 7 required secrets present: DATABASE_URL, UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN, TENANT_ORG_ALLOWLIST, MAX_JOURNEYS_PER_TENANT, GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET | `scripts/smoke-test-production.sh` AC1 — 7/7 PASS — 2026-06-29 | None |
| AC2 | ✅ | `fly logs` inspection of startup sequence showed no `PG write error`, `loadAllFromPg failed`, or `Redis load error` lines | `scripts/smoke-test-production.sh` AC2 — PASS — 2026-06-29 | None |
| AC3 | ✅ | `GET https://skills-framework.fly.dev/health` returned HTTP 200 | `scripts/smoke-test-production.sh` AC3 — PASS — 2026-06-29 | None |
| AC4 | ✅ | New journey created via UI; Neon SQL editor `SELECT journey_id, tenant_id FROM journeys ORDER BY created_at DESC LIMIT 1` returned a row with correct journey_id and tenant_id | Operator manual: Neon SQL editor — 2026-06-29; journey_id and tenant_id confirmed | None |
| AC5 | ✅ | Confirmed `GET /dashboard` returns 200 (authenticated); ran `fly machine restart --app skills-framework`; `GET /dashboard` returned 200 within 30 seconds without re-authentication (Redis session load confirmed by `[auth] login {"userId":1459496}` log entry on startup) | Operator manual: browser + fly machine restart — 2026-06-29 | None |
| AC6 | ✅ | Set `MAX_JOURNEYS_PER_TENANT=1`; with 1 existing journey, attempted `+ New feature` via UI; received HTTP 402 "Journey limit reached — You have reached the maximum of 1 journey for your account"; cap reset to 5 | Operator manual: browser UI + fly secrets set — 2026-06-29 | None |

---

## Scope Deviations

None against story ACs. Two bugs surfaced and fixed during smoke test execution (not AC deviations — production bug fixes):
- `journey.js:154` — `createdAt` Date object vs string in card render (commit `f5e7477`)
- `journey.js:276` — `createdAt` Date object vs string in sort comparator (commit `66555f7`)
Both bugs pre-existed in code merged before s4.1; surfaced only when Postgres data was present in production.

Also surfaced during s4.1 execution: SKILL_EXECUTOR_PROVIDER defaulted to `copilot`, which returned HTTP 400 for claude-sonnet-4-6. Fixed as production hotfix (commit `8c21dcd`); retroactive story s4.3 satisfies artefact-first rule.

---

## Test Plan Coverage

**Tests from plan implemented:** 6 / 6 (all smoke test ACs executed and passing — automated AC1–AC3 via smoke-test-production.sh; manual AC4–AC6 per guided prompts).

---

## Out-of-Scope Check

Load testing, automated CI/CD redeploy, and rollback procedure all confirmed out of scope and not implemented.

---

## NFR Check

**Secrets not committed:** All 7 required secrets confirmed present only as Fly.io secrets; none in any committed file. ✅
**accessToken not in session persistence:** Confirmed by s3.2 AC3 and production operation. ✅

---

**Status: PASS — all 6 ACs satisfied, no HIGH findings, no scope deviations from story.**
