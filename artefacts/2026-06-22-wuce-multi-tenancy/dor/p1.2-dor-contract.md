# DoR Contract — p1.2: Session shape + journey tenantId + isSameTenant enforcement

**Date:** 2026-06-23
**Approved:** Yes — all hard blocks passed
**schemaDepends:** []

---

## What will be built

1. `createJourney()` (location: confirm by reading — likely `src/web-ui/adapters/journey-store.js`) — add `journey.tenantId = session.tenantId || undefined` when creating a new journey; tenantId is optional (null/undefined for single-user mode)
2. Specific test files in `tests/` where the handler under test reads `session.tenantId` — update fixture from 3-field `{ accessToken, userId, login }` to 4-field `{ accessToken, userId, login, tenantId }` for cross-tenant test scenarios only
3. `tests/check-p1.2-tenant-session-journey.js` — new test file covering AC1 (createJourney stores tenantId), AC2/AC3/AC4 (isSameTenant behavior — already implemented), AC6 (cross-tenant 404 end-to-end), AC7 (regression), AC8 (automated)

**isSameTenant() is NOT modified** — already correctly implemented in `src/web-ui/middleware/journey-access.js` at Phase 0 (lines 5–8): returns true when either side null/undefined; false when both differ.

## What will NOT be built

- Storage namespacing — repoRoot or SESSION_STORE_PATH changes are Phase 2 (p2.1)
- Global fixture update across ALL test files — only tests for handlers that read `session.tenantId` are updated (AC5)
- Postgres/Redis persistence — Phase 3
- Modification to `session-store.js` — NFR-sec-no-accesstoken-disk must be preserved

## AC verification approach

| AC | Test approach | Type |
|----|---------------|------|
| AC1 — createJourney stores tenantId | Integration test: 4-field session → returned journey.tenantId === 'my-org' | Integration |
| AC2 — isSameTenant returns false (diff) | Unit assertion (already implemented; test verifies existing behavior) | Unit |
| AC3 — isSameTenant returns true (match) | Unit assertion | Unit |
| AC4 — isSameTenant passthrough (null/undefined) | Unit assertion; 4 sub-cases | Unit |
| AC5 — fixture scope (code review) | Code review at PR: list of changed vs unchanged test files; confirm unchanged tests still pass (AC7) | Code review |
| AC6 — cross-tenant read returns 404 | Integration test: alice (org-a) calling handler for bob's journey (org-b) → 404 | Integration |
| AC7 — pre-existing suite no regression | Run all check-*.js files; exit 0 for each | Automated regression |
| AC8 — p1.2 test suite passes | node tests/check-p1.2-tenant-session-journey.js; exit 0 | Automated |

## Assumptions

- `createJourney()` takes a session argument (or equivalent context) from which `session.tenantId` can be read — confirm by reading the actual function signature before implementing
- `isSameTenant()` behavior is already correct — verified by code inspection during DoR run
- `journey.tenantId` is optional on the journey object — null/undefined for pre-Phase-1 journeys; triggers permissive passthrough (AC4)

## Estimated touch points

**Files modified:** `src/web-ui/adapters/journey-store.js` (or equivalent — confirm path); specific `tests/check-*.js` files (only those for handlers reading session.tenantId)
**Files created:** `tests/check-p1.2-tenant-session-journey.js`
**Services:** None — in-memory store only; no external calls
**APIs:** None
