# Definition of Ready: p1.2 — Session shape + journey tenantId + isSameTenant enforcement

**Story reference:** artefacts/2026-06-22-wuce-multi-tenancy/stories/p1.2.md
**Test plan reference:** artefacts/2026-06-22-wuce-multi-tenancy/test-plans/p1.2-test-plan.md
**Review reference:** artefacts/2026-06-22-wuce-multi-tenancy/review/p1.2-review-1.md
**Verification script:** artefacts/2026-06-22-wuce-multi-tenancy/verification-scripts/p1.2-verification.md
**Assessed by:** /definition-of-ready skill (claude-sonnet-4-6)
**Date:** 2026-06-23

---

## Implementation note — isSameTenant() already correct

Read-before-DoR: `src/web-ui/middleware/journey-access.js` line 5–8 confirms `isSameTenant()` is already implemented with the correct Phase 1 logic (returns `true` when either side is null/undefined; returns `false` when both have tenantId and they differ). No change to `isSameTenant()` is required by p1.2. The story's AC2/AC3/AC4 will be satisfied by the existing implementation once tenantId is populated on journey and session (the work of this story).

This is consistent with the p0.1 DoR Coding Agent Instructions which explicitly stated: "implement correctly now so Phase 1 only needs to populate tenantId, not change this function."

---

## Hard Blocks

| # | Check | Status | Notes |
|---|-------|--------|-------|
| H1 | User story in As / Want / So format with named persona | ✅ | "As a solo founder / operator, I want `tenantId` stored on both the session and the journey at create time, and `isSameTenant()` to enforce the real tenant boundary, So that M2 is enforced at the application layer" |
| H2 | At least 3 ACs in Given / When / Then format | ✅ | 8 ACs; all Given/When/Then |
| H3 | Every AC has at least one test in the test plan | ✅ | AC1: integration; AC2/AC3/AC4: unit; AC5: code review at DoR; AC6: integration; AC7: regression run; AC8: automated |
| H4 | Out-of-scope section populated | ✅ | 3 items: storage namespacing (p2.1), global fixture update, Postgres (Phase 3) |
| H5 | Benefit linkage references a named metric | ✅ | M2 (Cross-tenant journey data leakage prevention) named in Metric linkage section |
| H6 | Complexity rated | ✅ | Complexity 2 |
| H7 | No unresolved HIGH findings from review | ✅ | 0 HIGH findings; review PASS — Run 1 |
| H8 | Test plan no uncovered ACs | ✅ | AC5 gap acknowledged (code review, not automated test); no UNCERTAIN items in gap table |
| H8-ext | Cross-story schema dependency check | ✅ | schemaDepends: [] — p1.2 adds session.tenantId (runtime) and journey.tenantId (runtime); no pipeline-state.json schema fields required |
| H9 | Architecture constraints populated; no Category E HIGH findings | ✅ | 5 constraints; review Category E score 5/5 (PASS) |
| H-E2E | No unmitigated CSS-layout-dependent ACs | ✅ | No CSS-layout ACs; pure session/journey/middleware behaviour |
| H-NFR | NFR profile exists | ✅ | artefacts/2026-06-22-wuce-multi-tenancy/nfr-profile.md present |
| H-NFR2 | Compliance NFRs have documented sign-off | ✅ | No external compliance frameworks; not applicable |
| H-NFR3 | Data classification populated in NFR profile | ✅ | Present in NFR profile (Journey data: Internal; Session data: Confidential; tenantId: Internal) |
| H-NFR-profile | NFR profile present (story has NFRs) | ✅ | Story has NFRs section; nfr-profile.md exists |
| H-GOV | Approved By section in discovery.md | ✅ | Section added at p1.1 DoR: "Hamish King — Solo Founder / Operator — 2026-06-22" |
| H-ADAPTER | Injectable adapter wiring check (D37) | ✅ | p1.2 introduces no new injectable adapters; isSameTenant() updated in-place; createJourney() modified in-place; D37 not triggered |

**All 17 hard blocks PASS. Story may proceed.**

---

## Warnings

| # | Check | Status | Risk if proceeding | Acknowledged by |
|---|-------|--------|--------------------|-----------------|
| W1 | NFRs populated | ✅ | — | Session cookie shape unchanged; NFR-sec-no-accesstoken-disk preserved (session-store.js not modified) |
| W2 | Scope stability declared | ✅ | — | Stable (pending OQ6 fixture audit — addressed in AC5) |
| W3 | MEDIUM findings acknowledged in /decisions | ✅ | — | Finding 4-M1 RESOLVED via Decision 6; finding 4-L1 LOW accepted |
| W4 | Verification script reviewed by domain expert | ⚠️ | LOW — sole operator reviews on PR | Acknowledged: sole operator (Hamish King) reviews on PR; 8 scenarios directly map to ACs |
| W5 | No UNCERTAIN gap table items | ✅ | — | AC5 gap is "code review" not UNCERTAIN; mitigated by DoR scope confirmation (below) |

**AC5 fixture scope — DoR code review check (W5 mitigation):**
The handlers that read `session.tenantId` after p1.2 are: any handler that calls `createJourney()` (which will now store `journey.tenantId = session.tenantId`). The session fixture update to 4-field shape must be applied in those specific tests. At PR review, the coding agent must add a comment listing every test file changed and confirming each changed handler reads `session.tenantId`. Tests for handlers that do NOT read `session.tenantId` must remain at 3-field shape and continue to pass (verified by AC7 regression test).

---

## Coding Agent Instructions

```
## Coding Agent Instructions

Proceed: Yes
Story: p1.2 — Session shape + journey tenantId + isSameTenant enforcement
        artefacts/2026-06-22-wuce-multi-tenancy/stories/p1.2.md
Test plan: artefacts/2026-06-22-wuce-multi-tenancy/test-plans/p1.2-test-plan.md
Verification script: artefacts/2026-06-22-wuce-multi-tenancy/verification-scripts/p1.2-verification.md

PREREQUISITE: p1.1 must be merged before p1.2 implementation begins.
session.tenantId is set by p1.1 at OAuth callback. p1.2 consumes it from the session.

IMPORTANT — isSameTenant() is ALREADY CORRECTLY IMPLEMENTED:
Read src/web-ui/middleware/journey-access.js before writing any code.
Line 5-8: isSameTenant() returns true when either side is null/undefined,
false when both have tenantId and they differ.
DO NOT change isSameTenant() — it was written to the Phase 1 spec in p0.1.
ACs 2, 3, 4 are already satisfied by the existing implementation.

## TASK ORDER:

Task 1 — Write tests/check-p1.2-tenant-session-journey.js (RED phase)
  Write all tests. They must FAIL before any implementation changes.
  Tests needed:
  - AC1: createJourney with 4-field session → journey.tenantId === 'my-org'
  - AC2: isSameTenant({tenantId:'org-a'},{tenantId:'org-b'}) === false
  - AC3: isSameTenant({tenantId:'org-a'},{tenantId:'org-a'}) === true
  - AC4 (x4): isSameTenant passthrough for null/undefined — all 4 sub-cases
  - AC6: cross-tenant read handler → 404 (alice tenantId org-a, journey tenantId org-b)
        Also: same-tenant read → not 4xx (alice tenantId org-b, journey tenantId org-b)
  - AC7: regression — run node tests/check-p0.1-journey-access.js and
         node tests/check-p0.2-journey-guard-wiring.js inline
  - AC8: all assertions pass, exit 0

Task 2 — Modify createJourney() to store tenantId (AC1)
  Find createJourney() — likely in src/web-ui/adapters/journey-store.js or
  a similar store module. Read the file before modifying.
  Add: when creating a new journey object, include tenantId from the session:
    journey.tenantId = (args.session && args.session.tenantId) || undefined;
  The tenantId is optional — null/undefined for journeys created in single-user
  mode (no TENANT_ORG_ALLOWLIST) triggers the permissive passthrough in isSameTenant().

Task 3 — Update test session fixtures to 4-field shape where required (AC5)
  Grep all tests/ files for makeReq() or session fixture patterns.
  For each test file:
    - If the handler under test calls requireJourneyAccess() AND the test is
      asserting on tenant behaviour (cross-tenant denial or same-tenant grant),
      update the session fixture to 4-field shape:
      { accessToken: 'tok', userId: '1', login: 'alice', tenantId: 'org-a' }
    - If the handler does NOT read session.tenantId and the test is not a
      cross-tenant test, leave the fixture at 3-field shape.
  List every changed test file in a PR comment with a one-line note for each:
  "Changed: [file] — handler [name] reads session.tenantId in AC [n] scenario"

## Files to create:
  tests/check-p1.2-tenant-session-journey.js

## Files to modify:
  src/web-ui/adapters/journey-store.js  (or wherever createJourney() lives — confirm by reading)
  Specific tests/check-*.js files       (only those for handlers that read session.tenantId)

## Files you MUST NOT touch:
  src/web-ui/middleware/journey-access.js   (isSameTenant is already correct; do NOT change it)
  src/web-ui/auth/oauth-adapter.js          (p1.1 work)
  src/web-ui/routes/auth.js                 (p1.1 work)
  src/web-ui/server.js                      (p1.1 wiring)
  src/web-ui/adapters/session-store.js      (NFR-sec-no-accesstoken-disk; do NOT modify)

## Security constraints:
  - req.session.accessToken is the canonical field name — unchanged
  - accessToken must NOT be written to disk — session-store.js strip-before-write
    must not be affected; this story does not modify session-store.js
  - session.tenantId may appear on disk (it is not sensitive)

## PR comment requirement (AC5 verification):
  Include a comment in the PR listing:
  - Every test file changed to 4-field session shape
  - Every test file intentionally left at 3-field shape
  - Confirmation that all check-*.js files pass (AC7)

Oversight level: Medium
Open a draft PR when all tests pass — do not mark ready for review.
If createJourney() signature differs from what the test plan assumes,
describe the actual signature in a PR comment.
```

---

## Sign-off

**Oversight level:** Medium
**Sign-off required:** No (Medium — sole operator; proceed to implementation)
**Signed off by:** Not required — proceed to implementation
