## Definition of Ready: Wire guard into all KNOWN BUG routes

**Story reference:** artefacts/2026-06-22-wuce-multi-tenancy/stories/p0.2.md
**Test plan reference:** artefacts/2026-06-22-wuce-multi-tenancy/test-plans/p0.2-test-plan.md
**Review reference:** artefacts/2026-06-22-wuce-multi-tenancy/review/p0.2-review-1.md
**Assessed by:** /definition-of-ready skill
**Date:** 2026-06-22

---

## Hard Blocks

| # | Check | Status | Notes |
|---|-------|--------|-------|
| H1 | User story is in As / Want / So format with a named persona | ✅ | "As a solo founder / operator, I want all 9 KNOWN BUG route handlers to call requireJourneyAccess()… So that T3-M1 is closed to 0 and M1 reaches 100%" |
| H2 | At least 3 ACs in Given / When / Then format | ✅ | 14 ACs; all Given/When/Then |
| H3 | Every AC has at least one test in the test plan | ✅ | 13 integration tests; AC14 is a meta-assertion (satisfied when all others pass); all 12 behaviour ACs (AC1–AC13) have dedicated test entries |
| H4 | Out-of-scope section is populated — not blank or N/A | ✅ | 4 items: session shape change (p1.2), tenant isolation (Phase 1), routes outside journey.js, response body format for authorized callers |
| H5 | Benefit linkage field references a named metric | ✅ | M1 (Authorization coverage rate) and T3-M1 (KNOWN BUG closure — 9 open → 0) both named |
| H6 | Complexity is rated | ✅ | Complexity 2 |
| H7 | No unresolved HIGH findings from the review report | ✅ | 0 HIGH findings; review verdict: PASS — Run 1 |
| H8 | Test plan has no uncovered ACs | ✅ | All 14 ACs covered; one gap noted (handler names confirmed by grep before impl) — explicitly acknowledged in test plan gap table |
| H9 | Architecture constraints populated; no Category E HIGH findings | ✅ | Architecture constraints populated; review Category E score: 5/5 (PASS); ADR-024 response shape explicitly cited; req.session.accessToken canonical name stated; path-traversal preservation mandated |
| H-E2E | No unmitigated CSS-layout-dependent ACs | ✅ | No CSS-layout ACs; server-side route handler logic |

**All hard blocks PASS. Story may proceed.**

**Dependency gate:** p0.1 must be merged to master before the coding agent begins p0.2 implementation (`require('../middleware/journey-access')` must resolve).

---

## Warnings

| # | Check | Status | Risk if proceeding | Acknowledged by |
|---|-------|--------|--------------------|-----------------|
| W1 | NFRs are identified | ✅ | — | NFR: no performance regression for authorized callers (synchronous guard, no I/O); NFR test included in test plan |
| W2 | Scope stability declared | ✅ | — | Stable |
| W3 | MEDIUM review findings acknowledged in /decisions | ✅ | — | Finding 2-M1 (benefit coverage matrix): RESOLVED — matrix added to benefit-metric.md; see decisions.md Decision 6. Finding 2-L1 (artefact path refs): LOW — non-blocking; accepted |
| W4 | Verification script reviewed by a domain expert | ⚠️ | Test file not yet written — coding agent writes it first (RED phase). Tests use req/res mocks so fidelity depends on mock completeness | Risk mitigated: test plan specifies exact makeReq/makeRes factory shapes; sole operator reviews the PR. Complexity 2 — risk LOW |
| W5 | No UNCERTAIN test plan gaps left unaddressed | ⚠️ | Handler name confirmation (module.exports grep) is a pre-implementation step in the test plan. If handler names differ, the test file must be updated before tests can pass | DoR instruction below mandates the grep as Step 0; the coding agent must not write any test assertions until handler names are confirmed |

---

## B1/D1 Contract Consistency Check

Per CLAUDE.md rule (B1/D1): "DoR contract must not contradict the test plan."

Confirmed: the "MUST NOT touch" list below excludes `skills.js`, `auth.js`, and files outside `journey.js`. The test plan asserts on handlers inside `src/web-ui/routes/journey.js` and the new test file only. No conflict. ✅

---

## Coding Agent Instructions

```
## Coding Agent Instructions

Proceed: Yes (dependency: p0.1 must be merged first)
Story: Wire guard into all KNOWN BUG routes
        artefacts/2026-06-22-wuce-multi-tenancy/stories/p0.2.md
Test plan: artefacts/2026-06-22-wuce-multi-tenancy/test-plans/p0.2-test-plan.md

Goal:
Wire requireJourneyAccess() from journey-access.js into all 11 route handlers
in src/web-ui/routes/journey.js and write the integration test suite.
Tests must fail before wiring (RED), then pass after implementation (GREEN).

New files to create:
  tests/check-p0.2-journey-guard-wiring.js

Files you MAY modify:
  src/web-ui/routes/journey.js              (required — wiring target)

Files you MUST NOT touch:
  src/web-ui/middleware/journey-access.js   (read-only — output of p0.1)
  src/web-ui/routes/skills.js
  src/web-ui/routes/auth.js
  src/web-ui/server.js
  Any existing tests/check-*.js file
  Any file not listed above

STEP 0 — REQUIRED BEFORE WRITING ANY CODE OR TESTS:
  Run: grep -n "module.exports" src/web-ui/routes/journey.js
  Record the exact exported handler names. Confirm that the 9 KNOWN BUG
  read handlers and 2 mutating handlers in the story match the actual
  export names. If any name differs from the story's AC list, add a PR
  comment identifying the discrepancy — do not silently rename.

Wiring rules:
  1. The 9 KNOWN BUG read handlers (AC1–AC9) must each call:
       requireJourneyAccess(journey, req.session, POLICY.TENANT)
     before returning any journey data. On error, call:
       asHttpResponse(err, POLICY.TENANT)
     and return the resulting HTTP status (404 for FORBIDDEN or NOT_FOUND,
     401 for UNAUTHENTICATED).

  2. The 2 mutating handlers (handlePostJourneyRecommit,
     handlePostJourneyStageCommit) must have their existing inline ownerId
     checks REPLACED (not supplemented) with:
       requireJourneyAccess(journey, req.session, POLICY.OWNER)
     On error: asHttpResponse(err, POLICY.OWNER) → 403 for FORBIDDEN.

  3. The guard call must come BEFORE any data access or downstream logic —
     the function must not return journey data and then check the guard.

  4. ADR-024 response shape contract: for AUTHORIZED callers, the response
     body shape must be identical to pre-p0.2 behaviour. The required fields
     (turns, stages, completedStages, stage, ownerId, activeSkill) must still
     be present in the GET /api/journey/:id response.

  5. Existing path-traversal guards in journey.js must NOT be removed,
     commented out, or weakened.

Session fixture shape in tests (3-field — do NOT add tenantId):
  { accessToken: 'tok-alice', userId: '1', login: 'alice' }
  (tenantId is added in Phase 1/p1.2 — do not pre-empt it here)

Test factory pattern:
  const makeReq = (overrides) => ({
    session: { accessToken: 'tok', userId: '1', login: 'alice' },
    params: {}, body: {}, ...overrides
  });
  const makeRes = () => {
    let _status, _body;
    return {
      status(s) { _status = s; return this; },
      json(b) { _body = b; },
      redirect(s) { _status = s; },
      _get: () => ({ status: _status, body: _body })
    };
  };

Use freshRequire() (or delete require.cache entries) between tests to
prevent module state leaking between test cases.

Constraints:
  - Plain Node.js only — no npm dependencies in the test file
  - req.session.accessToken is the canonical authentication field name —
    never req.session.token
  - isSameTenant() returns true in Phase 0 (no tenantId on sessions or
    journeys yet) — do not add any tenantId checks or Phase 1 logic
  - Architecture standards: read .github/architecture-guardrails.md
    before implementing (confirm no active ADRs affect journey.js)

Oversight level: Medium
Open a draft PR when all 13 tests pass — do not mark ready for review.
If handler names discovered in Step 0 differ from the story's AC list,
add a PR comment before proceeding.
```

---

## Sign-off

**Oversight level:** Medium
**Sign-off required:** No (Medium — sole operator is author; will review PR)
**Signed off by:** Not required — proceed to implementation once p0.1 is merged
