# Definition of Ready: p1.1 — OAuth scope + tenant resolution

**Story reference:** artefacts/2026-06-22-wuce-multi-tenancy/stories/p1.1.md
**Test plan reference:** artefacts/2026-06-22-wuce-multi-tenancy/test-plans/p1.1-test-plan.md
**Review reference:** artefacts/2026-06-22-wuce-multi-tenancy/review/p1.1-review-1.md
**Verification script:** artefacts/2026-06-22-wuce-multi-tenancy/verification-scripts/p1.1-verification.md
**Assessed by:** /definition-of-ready skill (claude-sonnet-4-6)
**Date:** 2026-06-23

---

## Hard Blocks

| # | Check | Status | Notes |
|---|-------|--------|-------|
| H1 | User story in As / Want / So format with named persona | ✅ | "As a solo founder / operator, I want… So that M2 (Cross-tenant journey data leakage prevention) moves toward its target" |
| H2 | At least 3 ACs in Given / When / Then format | ✅ | 9 ACs (8 original + AC9 added at DoR); all Given/When/Then |
| H3 | Every AC has at least one test in the test plan | ✅ | AC1–AC7 each have a unit test; AC8 is the automated run; AC9 covered by code-review + verification script edge case |
| H4 | Out-of-scope section populated | ✅ | 5 explicit items: journey tenantId write, isSameTenant enforcement, fixture update, multi-org disambiguation, GHE token URL fix |
| H5 | Benefit linkage references a named metric | ✅ | M2 (Cross-tenant journey data leakage prevention) named in Metric linkage section |
| H6 | Complexity rated | ✅ | Complexity 2 |
| H7 | No unresolved HIGH findings from review | ✅ | 0 HIGH findings; review PASS — Run 1 |
| H8 | Test plan no uncovered ACs | ✅ | All 9 ACs covered; 2 acknowledged risks in gap table — both have mitigations; no UNCERTAIN items |
| H8-ext | Cross-story schema dependency check | ✅ | schemaDepends: [] — p1.1 adds session.tenantId to runtime session object only; no pipeline-state.json schema fields required |
| H9 | Architecture constraints populated; no Category E HIGH findings | ✅ | 5 constraints in Architecture Constraints; review Category E score 5/5 (PASS) |
| H-E2E | No unmitigated CSS-layout-dependent ACs | ✅ | No CSS-layout ACs; pure OAuth/session behaviour |
| H-NFR | NFR profile exists | ✅ | artefacts/2026-06-22-wuce-multi-tenancy/nfr-profile.md present |
| H-NFR2 | Compliance NFRs have documented sign-off | ✅ | NFR profile: no external compliance frameworks; not applicable |
| H-NFR3 | Data classification populated in NFR profile | ✅ | Journey data: Internal; Session data (accessToken): Confidential; tenantId: Internal |
| H-NFR-profile | NFR profile present (story has NFRs) | ✅ | Story has NFRs section; nfr-profile.md exists |
| H-GOV | Approved By section in discovery.md | ✅ | Section added: "Hamish King — Solo Founder / Operator — 2026-06-22". Non-engineering role (founder/product owner). M1 metric signal: sole-operator self-approval; no independent reviewer available for personal project. **Pre-DoR fix:** section was absent; added before sign-off per H-GOV AC3 resolution. |
| H-ADAPTER | Injectable adapter wiring check (D37) | ✅ | setFetchOrgs() introduced; (a) AC9 added to story explicitly scoping production wiring in server.js; (b) stub default must throw — exact error message in AC7; (c) implementation plan must name wiring as separate task (enforced in Coding Agent Instructions). **Pre-DoR fix:** AC9 added before sign-off per H-ADAPTER resolution. |

**All 17 hard blocks PASS. Story may proceed.**

---

## Warnings

| # | Check | Status | Risk if proceeding | Acknowledged by |
|---|-------|--------|--------------------|-----------------|
| W1 | NFRs populated | ✅ | — | Async pagination, 5s timeout, allowlist non-disclosure all specified |
| W2 | Scope stability declared | ✅ | — | Stable (OQ4/OQ5 from discovery both addressed in ACs) |
| W3 | MEDIUM findings acknowledged in /decisions | ✅ | — | Finding 3-M1 RESOLVED via Decision 6 (benefit-metric.md updated); finding 3-L1 LOW accepted as-is |
| W4 | Verification script reviewed by domain expert | ⚠️ | LOW — sole operator reviews on PR | Acknowledged: Hamish King reviews on PR; all 8 scenarios map directly to ACs; injectable adapter design makes all scenarios unit-testable without real API. No /decisions entry required. |
| W5 | No UNCERTAIN gap table items | ✅ | — | 2 risks acknowledged with mitigations (production wiring → AC9; pagination cursor format → DoR confirmation in instructions) |

---

## Coding Agent Instructions

```
## Coding Agent Instructions

Proceed: Yes
Story: p1.1 — OAuth scope + tenant resolution
        artefacts/2026-06-22-wuce-multi-tenancy/stories/p1.1.md
Test plan: artefacts/2026-06-22-wuce-multi-tenancy/test-plans/p1.1-test-plan.md
Verification script: artefacts/2026-06-22-wuce-multi-tenancy/verification-scripts/p1.1-verification.md

Goal:
Implement GitHub org-based tenant resolution at OAuth callback using an injectable
fetchOrgs adapter. Write the test file first (RED), then implement (GREEN). All 9 ACs
must be satisfied.

## TASK ORDER — implement in this sequence (D37 rule 3 requires wiring as a separate task):

Task 1 — Write tests/check-p1.1-oauth-tenant-resolution.js (RED phase)
  Write all 7 unit tests + 3 NFR tests. They must FAIL before any implementation exists.
  Use injectable mock via setFetchOrgs(). Do not call real GitHub API.
  Test file: tests/check-p1.1-oauth-tenant-resolution.js

Task 2 — Modify src/web-ui/auth/oauth-adapter.js (AC1)
  Read the current scope string (confirmed: 'repo,read:user' at line 40).
  Add conditional: when TENANT_ORG_ALLOWLIST is set and non-empty, append 'read:org'.
  When absent or empty: scope unchanged. No other changes to this file.

Task 3 — Add tenant resolution to OAuth callback handler (AC2–AC7)
  Locate the OAuth callback handler (likely src/web-ui/routes/auth.js — confirm by reading).
  Add AFTER the token exchange, BEFORE setting session fields:
    - Parse TENANT_ORG_ALLOWLIST: process.env.TENANT_ORG_ALLOWLIST?.split(',').map(s => s.trim()).filter(Boolean) || []
    - If allowlist is empty → skip org fetch entirely (AC6)
    - If allowlist non-empty → call _fetchOrgs(accessToken) across all pages (AC3 pagination)
    - Find first match in allowlist ORDER (not orgs-response order) (AC2, AC5)
    - Match found → set req.session.tenantId = matchedOrgLogin (AC2)
    - No match → return 403 with message "You are not a member of an authorised organisation."
      Message must NOT contain any org login from the allowlist (AC4 + NFR-sec-allowlist-disclosure)
      Session must NOT be created (AC4)
  Add the injectable adapter in this same module:
    let _fetchOrgs = () => { throw new Error('Adapter not wired: fetchOrgs. Call setFetchOrgs() with a real implementation before use.') };
    function setFetchOrgs(fn) { _fetchOrgs = fn; }
    module.exports.setFetchOrgs = setFetchOrgs; (or equivalent export)
  The _fetchOrgs function signature: async (accessToken, page) => array of { login } objects
    When no more pages: return empty array or null (implementation defines; update test mock accordingly)
    OR return { orgs: [...], nextPage: number|null } — choose one shape and keep consistent
  Pagination: use async/await with a while loop; do NOT use synchronous waits.
  Log a warning (console.warn) if total fetch time exceeds 3000ms.

Task 4 — Wire setFetchOrgs() in src/web-ui/server.js (AC9) — SEPARATE TASK, not merged with Task 3
  Import setFetchOrgs from the OAuth callback module.
  Call setFetchOrgs(realFetchFn) where realFetchFn makes GET /user/orgs calls:
    async function realFetchFn(accessToken, page) {
      const url = `${GITHUB_API_BASE_URL}/user/orgs?per_page=30&page=${page || 1}`;
      const res = await fetch(url, { headers: { Authorization: 'token ' + accessToken } });
      if (!res.ok) return [];
      return await res.json(); // array of { login, ... }
    }
  The wiring call must appear BEFORE the OAuth routes are registered.
  Confirm the pagination contract between realFetchFn and the handler is consistent.

## Files to create:
  tests/check-p1.1-oauth-tenant-resolution.js

## Files to modify:
  src/web-ui/auth/oauth-adapter.js
  src/web-ui/routes/auth.js  (confirm path; do NOT assume — read server.js to find callback route)
  src/web-ui/server.js

## Files you MUST NOT touch:
  src/web-ui/middleware/journey-access.js   (Phase 0 guard — do not modify)
  src/web-ui/routes/journey.js              (Phase 0 wiring — do not modify)
  Any existing tests/check-*.js file        (do not modify existing test files)
  src/web-ui/adapters/session-store.js      (Phase 2 — do not modify)

## Security constraints (non-negotiable):
  - req.session.accessToken is the canonical field name — do not rename or alias
  - accessToken must never be written to disk — the strip-before-write invariant in
    session-store.js must not be affected by anything in p1.1 (p1.1 does not modify session-store.js)
  - The zero-match error message must NOT contain any org login from TENANT_ORG_ALLOWLIST
    (NFR-sec-allowlist-disclosure). Keep the message generic:
    "You are not a member of an authorised organisation."
  - The _fetchOrgs stub default MUST throw — never return null or empty array silently

## Pagination implementation note:
  GitHub's /user/orgs endpoint uses Link header for pagination:
  Link: <https://api.github.com/user/orgs?page=2>; rel="next"
  Parse the Link header to determine if a next page exists.
  The test mock uses a simplified page-number interface (page 1, page 2...);
  the production implementation uses Link header parsing.
  Confirm the exact mock contract you're implementing in Task 3 before writing Task 1.

## Test file conventions:
  - Plain Node.js assert: const assert = require('assert');
  - No test runner (no Jest, no Mocha)
  - Use freshRequire pattern for module isolation between tests (re-require module per test)
  - env var overrides: save and restore process.env.TENANT_ORG_ALLOWLIST per test
  - Run all tests and report results; exit 0 on all pass

## Architecture standards:
  Read .github/architecture-guardrails.md before implementing.
  Guardrails apply to viz/schema/scripts. Confirm no active ADRs affect auth/ or routes/auth.js.

Oversight level: Medium
Open a draft PR when all tests pass — do not mark ready for review.
If you encounter an ambiguity about the pagination contract between the mock and the
real implementation, add a PR comment describing it before marking ready for review.
```

---

## Sign-off

**Oversight level:** Medium
**Sign-off required:** No (Medium — sole operator; self-awareness sufficient; proceed to implementation)
**Signed off by:** Not required — proceed to implementation
