# Definition of Ready — s6.1: Thread session into prompt-cache scoping (activate Decision 8)

**Date:** 2026-07-11
**Story:** artefacts/2026-06-22-wuce-multi-tenancy/stories/s6.1-thread-session-into-cache-scope.md
**Run:** 1
**Assessor:** /definition-of-ready (agent)

---

## Hard block checklist

| # | Gate | Result |
|---|------|--------|
| H1 | Review passed (reviewStatus=passed) | ✅ PASS — 0 HIGH, 0 MEDIUM, 1 LOW (non-blocking) |
| H2 | No HIGH findings | ✅ PASS |
| H3 | Test plan written | ✅ PASS — 7 ACs covered, AC5 explicitly recorded as manual diff-review gap type, no unaddressed gaps |
| H4 | Benefit-metric coverage matrix present | ✅ PASS — confirmed present in `benefit-metric.md` (added at Decision 6, unaffected by this story) |
| H5 | `req.session.accessToken` canonical field unchanged | ✅ PASS — story reads only `req.session.tenantId`, does not touch `accessToken` |
| H6 | Path-traversal guard N/A | ✅ N/A — no disk writes derived from request data introduced by this story |
| H7 | DoR contract does not contradict test plan (B1/D1 check) | ✅ PASS — every file the test plan asserts against (`skill-turn-executor.js`, `routes/skills.js`) is listed as a MODIFY touchpoint in the contract, not excluded |
| H8 | Story dependencies met | ✅ PASS — p4.1 (PR #396) is merged; `buildCacheKey()` and `_anthropicSystem()` confirmed present and unchanged in current `master` |

No warnings requiring RISK-ACCEPT. This story activates existing, already-reviewed p4.1 logic — no new architectural decision beyond what Decision 8 already recorded.

---

## Touch points

| File | Change type |
|------|-------------|
| `src/modules/skill-turn-executor.js` | MODIFY — thread `session` through `skillTurnExecutor()` / `skillTurnExecutorStream()` to `_callAnthropic()` / `_callAnthropicStream()` to `_anthropicSystem()` |
| `src/web-ui/routes/skills.js` | MODIFY — `htmlSubmitTurn()`, `handlePostTurnHtml()`, `handlePostTurnStreamHtml()` pass `req.session.tenantId` + `sessionId` down |
| `tests/check-s6.1-cache-scope-session-threading.js` | NEW — AC1–AC4, AC7 |

---

## Coding agent instructions

1. `src/modules/skill-turn-executor.js`:
   - `_callAnthropic(systemPrompt, history, currentInput, maxTokens, timeoutMs, session)` — add `session` as a 6th, optional parameter; change `system: _anthropicSystem(systemPrompt)` to `system: _anthropicSystem(systemPrompt, session)`
   - `_callAnthropicStream(systemPrompt, history, currentInput, maxTokens, timeoutMs, onChunk, onThinkingChunk, onFirstChunk, noThinking, modelOverride, session)` — add `session` as a new trailing optional parameter; same `_anthropicSystem(systemPrompt, session)` change
   - `skillTurnExecutor(systemPrompt, history, currentInput, token, meta)` — derive `session` from `meta` (`meta.tenantId`/`meta.sessionId`) when present; pass to `_callAnthropic`
   - `skillTurnExecutorStream(...)` — derive `session` from `options` (`options.tenantId`/`options.sessionId`) when present; pass to `_callAnthropicStream`
   - Do not touch `_callCopilot` / `_callCopilotStream` / `_anthropicSystem`'s own body / `buildCacheKey` import

2. `src/web-ui/routes/skills.js`:
   - `htmlSubmitTurn(skillName, sessionId, rawAnswer, token, tenantId)` — add optional 5th `tenantId` parameter; add `tenantId: tenantId || null, sessionId: sessionId` to the existing `_turnMeta` object
   - `handlePostTurnHtml(req, res)` — pass `req.session.tenantId` as the 5th argument at its existing `htmlSubmitTurn(...)` call
   - `handlePostTurnStreamHtml(req, res)` — add `tenantId: req.session.tenantId || null, sessionId: sessionId` to the existing `_turnOptions` object before the `_skillTurnExecutorStream(...)` call
   - Do not touch `htmlRecordAnswer` / `handlePostAnswerHtml`

3. `tests/check-s6.1-cache-scope-session-threading.js` — plain Node.js `assert`, mock `https.request` per `check-wuce26-per-answer-model-response.js` T9/T10 pattern. Cover AC1–AC4, AC7.

4. Re-run and confirm unmodified pass: `check-wuce26-per-answer-model-response.js`, `check-p4.1-tenant-cache-ratelimit.js`, `check-mfc1-model-first-chat-session.js`, `check-bri-s3.1-mock-llm-gateway.js`, `check-pla-s2-posthog-wiring.js` (AC6).

5. Add a `decisions.md` entry recording this activation, referencing Decision 8 — do not duplicate Decision 8's content, only record that it is now activated and by which commit/PR.

6. **req.session.accessToken** — canonical field name; not read or written by this story.

7. **No changes** to `cache-key.js`, `_anthropicSystem()`'s body, `htmlRecordAnswer`, `handlePostAnswerHtml`, `rate-limiter.js`, or `_callCopilot*`.

---

## Proceed

**SIGNED OFF ✅ — 2026-07-11**

All hard blocks PASS. No RISK-ACCEPTs required.
