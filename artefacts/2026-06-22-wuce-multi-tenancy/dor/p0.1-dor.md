## Definition of Ready: Authorization guard module — journey-access.js

**Story reference:** artefacts/2026-06-22-wuce-multi-tenancy/stories/p0.1.md
**Test plan reference:** artefacts/2026-06-22-wuce-multi-tenancy/test-plans/p0.1-test-plan.md
**Review reference:** artefacts/2026-06-22-wuce-multi-tenancy/review/p0.1-review-1.md
**Assessed by:** /definition-of-ready skill
**Date:** 2026-06-22

---

## Hard Blocks

| # | Check | Status | Notes |
|---|-------|--------|-------|
| H1 | User story is in As / Want / So format with a named persona | ✅ | "As a solo founder / operator, I want… So that M1 authorization coverage rate reaches 100%" |
| H2 | At least 3 ACs in Given / When / Then format | ✅ | 11 ACs; all Given/When/Then |
| H3 | Every AC has at least one test in the test plan | ✅ | 12 unit tests across 11 ACs; AC10 covered by 2 tests (null and undefined passthrough sub-cases) |
| H4 | Out-of-scope section is populated — not blank or N/A | ✅ | 3 items: HTTP routing (p0.2), Phase 1 isSameTenant real enforcement (p1.2), error message content |
| H5 | Benefit linkage field references a named metric | ✅ | M1 (Authorization coverage rate) and T3-M1 (KNOWN BUG closure) both named in Metric linkage section |
| H6 | Complexity is rated | ✅ | Complexity 2 |
| H7 | No unresolved HIGH findings from the review report | ✅ | 0 HIGH findings; review verdict: PASS — Run 1 |
| H8 | Test plan has no uncovered ACs (or gaps explicitly acknowledged) | ✅ | All 11 ACs covered; Coverage gaps: None |
| H9 | Architecture constraints field populated; no Category E HIGH findings | ✅ | Architecture constraints populated; review Category E score: 5/5 (PASS); guardrails.md scope is viz/schema/scripts — does not apply to src/web-ui/middleware/ |
| H-E2E | No unmitigated CSS-layout-dependent ACs | ✅ | No CSS-layout ACs; pure business-logic module |

**All hard blocks PASS. Story may proceed.**

---

## Warnings

| # | Check | Status | Risk if proceeding | Acknowledged by |
|---|-------|--------|--------------------|-----------------|
| W1 | NFRs are identified | ✅ | — | NFR: synchronous, no I/O; NFR test included in test plan |
| W2 | Scope stability declared | ✅ | — | Stable |
| W3 | MEDIUM review findings acknowledged in /decisions | ✅ | — | Finding 1-M1 (benefit coverage matrix): RESOLVED — matrix added to benefit-metric.md; see decisions.md Decision 6. Finding 1-L1 (artefact path refs): LOW — non-blocking; accepted as-is |
| W4 | Verification script reviewed by a domain expert | ⚠️ | Test file does not exist yet — coding agent writes it as the first step (RED phase). Unreviewed tests may miss an edge case | Risk is LOW for this story: all 12 tests are pure-function assertions on a simple module with no I/O. Sole operator (Hamish King) will review the PR which includes the test file. |
| W5 | No UNCERTAIN test plan gaps left unaddressed | ✅ | — | No gaps in test plan; all ACs are pure-function testable |

---

## Coding Agent Instructions

```
## Coding Agent Instructions

Proceed: Yes
Story: Authorization guard module — journey-access.js
        artefacts/2026-06-22-wuce-multi-tenancy/stories/p0.1.md
Test plan: artefacts/2026-06-22-wuce-multi-tenancy/test-plans/p0.1-test-plan.md

Goal:
Create src/web-ui/middleware/journey-access.js with the four named exports
and write tests/check-p0.1-journey-access.js. Tests must fail before the
module is written (RED), then pass after implementation (GREEN).

New files to create:
  src/web-ui/middleware/journey-access.js
  tests/check-p0.1-journey-access.js

Files you MUST NOT touch:
  src/web-ui/routes/journey.js              (wiring is story p0.2, not this story)
  src/web-ui/routes/skills.js
  src/web-ui/routes/auth.js
  Any existing tests/check-*.js file

Required exports from journey-access.js:
  requireJourneyAccess(journey, session, policy)
    — throws typed error { code: 'NOT_FOUND' }      when journey is null/undefined
    — throws typed error { code: 'UNAUTHENTICATED' } when session.accessToken is falsy
    — returns without throwing                        when journey.ownerId is null (AC1 legacy passthrough)
    — returns without throwing                        when session.login === journey.ownerId (AC2 owner)
    — throws typed error { code: 'FORBIDDEN' }        when session.login !== journey.ownerId AND isSameTenant() returns false (AC3)
    — note: isSameTenant() always returns true in Phase 0 (no tenantId populated yet),
            so the FORBIDDEN path is only reachable via ownerId mismatch in this phase

  asHttpResponse(err, policy)
    — TENANT policy: FORBIDDEN → 404, UNAUTHENTICATED → 401, NOT_FOUND → 404
    — OWNER policy:  FORBIDDEN → 403, UNAUTHENTICATED → 401, NOT_FOUND → 404

  isSameTenant(journey, session)
    — Phase 0 implementation: returns true when journey.tenantId or session.tenantId
      is null or undefined (either side absent → permissive passthrough)
    — returns false only when BOTH sides have tenantId AND they differ
      (this will be the Phase 1 behaviour; implement correctly now so Phase 1
       only needs to populate tenantId, not change this function)

  POLICY = { TENANT: 'TENANT', OWNER: 'OWNER' }
    — plain object constant, exported for use by route handlers in p0.2

Constraints:
  - Plain Node.js only — no npm dependencies in the new module or test file
  - Test file uses require('assert') and require('../src/web-ui/middleware/journey-access')
    with a relative path — adjust the require path to match the actual directory depth
    from tests/ to src/
  - No I/O in journey-access.js — synchronous pure functions only
  - req.session.accessToken is the canonical authentication token field name —
    the module checks session.accessToken to determine authentication status
  - Do not add a module.exports.default or default export — named exports only
  - Architecture standards: read .github/architecture-guardrails.md before
    implementing. Guardrails apply to viz/schema/scripts — not to
    src/web-ui/middleware/ — but read it to confirm no active ADRs affect this file.

Test file conventions:
  - Use plain Node.js assert: const assert = require('assert');
  - No test runner (no Jest, no Mocha)
  - Each test is a standalone assert call with a descriptive failure message
  - Run all tests at the end: if process exits with code 0, all tests passed
  - Follow the exact test structure from the test plan (12 tests)

Oversight level: Medium
Open a draft PR when all 12 tests pass — do not mark ready for review.
If you encounter an ambiguity not covered by the ACs or tests,
add a PR comment describing the ambiguity and do not mark ready for review.
```

---

## Sign-off

**Oversight level:** Medium
**Sign-off required:** No (Medium — engineering lead awareness; sole operator is author)
**Signed off by:** Not required — proceed to implementation
