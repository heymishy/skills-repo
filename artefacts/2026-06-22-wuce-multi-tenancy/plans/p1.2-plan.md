# Implementation Plan — p1.2: Session shape + journey tenantId + isSameTenant enforcement

**Feature:** wuce-multi-tenancy
**Story:** p1.2
**Worktree:** `.worktrees/p1.2` on `feature/p1.2`
**Baseline:** 44 tests passing

---

## Scope summary

**One code change needed:**
- `src/web-ui/routes/journey.js` — `handlePostJourney`, add `created.tenantId = req.session.tenantId || undefined;` after the existing `created.ownerId = req.session.login || null;` line (~line 298)

**No other source changes needed:**
- `isSameTenant()` in `src/web-ui/middleware/journey-access.js` is already correctly implemented (Phase 0). Returns true when either side null/undefined; returns false when both differ.
- No change to `createJourney()` signature — tenantId is set on the returned object in the route handler, same as ownerId.
- No change to existing test files — no existing test asserts `session.tenantId`; no handler under existing tests reads `session.tenantId`.

---

## Task 1 — RED: Write failing test file

**File:** `tests/check-p1.2-tenant-session-journey.js`

**Test structure (sequential async IIFE, same as p1.1 pattern):**

### Test 1 — AC1: createJourney stores tenantId from session
- Use `freshRequire()` of journey.js
- Mock `setRegisterHtmlSession(noOp)`, `setLinkSessionToJourney(noOp)`
- POST with session `{ accessToken: 'tok', userId: '1', login: 'alice', tenantId: 'my-org' }` and body `featureName='test-feature'`
- After handler, call `journeyStore.listJourneys()` to get the stored journey
- Assert `journey.tenantId === 'my-org'`
- **RED until Task 2**

### Tests 2–4 — AC2/AC3/AC4: isSameTenant unit assertions
- Direct calls to `isSameTenant()` from `journey-access.js`
- AC2: `isSameTenant({ tenantId: 'org-a' }, { tenantId: 'org-b' })` → false
- AC3: `isSameTenant({ tenantId: 'org-a' }, { tenantId: 'org-a' })` → true
- AC4 (4 sub-cases): null journey, undefined session, both absent, null/undefined mix → true

### Test 5 — AC6: cross-tenant read returns 404
- Mock journey store: `{ ownerId: 'bob', tenantId: 'org-b', featureSlug: 'f', completedStages: [], activeSkill: null }`
- Session: `{ accessToken: 'tok', userId: '1', login: 'alice', tenantId: 'org-a' }`
- Call `handleGetJourneyState(req, res)` → assert status 404
- **Passes immediately** (requireJourneyAccess already enforces FORBIDDEN on non-owner; isSameTenant correctness is verified separately by AC2-AC4)

### Test 6 — AC6 regression: owner access not broken by tenantId
- Alice's own journey: `{ ownerId: 'alice', tenantId: 'org-a', featureSlug: 'f', completedStages: [], activeSkill: null }`
- Session: `{ accessToken: 'tok', userId: '1', login: 'alice', tenantId: 'org-a' }`
- Call `handleGetJourneyState(req, res)` → assert status not 4xx

### Tests 7-9 — AC7 regression: pre-existing suites pass
- `execSync('node tests/check-p0.1-journey-access.js', { stdio: 'inherit' })`
- `execSync('node tests/check-p0.2-journey-guard-wiring.js', { stdio: 'inherit' })`
- `execSync('node tests/check-p1.1-oauth-tenant-resolution.js', { stdio: 'inherit' })`

---

## Task 2 — GREEN: Add tenantId assignment in handlePostJourney

**File:** `src/web-ui/routes/journey.js`
**Location:** ~line 297–298 in `handlePostJourney`

```javascript
// Before (existing):
var created = _journeyStore.createJourney(featureSlug, profileName);
var journeyId = created.journeyId;
created.ownerId = req.session.login || null;

// After (add one line):
var created = _journeyStore.createJourney(featureSlug, profileName);
var journeyId = created.journeyId;
created.ownerId  = req.session.login   || null;
created.tenantId = req.session.tenantId || undefined;
```

---

## AC5 verification

No existing test files changed. The only new 4-field session usage is in `tests/check-p1.2-tenant-session-journey.js` for tests that exercise `session.tenantId` behavior.

Handlers that do NOT read `session.tenantId` — unchanged:
- All handlers in check-p0.1, check-p0.2: existing 3-field sessions still work (isSameTenant passthrough when tenantId absent)
- All handlers in check-p1.1: those tests exercise `resolveTenant()` and `buildOAuthRedirectURL()`, not journey creation

---

## Self-review checklist

- [ ] `created.tenantId = req.session.tenantId || undefined` is the only source change
- [ ] `isSameTenant` not modified
- [ ] `createJourney` signature not modified
- [ ] No existing test session fixtures changed
- [ ] NFR-sec-no-accesstoken-disk: `session-store.js` not modified; accessToken strip-before-write invariant unaffected
- [ ] Conflict marker scan before `git add` (wsm/D40 rule)
- [ ] D37 injectable adapter rule: not applicable (no new adapter introduced in p1.2)
