# AC Verification Script: s3.2 — Upstash Redis provisioning and session persistence activation

**Story reference:** artefacts/2026-06-22-wuce-multi-tenancy/stories/s3.2.md
**Technical test plan:** artefacts/2026-06-22-wuce-multi-tenancy/test-plans/s3.2-test-plan.md
**Script version:** 1
**Verified by:** _____ | **Date:** _____ | **Context:** [ ] Pre-code  [ ] Post-merge  [ ] Demo

---

## Setup

**Before you start:**
1. Have your Upstash REST URL and token available (from console.upstash.com, under REST API section).
2. Have the Fly.io CLI installed and authenticated.
3. Know your Fly.io app name.

**Reset between scenarios:** Each scenario is independent. No shared state.

---

## Scenarios

---

### Scenario 1: Upstash REST credentials are valid (AC1)

**Covers:** AC1

**Steps:**
1. Open console.upstash.com and open your Redis database.
2. Navigate to the REST API section. Copy `UPSTASH_REDIS_REST_URL` (starts with `https://`) and `UPSTASH_REDIS_REST_TOKEN`.
3. Test the credentials: `curl https://<your-upstash-url>/set/health-check/ok -H "Authorization: Bearer <your-token>"`

**Expected outcome:**
> The curl command returns a JSON response like `{"result":"OK"}`. No error message appears.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 2: Smoke test passes — write, read, delete (AC2)

**Covers:** AC2

**Steps:**
1. Run: `UPSTASH_REDIS_REST_URL="<url>" UPSTASH_REDIS_REST_TOKEN="<token>" node scripts/smoke-test-redis.js`

**Expected outcome:**
> The script prints, in order:
> ```
>   SET session:smoke-test ... OK
>   GET session:smoke-test ... OK
>   accessToken absent, login present ... OK
>   TTL=<number>s (within [86390, 86400]) ... OK
>   DEL session:smoke-test ... OK
>
> Upstash smoke test PASSED
> ```
> The script exits with code 0. After the script completes, the key `session:smoke-test` no longer exists in Upstash (you can verify in the Upstash console Data Browser).

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 3: accessToken is not stored in Redis (AC3)

**Covers:** AC3 (verified automatically by Scenario 2 — this scenario confirms the log line)

**Steps:**
1. Review the output from Scenario 2.

**Expected outcome:**
> The output from Scenario 2 includes the line: `accessToken absent, login present ... OK`
> This confirms that `accessToken: "should-not-persist"` was stripped before the Redis write and `login: "smoke-user"` is present in the stored value.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 4: Session key TTL is approximately 24 hours (AC4)

**Covers:** AC4 (verified automatically by Scenario 2 — this scenario confirms the log line)

**Steps:**
1. Review the output from Scenario 2.

**Expected outcome:**
> The output includes a line like: `TTL=86399s (within [86390, 86400]) ... OK`
> The TTL number is between 86390 and 86400 (i.e. within 10 seconds of 86400, which is exactly 24 hours).

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 5: Credentials stored as Fly.io secrets, not in code (AC5)

**Covers:** AC5

**Steps:**
1. Run: `fly secrets list --app $APP`
2. Confirm both `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN` appear.
3. Run: `git grep "upstash.io" -- ':!artefacts/' ':!workspace/'`

**Expected outcome:**
> Step 1: Both `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN` appear in the secrets list.
> Step 3: The `git grep` returns zero results — no Upstash URL or token appears in any committed file.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

## Summary

| Scenario | Result | Notes |
|----------|--------|-------|
| Scenario 1 — Credentials valid | | |
| Scenario 2 — Smoke test passes | | |
| Scenario 3 — accessToken not stored | | |
| Scenario 4 — TTL ~24 hours | | |
| Scenario 5 — Credentials as secrets | | |

**Overall verdict:** [ ] All pass — ready to proceed
[ ] Failures found — log findings below before proceeding

---

## Findings

| Scenario | Expected | Actual | Severity | Action |
|----------|----------|--------|----------|--------|
| | | | | |
