# AC Verification Script: p1.2 — Session shape + journey tenantId + isSameTenant enforcement

**Story reference:** artefacts/2026-06-22-wuce-multi-tenancy/stories/p1.2.md
**Technical test plan:** artefacts/2026-06-22-wuce-multi-tenancy/test-plans/p1.2-test-plan.md
**Script version:** 1
**Verified by:** — | **Date:** — | **Context:** [ ] Pre-code  [ ] Post-merge  [ ] Demo

---

## Setup

**Before you start:**
1. Ensure you are on the merged implementation branch (or master after merge).
2. Run `npm install` from the repo root if not done.
3. No environment variables required — all test data is synthetic and inline.

**Reset between scenarios:** Each scenario is self-contained. No reset needed.

---

## Scenarios

---

### Scenario 1: `createJourney()` stores `tenantId` from session on the new journey

**Covers:** AC1

**Steps:**
1. Prepare a 4-field session: `{ accessToken: 'tok', userId: '1', login: 'alice', tenantId: 'my-org' }`.
2. Call `createJourney()` with that session and any valid `featureSlug`.
3. Inspect the returned journey object.

**Expected outcome:**
> `journey.tenantId === 'my-org'` on the returned object.
> The same `tenantId` is also present on the journey record in the in-memory store (not just on the return value).
> If the disk adapter is active: read the stored journey file and confirm `tenantId` is written; also confirm `accessToken` does NOT appear in the disk file (NFR-sec-no-accesstoken-disk).

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 2: `isSameTenant()` returns `false` when tenantIds differ

**Covers:** AC2

**Steps:**
1. Import `isSameTenant` from `src/web-ui/middleware/journey-access.js`.
2. Call `isSameTenant({ tenantId: 'org-a' }, { tenantId: 'org-b' })`.
3. Inspect the return value.

**Expected outcome:**
> Returns `false` — tenantIds are different strings, access should be denied.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 3: `isSameTenant()` returns `true` when tenantIds match

**Covers:** AC3

**Steps:**
1. Import `isSameTenant` from `src/web-ui/middleware/journey-access.js`.
2. Call `isSameTenant({ tenantId: 'org-a' }, { tenantId: 'org-a' })`.
3. Inspect the return value.

**Expected outcome:**
> Returns `true` — identical tenantIds, same tenant.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 4: `isSameTenant()` Phase 0 passthrough — returns `true` when either side is null/undefined

**Covers:** AC4

**Steps:**
1. Import `isSameTenant` from `src/web-ui/middleware/journey-access.js`.
2. Test all four null/undefined sub-cases:
   - a. `isSameTenant({ tenantId: null }, { tenantId: 'org-a' })`
   - b. `isSameTenant({ tenantId: 'org-a' }, { tenantId: undefined })`
   - c. `isSameTenant({ tenantId: null }, { tenantId: undefined })`
   - d. `isSameTenant({}, {})` — both absent (single-user mode)
3. Inspect each return value.

**Expected outcome:**
> All four sub-cases return `true`.
> Journeys created before Phase 1 (no tenantId) and sessions in single-user mode (no TENANT_ORG_ALLOWLIST) must not be blocked.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 5: AC5 fixture scope — code review check

**Covers:** AC5

**Steps:**
1. Run `git diff origin/master -- tests/` (or compare to pre-p1.2 baseline).
2. For each test file that was modified to add `tenantId` to the session fixture, confirm the handler under test actually reads `session.tenantId`.
3. For each test file that was NOT modified, confirm its handler does not read `session.tenantId`.

**Expected outcome:**
> Only test files for handlers that read `session.tenantId` were updated to 4-field session shape `{ accessToken, userId, login, tenantId }`.
> Tests for handlers that do not touch `session.tenantId` remain at 3-field shape and still pass.
> No test file that was passing before p1.2 now fails due to missing `tenantId` (regression would surface in Scenario 7).

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 6: Cross-tenant read returns 404 — full guard chain

**Covers:** AC6

**Steps:**
1. Pre-load journey `{ journeyId: 'j1', ownerId: 'bob', tenantId: 'org-b' }` into the in-memory store.
2. Prepare session `{ accessToken: 'tok', userId: '1', login: 'alice', tenantId: 'org-a' }`.
3. Call `handleGetJourneyState` (or any read handler) with alice's session for bob's journey.
4. Inspect the HTTP response.

**Expected outcome:**
> HTTP 404 response.
> `isSameTenant({ tenantId: 'org-b' }, { tenantId: 'org-a' })` returns `false` → guard throws FORBIDDEN → TENANT policy maps to 404.
> **Also verify same-tenant access is NOT broken:** repeat with session `{ ..., tenantId: 'org-b' }` (matching journey) — response is not 4xx.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 7: Pre-existing test suite does not regress

**Covers:** AC7

**Steps:**
1. Run the Phase 0 suites first: `node tests/check-p0.1-journey-access.js` and `node tests/check-p0.2-journey-guard-wiring.js`.
2. Run `node tests/check-p1.1-oauth-tenant-resolution.js`.
3. Run any other `check-*.js` files present in `tests/`.

**Expected outcome:**
> All previously passing tests still pass. Zero FAIL lines across all suites.
> Exit code 0 for every file.
> p1.2 must not cause regressions in p0.1 (16/16), p0.2 (13/13), or p1.1 test suites.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 8: p1.2 test suite passes (automated)

**Covers:** AC8

**Steps:**
1. Run `node tests/check-p1.2-tenant-session-journey.js`.

**Expected outcome:**
> All assertions pass. Exit code 0.
> Output ends with "All N tests passed." (no FAIL lines).

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Edge case: `accessToken` not written to disk when `tenantId` is added

**Covers:** NFR-sec-no-accesstoken-disk

**Steps:**
1. Create a journey with a 4-field session that includes `accessToken`.
2. If the disk session adapter is active, read the stored session file for that journey.
3. Check whether `accessToken` appears in the JSON.

**Expected outcome:**
> `accessToken` is absent from the disk file.
> `tenantId` may appear on disk (it is not sensitive), but `accessToken` must have been stripped before the write.
> If the disk adapter is not active in the test environment, mark this as "N/A — disk adapter inactive" and note that the invariant is enforced by the strip-before-write function, which is unchanged.

**Result:** [ ] Pass  [ ] Fail / [ ] N/A
**Notes:**

---

## Summary

| Scenario | Result | Notes |
|----------|--------|-------|
| Scenario 1 — createJourney stores tenantId | | |
| Scenario 2 — isSameTenant returns false (diff tenantIds) | | |
| Scenario 3 — isSameTenant returns true (matching tenantIds) | | |
| Scenario 4 — isSameTenant Phase 0 passthrough (null/undefined) | | |
| Scenario 5 — AC5 fixture scope (code review) | | |
| Scenario 6 — cross-tenant read returns 404 | | |
| Scenario 7 — pre-existing test suite no regression | | |
| Scenario 8 — p1.2 test suite | | |
| Edge case — accessToken not on disk | | |

**Overall verdict:** [ ] All pass — ready to proceed
[ ] Failures found — log findings below before proceeding

---

## Findings

| Scenario | Expected | Actual | Severity | Action |
|----------|----------|--------|----------|--------|
| | | | HIGH / MED / LOW | Fix AC / Fix implementation / Accept |
