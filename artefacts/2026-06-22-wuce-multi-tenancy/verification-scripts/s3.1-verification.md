# AC Verification Script: s3.1 — Neon Postgres provisioning and schema activation

**Story reference:** artefacts/2026-06-22-wuce-multi-tenancy/stories/s3.1.md
**Technical test plan:** artefacts/2026-06-22-wuce-multi-tenancy/test-plans/s3.1-test-plan.md
**Script version:** 1
**Verified by:** _____ | **Date:** _____ | **Context:** [ ] Pre-code  [ ] Post-merge  [ ] Demo

---

## Setup

**Before you start:**
1. Have your Neon project connection string available (from console.neon.tech).
2. Have the Fly.io CLI installed and authenticated (`fly auth whoami` should print your email).
3. Know your Fly.io app name (set `APP=<name>` in your shell before running fly commands).

**Reset between scenarios:** Each scenario is independent. No shared state.

---

## Scenarios

---

### Scenario 1: Neon connection string is valid and accessible (AC1)

**Covers:** AC1

**Steps:**
1. Open console.neon.tech and navigate to your project's connection details.
2. Copy the connection string — it should look like `postgresql://user:password@host/dbname?sslmode=require`.
3. Run: `DATABASE_URL="<your-connection-string>" node -e "const {Pool}=require('pg'); const p=new Pool({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}}); p.query('SELECT 1').then(()=>{console.log('OK'); process.exit(0)}).catch(e=>{console.error(e.message); process.exit(1)})"`

**Expected outcome:**
> The command prints `OK` and exits with code 0. No error message appears.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 2: Schema migration runs and is idempotent (AC2)

**Covers:** AC2

**Steps:**
1. Run: `DATABASE_URL="<your-connection-string>" node scripts/migrate-schema-pg.js`
2. Check the output and exit code.
3. Run the same command again (second run — tests idempotency).

**Expected outcome:**
> Both runs print: `Schema created (or already exists): journeys table + tenant_id index`
> Both runs exit with code 0. No error is printed on either run.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 3: journeys table and index exist in Neon (AC3)

**Covers:** AC3

**Steps:**
1. Open the Neon SQL editor at console.neon.tech.
2. Run: `\dt journeys`
3. Run: `\di journeys_tenant_id_idx`

**Expected outcome:**
> `\dt journeys` shows a row for the `journeys` table.
> `\di journeys_tenant_id_idx` shows a row for the `journeys_tenant_id_idx` index.
> Both commands return results, not "Did not find any relation named..."

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 4: Smoke test passes (AC4)

**Covers:** AC4

**Steps:**
1. Run: `DATABASE_URL="<your-connection-string>" node scripts/smoke-test-pg.js`

**Expected outcome:**
> The script prints, in order:
> ```
>   INSERT ... OK
>   SELECT ... OK (tenant_id=smoke-tenant)
>   accessToken absent from stored data ... OK
>   DELETE ... OK
>
> Neon smoke test PASSED
> ```
> The script exits with code 0. No errors are printed. When you check the Neon SQL editor immediately after, there is no row with a `journey_id` starting with `smoke-test-`.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 5: DATABASE_URL is stored as Fly.io secret, not in code (AC5)

**Covers:** AC5

**Steps:**
1. Run: `fly secrets list --app $APP`
2. Confirm `DATABASE_URL` appears in the list.
3. Run: `git grep "postgresql://" -- ':!artefacts/' ':!workspace/'`

**Expected outcome:**
> Step 1: `DATABASE_URL` appears in the `fly secrets list` output.
> Step 3: The `git grep` command returns zero results — no `postgresql://` URL appears in any committed source file.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

## Summary

| Scenario | Result | Notes |
|----------|--------|-------|
| Scenario 1 — Connection valid | | |
| Scenario 2 — Schema migration idempotent | | |
| Scenario 3 — Table and index in Neon | | |
| Scenario 4 — Smoke test passes | | |
| Scenario 5 — DATABASE_URL as secret | | |

**Overall verdict:** [ ] All pass — ready to proceed
[ ] Failures found — log findings below before proceeding

---

## Findings

| Scenario | Expected | Actual | Severity | Action |
|----------|----------|--------|----------|--------|
| | | | | |
