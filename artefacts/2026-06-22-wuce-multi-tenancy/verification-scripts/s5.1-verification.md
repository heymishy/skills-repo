# AC Verification Script: s5.1 — Beta monitoring signals

**Story reference:** artefacts/2026-06-22-wuce-multi-tenancy/stories/s5.1.md
**Technical test plan:** artefacts/2026-06-22-wuce-multi-tenancy/test-plans/s5.1-test-plan.md
**Script version:** 1
**Verified by:** _____ | **Date:** _____ | **Context:** [ ] Pre-code  [ ] Post-merge  [ ] Demo

---

## Setup

**Before you start:**
1. s4.1 (production deployment) must be complete — the app is live and healthy.
2. Set `APP=<your-fly-app-name>` in your shell.
3. Fly.io CLI is installed and authenticated.

**Quick automated run for AC5:** `APP=<name> bash scripts/check-beta-health.sh`
All other scenarios are verification of static artefacts and log patterns.

**Reset between scenarios:** No shared state — each scenario is independent.

---

## Scenarios

---

### Scenario 1: Billing gate log pattern is correctly defined (AC1)

**Covers:** AC1

**Steps:**
1. Run: `grep -n "Journey limit reached" src/web-ui/modules/journey-store.js`

**Expected outcome:**
> At least one line is printed showing the log string `"Journey limit reached"` in the journey-store module.
> This confirms that `fly logs --app $APP | grep "Journey limit reached"` will capture billing gate events when they occur.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 2: PG write error log pattern is correctly defined (AC2)

**Covers:** AC2

**Steps:**
1. Run: `grep -n "\[journey-store\] PG write error:" src/web-ui/modules/journey-store.js`

**Expected outcome:**
> At least one line is printed showing `[journey-store] PG write error:` in the journey-store module.
> This confirms the grep query in the runbook (`fly logs | grep "PG write error"`) will capture Postgres write failures.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 3: Redis write error log pattern is correctly defined (AC3)

**Covers:** AC3

**Steps:**
1. Run: `grep -n "\[session\] Redis write error:" src/web-ui/middleware/session.js`

**Expected outcome:**
> At least one line is printed showing `[session] Redis write error:` in the session middleware.
> This confirms the grep query in the runbook will capture Redis write failures.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 4: Monitoring runbook exists and covers all three failure modes (AC4)

**Covers:** AC4

**Steps:**
1. Open `artefacts/2026-06-22-wuce-multi-tenancy/reference/beta-monitoring-runbook.md`.
2. Confirm the file contains all three sections: "Failure Mode 1 — Billing Gate Fire", "Failure Mode 2 — Postgres Write Error", "Failure Mode 3 — Redis Session Write Error".
3. Confirm each section contains a `fly logs | grep` command, an expected log format, and a remediation step.

**Expected outcome:**
> The runbook file exists at the path above.
> All three failure modes are documented with: the exact `fly logs | grep` query, the expected log line format, and at least one remediation step per mode.
> The grep queries match the patterns verified in Scenarios 1–3.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 5: check-beta-health.sh runs and reports PASS/WARN/FAIL per check (AC5)

**Covers:** AC5

**Steps:**
1. Run: `APP=<your-app-name> bash scripts/check-beta-health.sh`

**Expected outcome:**
> The script prints four check sections in order:
> ```
> [ Check 1 ] GET /health
>   [PASS] GET /health → 200
>
> [ Check 2 ] Billing gate error pattern (fly logs)
>   [PASS] No 'Journey limit reached' errors in recent logs   ← or [WARN] if any exist
>
> [ Check 3 ] PG write error pattern (fly logs)
>   [PASS] No PG write errors in recent logs
>
> [ Check 4 ] Redis write error pattern (fly logs)
>   [PASS] No Redis write errors in recent logs
> ```
> The script ends with a summary showing PASS, WARN, and FAIL counts, and a final `Result: PASS` or `Result: WARN` line.
> The script exits with code 0 (PASS or WARN — not FAIL) when no PG or Redis errors are present.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

## Summary

| Scenario | Result | Notes |
|----------|--------|-------|
| Scenario 1 — Billing gate log pattern defined | | |
| Scenario 2 — PG write error log pattern defined | | |
| Scenario 3 — Redis write error log pattern defined | | |
| Scenario 4 — Runbook covers all three failure modes | | |
| Scenario 5 — Health check script runs and reports PASS/WARN/FAIL | | |

**Overall verdict:** [ ] All pass — ready to proceed
[ ] Failures found — log findings below before proceeding

---

## Findings

| Scenario | Expected | Actual | Severity | Action |
|----------|----------|--------|----------|--------|
| | | | | |
