# Trace Report: Web UI Guardrails & Standards Surface

**Date:** 2026-08-14 | **Pipeline stage:** definition-of-done (all 12 stories)
**Feature:** 2026-08-11-web-ui-guardrails-standards-surface
**Triggered by:** Manual (operator request, post-feature-completion)
**Overall:** ⚠️ WARNINGS — no broken links, 7 findings originally raised (1 HIGH, 3 MEDIUM, 3 LOW); as of 2026-08-14 post-resolution update: 4 RESOLVED (the HIGH finding and all 3 MEDIUM findings), 3 LOW remain open and unaddressed

---

## Chain by Story

| Story | Upstream (→ discovery) | Downstream (→ shipped code) | Issues |
|-------|----------------------|----------------------------|--------|
| wugs-s1 (Extend artefact fetcher) | ✅ | ✅ | None |
| wugs-s2 (Product-level guardrails view) | ✅ | ✅ | Follow-up action still open — see Findings #1 |
| wugs-s3 (Org-level guardrails view + seeding) | ✅ | ✅ | Minor pre-merge test-rigor fix, already resolved |
| wugs-s4 (No-connected-repo fallback) | ✅ | ✅ | None |
| wugs-s5 (Create/edit form) | ✅ | ✅ | Follow-up action confirmed resolved (wugs-s6 wired the route) |
| wugs-s6 (Branch/PR creation adapter) | ✅ | ✅ | DoR manual sandbox-verification step performed 2026-08-14 — RESOLVED, see Findings #2 |
| wugs-s7 (Surface PR state in view) | ✅ | ✅ | None |
| wugs-s8 (Request promotion) | ✅ | ✅ | None |
| wugs-s9 (Approve/reject promotion) | ✅ | ✅ | Accessible UI button for approve/reject never shipped anywhere — see Findings #3 |
| wugs-s10 (Audit-log promotion events) | ✅ | ✅ | Implementation plan artefact missing from git history — see Findings #4 |
| wugs-s11 (Remove smug-s1 routes/tab) | ✅ | ✅ | Implementation plan artefact missing from git history — see Findings #4 |
| wugs-s12 (Remove DB tables) | ✅ | ✅ | Implementation plan artefact missing from git history — see Findings #4 |

All 12 stories: epic reference ✅, benefit-metric reference ✅ (metric name spelling inconsistency noted — see Findings #5), test plan ✅, DoR PROCEED ✅, DoD present ✅, DoD verdict COMPLETE (8) or COMPLETE WITH DEVIATIONS (4, all deviations resolved or explicitly tracked — none silent).

---

## Artefact Inventory

| Artefact | Present? | Notes |
|----------|----------|-------|
| discovery.md | ✅ | Status: Approved |
| benefit-metric.md | ✅ | 2 metrics defined, both covered |
| decisions.md | ✅ | 23 entries: 19 RISK-ACCEPT, 1 GAP-FLAG (resolved), 3 SCOPE/ARCH entries |
| nfr-profile.md | ✅ | Performance + Security + Data residency + Availability + Compliance sections populated; **no dedicated Accessibility section despite 6 stories having explicit Accessibility NFR bullets** — see Findings #6 |
| epics/*.md | ✅ [4 found] | epic-1 (viewing), epic-2 (add/edit), epic-3 (promotion-approval), epic-4 (smug-s1 migration) — all 4 exercised by at least 2 stories |
| stories/*.md | ✅ [12 found] | wugs-s1 through wugs-s12 |
| review/*.md | ✅ [12 found] | One per story |
| test-plans/*.md | ✅ [12 found] | One per story |
| verification-scripts/*.md | ✅ [12 found] | One per story |
| dor/*.md | ✅ [24 found] | 12 `-dor.md` + 12 `-dor-contract.md` |
| dod/*.md | ✅ [12 found] | One per story, all present |
| plans/*.md | ⚠️ [9 of 12 found] | **wugs-s10, wugs-s11, wugs-s12's implementation plans were written during delivery but never committed to git anywhere — see Findings #4** |

---

## Metric Coverage

| Metric | Stories covering it | DoD signal recorded |
|--------|--------------------|---------------------|
| Guardrail/standard visibility in the web UI | wugs-s1, wugs-s2, wugs-s3, wugs-s4 (primary), wugs-s5, wugs-s6, wugs-s7 (indirect), wugs-s11, wugs-s12 (indirect) | ✅ Not-yet-measured, correctly recorded on every contributing story's DoD |
| Product→org promotion-approval workflow usage | wugs-s8, wugs-s9, wugs-s10 (primary) | ✅ Not-yet-measured, correctly recorded — **but see Findings #3: the workflow this metric measures has no UI a real user can click** |

**Metric orphans** (metrics with no covering stories): None.

---

## Scope Deviations

Collected from all 12 DoD artefacts:

| Story | PR | Deviation | Status |
|-------|-----|-----------|--------|
| wugs-s2 | #713 | AbortController-based fetch timeout never added to `wugs-s1`'s shared read adapter, despite being logged as a required follow-up | **Still open** — see Findings #1 |
| wugs-s3 | #727 | AC4's test initially exercised the same product ID twice instead of two distinct products | Caught and fixed pre-merge, no outstanding action |
| wugs-s5 | #723 | `wugs-s6`'s implementation plan needed an explicit task wiring the real write adapter into `POST /products/:id/guardrails/form` | Confirmed resolved — `handlePostGuardrailsForm` is wired at `server.js:3151` |
| wugs-s6 | #726 | DoR's own REQUIRED pre-merge manual sandbox-GitHub verification step was not performed before merge | **RESOLVED 2026-08-14** — see Findings #2 |
| wugs-s11 | #733 | Story's own AC1/AC3/AC4 text named 3 routes/2 test files vs. the real 7 routes/5 test files; a real CI-caught regression (E2E spec referencing a removed route) was found and fixed pre-merge | Fully resolved before merge, documented in DoD |
| wugs-s12 | #734 | Story's own AC text named 1 of 3 real cross-references to the tables being dropped; a mid-task plan gap (obsolete D37-compliance test) was found and fixed transparently; one residual cross-reference deliberately left in place (documented, low-risk) | Fully resolved before merge, documented in DoD |

No deviation crosses a named architecture guardrail or mandatory constraint (checked against `.github/architecture-guardrails.md`'s Active ADR list — ADR-003, ADR-012, ADR-025 all referenced and all still Active).

---

## AC Coverage Gaps

None identified as blocking — every story's test plan achieved 🟢 risk rating on all ACs at test-plan time, and every DoD confirms all ACs verified (`acVerified` = `acTotal` on all 12 stories in `pipeline-state.json`).

One AC-adjacent gap not captured by the mechanical AC-coverage check: **wugs-s9's own story text lists "Approve/reject actions are real, keyboard-accessible buttons" as an Accessibility NFR — this was never built by any of the 12 stories.** See Findings #3.

---

## Open Spikes

None found in this feature's artefact folder.

---

## NFR Orphan Check

**nfr-profile.md structural gap:** 6 stories (`wugs-s2`, `wugs-s3`, `wugs-s4`, `wugs-s5`, `wugs-s7`, `wugs-s9`) have an explicit Accessibility NFR bullet in their own story text (several citing `MC-A11Y-02` directly), but `nfr-profile.md` has no dedicated Accessibility section at all — only Performance, Security, Data residency, Availability, and Compliance. See Findings #6.

**Performance NFR gap already self-documented and correctly tracked:** `nfr-profile.md`'s own Gaps table already records the `AbortController`/timeout gap (found during `wugs-s2`'s review) with an owner and a soft due date ("before or shortly after `wugs-3`/`wugs-4` ship"). Both `wugs-s3` and `wugs-s4` have since shipped and merged, and the fix still has not been applied — see Findings #1, this is now overdue relative to its own stated target, not just "not yet measured."

**No compliance NFRs with named clauses require sign-off** — `nfr-profile.md`'s own Compliance section is explicitly "Not applicable," consistent with `context.yml`'s `regulated: false`.

---

## Architecture Compliance Check

- All 3 ADRs referenced across the 12 stories' Architecture Constraints fields (ADR-003, ADR-012, ADR-025) are Active in `.github/architecture-guardrails.md` — no superseded/deprecated references.
- No DoD artefact records a scope deviation crossing a named guardrail or mandatory constraint.
- **Minor content nuance, not a hard violation:** `wugs-s12`'s story text cites "Schema-first (ADR-003) in reverse" to justify its `DROP TABLE` approach — code review during implementation found ADR-003's actual documented scope is `pipeline-state.schema.json` field evolution, not Postgres table lifecycle. The code comment carrying this citation was corrected during review (removed the specific ADR-003 citation, kept the underlying reasoning); the story artefact itself was not retroactively edited, consistent with this pipeline's own convention of not editing already-signed-off story text after delivery. Not flagged as a numbered finding since it was already caught and the code-level instance fixed — noted here for trace completeness only.

**Guardrails Registry check:** `.github/architecture-guardrails.md` has a `guardrails-registry` YAML block; the feature-level `guardrails[]` array in `pipeline-state.json` already carries 5 assessed entries relevant to this feature (MC-SEC-01, MC-SEC-02, MC-A11Y-02, ADR-003, ADR-012, ADR-025, AP-11, ADR-011 — all `status: "met"`), populated incrementally by `/definition-of-ready` and `/definition-of-done` across the session. No stale-registry mismatch found (spot-checked IDs cited in this feature's own stories against the registry — all present).

---

## Coverage Map Check

`coverageMapPath` is not set on this feature, and all 12 stories are well past `test-plan` stage (all at `definition-of-done`).
🗺️ Coverage map not generated — run `/coverage-map` for gap visibility (suggestion, not a finding — the feature is already fully delivered, so this is retrospective value only, not a blocker).

---

## Traces Branch Health

```
Last commit: 2026-08-12T00:09:27+00:00
Status: ⚠️ Stale
```

The `origin/traces` branch's last commit (`025e4bb6`, tagged to merge commit `07134b42` — `wugs-s5`) predates this feature's 7 most recent merges (`wugs-s6` through `wugs-s12`). The post-merge trace-commit workflow does not appear to have run since `wugs-s5` merged, roughly two days before this report. See Findings #4 (related — both point at the same underlying pattern of post-merge bookkeeping steps not completing for the back half of this feature's stories).

---

## Findings Requiring Action

| # | Severity | Finding | Owner |
|---|----------|---------|-------|
| 1 | MEDIUM | `wugs-s2`'s AbortController/fetch-timeout follow-up (tracked in `decisions.md` 2026-08-12 and `nfr-profile.md`'s own Gaps table) is still unimplemented in `src/web-ui/adapters/artefact-fetcher.js`, and is now **overdue** relative to its own stated target ("before or shortly after `wugs-3`/`wugs-4` ship" — both have shipped). A hung GitHub API call still hangs the whole product/org guardrails page render indefinitely, with no timeout error state. **STATUS 2026-08-14: RESOLVED — `wugs-s14` merged** (https://github.com/heymishy/skills-repo/pull/736, merge commit `6951c06a`) and DoD-complete (`artefacts/2026-08-11-web-ui-guardrails-standards-surface/dod/wugs-s14-dod.md`). | Hamish King |
| 2 | MEDIUM | `wugs-s6`'s DoR-required manual sandbox-GitHub verification (confirming `realCreateGuardrailPr`'s branch-ref/Contents/Pulls API assumptions against a real GitHub API, not just a hand-authored mock) has still not been performed, per the last session checkpoint. Runnable procedure exists at `artefacts/2026-08-11-web-ui-guardrails-standards-surface/reference/wugs-s6-manual-verification.md`. This is the write path every subsequent Epic 2/3 story (`wugs-s7` through `wugs-s10`) depends on. **STATUS 2026-08-14: RESOLVED** — operator ran the verification script against a real sandbox repo, producing a real branch + PR (https://github.com/heymishy/skills-repo-sandbox/pull/1, independently confirmed OPEN via `gh pr view`). See `decisions.md`'s RISK-ACCEPT-RESOLVED entry (2026-08-14) and PR #726's own comment thread. Note: this closes the specific mock-shape-verification gap (Scenario 1 of `verification-scripts/wugs-s6-verification.md`) — Scenarios 2 (edit/stale-edit) and 3 (failure path) of that same script remain unexercised, lower-severity and not currently blocking anything. | Hamish King |
| 3 | HIGH | Epic 3's promotion-approval workflow (`wugs-s8` request → `wugs-s9` approve/reject → `wugs-s10` audit-log) has fully-working, fully-tested backend endpoints, but **no web UI surface anywhere renders an approve/reject button** — confirmed via a direct search of `products.js`'s rendering functions, zero `fetch('/api/admin/promotions/...')` calls exist client-side. `wugs-s9`'s own story text names this as an Accessibility NFR ("real, keyboard-accessible buttons"); its DoD explicitly deferred it "to a later story in this epic," but no subsequent story (`wugs-s10`, `wugs-s11`, `wugs-s12`) built it. As shipped, an admin can only approve or reject a promotion via a direct API call (curl/Postman), not through the product. This directly limits Metric 2's own measurability — the metric this whole epic exists to move cannot yet be moved by a real user through the UI. **STATUS 2026-08-14: RESOLVED — `wugs-s13` merged** (https://github.com/heymishy/skills-repo/pull/735, merge commit `6ea307f9`, after resolving a real merge conflict with `wugs-s14`'s own `pipeline-state.json`/`decisions.md` changes) and DoD-complete (`artefacts/2026-08-11-web-ui-guardrails-standards-surface/dod/wugs-s13-dod.md`). | Hamish King — needs a decision: new follow-up story, or confirm this is intentionally deferred past MVP |
| 4 | MEDIUM | Three implementation-plan artefacts (`wugs-s10-plan.md`, `wugs-s11-plan.md`, `wugs-s12-plan.md`) were written to their respective worktrees during delivery but never committed to git anywhere — confirmed via `git log --all` returning zero results for all 3 paths. `wugs-s9`'s plan had the same gap and was caught/fixed at that story's own DoD time; this recurred 3 more times afterward without the fix generalizing into a habit. The artefact chain for these 3 stories is otherwise complete (story, DoR, test-plan, DoD all present) — only the implementation-plan record itself is missing from version control. Correlates with the stale traces-branch finding (both are post-merge bookkeeping steps that stopped completing reliably for this feature's back half). **STATUS 2026-08-14: RESOLVED** — all 3 plan artefacts copied from their worktrees and committed (`4767e949`). The same gap recurred independently for `wugs-s13`/`wugs-s14` (their own plan + verification-script artefacts were also left uncommitted until caught at their own `/branch-complete` time) — worth reinforcing as a durable habit, not just a one-time sweep. | Claude (agent) / process fix |
| 5 | LOW | Metric 2's canonical name in `benefit-metric.md` is "Product→org promotion-approval workflow usage" (arrow character); stories `wugs-s8`, `wugs-s9`, `wugs-s10` and their DoDs consistently spell it "Product-to-org promotion-approval workflow usage" (spelled out). Same metric, cosmetic spelling drift only — not a broken reference, but worth normalizing if this metric name is referenced again in future work on this feature. | — |
| 6 | LOW | `nfr-profile.md` has no dedicated Accessibility section, despite 6 of this feature's 12 stories (`wugs-s2`, `wugs-s3`, `wugs-s4`, `wugs-s5`, `wugs-s7`, `wugs-s9`) having explicit Accessibility NFR bullets in their own story text, several citing `MC-A11Y-02` directly. This gap is directly connected to Finding #3 — a profile-level Accessibility section might have made `wugs-s9`'s deferred button UI more visible as an outstanding NFR rather than something easy to lose track of across 3 subsequent stories. | Hamish King — consider for this feature's `nfr-profile.md`, or as a `/definition` template improvement for future features |
| 7 | LOW | `pipeline-state.json`'s feature-level `metrics[]` array (distinct from `benefit-metric.md`, which is correctly populated) is stale: `m1`'s `contributingStories` lists only `["wugs-s2"]` (should include all 9 per `benefit-metric.md`'s own coverage matrix — `wugs-s1/s2/s3/s4/s5/s6/s7/s11/s12`), and `m2`'s `contributingStories` is empty `[]` (should list `wugs-s8/s9/s10`). Both metrics' `evidence`/`lastMeasured` fields are also unchanged since the very first write at `benefit-metric`-active time (2026-08-11T08:38:42Z), predating all 12 stories' actual delivery. This affects what any dashboard/viz reading this array directly (rather than `benefit-metric.md`) would show — it would undercount which stories move each metric. | Claude (agent) / process fix — likely needs a per-story "record metric contribution" step added to `/definition-of-done`'s own mandatory-write instructions, since no skill currently appends to this array after its initial `/benefit-metric` write |

<!-- Chain is not fully healthy: 7 findings, 1 HIGH, 3 MEDIUM, 3 LOW. No broken upstream/downstream links — all findings are gaps/follow-ups, not structural breaks. -->
