# Test Plan: Register agent behaviour observability as Phase 4 backlog item (p3.1e)

**Story reference:** artefacts/2026-04-14-skills-platform-phase3/stories/p3.1e-agent-behaviour-observability-register.md
**Review artefact:** artefacts/2026-04-14-skills-platform-phase3/review/p3.1e-agent-behaviour-observability-register-review-1.md
**Epic reference:** artefacts/2026-04-14-skills-platform-phase3/epics/e1-governance-chain-integrity.md
**Test plan version:** 1
**Written:** 2026-04-14
**Status:** Written — awaiting implementation (all tests FAIL at time of writing)

---

## AC Coverage Table

| AC | Description | Unit | Integration | NFR | Gap |
|----|-------------|------|-------------|-----|-----|
| AC1 | `docs/agent-behaviour-observability.md` exists with exactly 3 candidates, each with name/mechanism/effort/tradeoff | U1, U2, U3, U4 | — | — | None |
| AC2 | Doc explicitly states no Phase 3 implementation and names Phase 4 registration as only deliverable | U5 | — | — | None |
| AC3 | `workspace/phase4-backlog-*.md` exists with title, problem statement, candidates by reference, NOT STARTED status | U6, U7 | — | — | None |

---

## Test Data Strategy

**Category:** Synthetic / Fixtures — file presence and file content checks
**Rationale:** This is a documentation-only story. All assertions are structural: does the file exist, and does it contain the required content. No code execution, no external services, no fixtures beyond the implementation files themselves.

---

## Unit Tests

### U1 — AC1a: `docs/agent-behaviour-observability.md` exists

**File:** `tests/check-workspace-state.test.js` (or a new documentation-check test)
**Command:** `npm test -- --grep "agent-behaviour-observability"`

**Assertion:** `fs.existsSync('docs/agent-behaviour-observability.md')` returns `true`.

**Expected (FAIL before impl):** File does not exist.
**Expected (PASS after impl):** File exists at `docs/agent-behaviour-observability.md`.

---

### U2 — AC1b: Document contains exactly 3 candidate sections

**File:** `tests/check-workspace-state.test.js`
**Command:** `npm test -- --grep "agent-behaviour-observability"`

**Assertion:** Read the file. Count heading markers (`##` or equivalent) or list items that introduce a candidate. Assert count === 3. Assert no fourth candidate section is present.

**Expected (FAIL before impl):** File absent (→ read throws) or fewer/more than 3 candidate sections found.
**Expected (PASS after impl):** Exactly 3 candidate sections are identifiable in the document.

---

### U3 — AC1c: Each candidate has a name and an effort level

**File:** `tests/check-workspace-state.test.js`
**Command:** `npm test -- --grep "agent-behaviour-observability"`

**Assertion:** For each of the 3 candidate sections:
- A heading or bold title (the name) is present.
- The text `Low`, `Medium`, or `High` appears as the effort level (case-insensitive) within the candidate section.

**Expected (FAIL before impl):** File absent or effort level missing for one or more candidates.
**Expected (PASS after impl):** All 3 candidates have a non-empty name and a valid effort level (Low / Medium / High).

---

### U4 — AC1d: Each candidate has a mechanism description and a tradeoff note

**File:** `tests/check-workspace-state.test.js`
**Command:** `npm test -- --grep "agent-behaviour-observability"`

**Assertion:** For each of the 3 candidate sections:
- A non-empty paragraph of at least 1 sentence is present (mechanism description).
- The word `tradeoff` (or `trade-off`) or a `**Tradeoff:**` label appears within the section.

**Expected (FAIL before impl):** File absent or one or more candidates missing mechanism or tradeoff.
**Expected (PASS after impl):** All 3 candidates have both a mechanism paragraph and an identified tradeoff.

---

### U5 — AC2: Document explicitly states no Phase 3 implementation and references Phase 4 registration

**File:** `tests/check-workspace-state.test.js`
**Command:** `npm test -- --grep "agent-behaviour-observability"`

**Assertion:**
1. The contents of `docs/agent-behaviour-observability.md` contain a phrase equivalent to "no implementation" or "not implemented" in Phase 3 (case-insensitive search).
2. The contents reference "Phase 4" — explicitly naming the Phase 4 registration as the only Phase 3 deliverable from this work.

**Expected (FAIL before impl):** File absent or neither phrase found.
**Expected (PASS after impl):** Both phrases present. A reader is unambiguously informed that nothing is being built in Phase 3.

---

### U6 — AC3a: `workspace/phase4-backlog-*.md` file exists

**File:** `tests/check-workspace-state.test.js`
**Command:** `npm test -- --grep "agent-behaviour-observability"`

**Assertion:** `glob.sync('workspace/phase4-backlog-*.md')` returns at least one match (or `fs.readdirSync('workspace').some(f => f.startsWith('phase4-backlog-') && f.endsWith('.md'))`).

**Expected (FAIL before impl):** No file matching `workspace/phase4-backlog-*.md` exists.
**Expected (PASS after impl):** At least one file matching the pattern exists.

---

### U7 — AC3b: Phase 4 backlog file has required content fields

**File:** `tests/check-workspace-state.test.js`
**Command:** `npm test -- --grep "agent-behaviour-observability"`

**Assertion:** Read the first matching `workspace/phase4-backlog-*.md` file. Assert all four required content fields are present:
1. A title (first `#` heading or `**Title:**` label) — non-empty.
2. A problem statement paragraph — non-empty and at least 1 sentence.
3. A reference to the `docs/agent-behaviour-observability.md` candidates file (contains the string `agent-behaviour-observability`).
4. The status marker `NOT STARTED` (case-insensitive) — confirm Phase 4 has not begun.

**Expected (FAIL before impl):** File absent or missing one or more required fields.
**Expected (PASS after impl):** All four fields present; the backlog item is self-describing and clearly deferred to Phase 4.

---

## Integration Tests

None. This is a documentation-only story with no code execution paths to chain. All assertions are file-content checks covered by unit tests.

---

## NFR Tests

None specific to this story (the story itself lists no NFRs). Performance and security are not applicable to a documentation-only delivery.

---

## Gap Table

| Gap | Reason | Risk |
|-----|--------|------|
| None | All ACs are file-presence and file-content assertions with deterministic outcomes. | — |

**Layout-dependent gaps:** None.
**E2E tooling required:** No.

---

## Test Count Summary

| Type | Count |
|------|-------|
| Unit | 7 |
| Integration | 0 |
| NFR | 0 |
| **Total** | **7** |
