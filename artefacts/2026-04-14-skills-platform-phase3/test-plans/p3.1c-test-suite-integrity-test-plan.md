# Test Plan: Test suite integrity — import path, passRate floor, anti-overfitting counter (p3.1c)

**Story reference:** artefacts/2026-04-14-skills-platform-phase3/stories/p3.1c-test-suite-integrity.md
**Review artefact:** artefacts/2026-04-14-skills-platform-phase3/review/p3.1c-test-suite-integrity-review-1.md
**Epic reference:** artefacts/2026-04-14-skills-platform-phase3/epics/e1-governance-chain-integrity.md
**Test plan version:** 1
**Written:** 2026-04-14
**Status:** Written — awaiting implementation (all tests FAIL at time of writing)

---

## AC Coverage Table

| AC | Description | Unit | Integration | NFR | Gap |
|----|-------------|------|-------------|-----|-----|
| AC1 | `check-definition-skill.js` imports from `src/` path | U1 | — | — | None |
| AC2 | `watermark-gate.js` refuses passRate < 0.70; named constant in source | U2, U3, U4, U5 | — | — | None |
| AC3 | add-1-remove-1 → exits 1; counts reported separately | U6, U7 | I1 | — | None |
| AC4 | add-1-remove-0 → exits 0 | U8 | — | — | None |
| AC5 | `npm test` passes with no regressions | — | I2 | N1 | None |

---

## Test Data Strategy

**Category:** Synthetic / Fixtures
**Rationale:** All three changes are in plain Node.js source files. No external service or Docker dependency required. All scenarios use in-process or fixture-file inputs.

**Fixtures required:**
- `tests/fixtures/watermark-scenarios/below-floor.json` — scenario set where 3 of 5 scenarios pass (passRate = 0.60)
- `tests/fixtures/watermark-scenarios/at-floor.json` — scenario set where 7 of 10 scenarios pass (passRate = 0.70)
- `tests/fixtures/watermark-scenarios/above-floor.json` — scenario set where 5 of 5 scenarios pass (passRate = 1.00)
- `tests/fixtures/proposal-scenarios/add-one-remove-one.json` — baseline + proposal where 1 new check added, 1 existing check result removed
- `tests/fixtures/proposal-scenarios/add-one-remove-zero.json` — baseline + proposal where 1 new check added, 0 removed

All fixtures are self-contained synthetic data. No production data referenced.

---

## Unit Tests

### U1 — AC1: check-definition-skill.js uses src/ import path

**File:** `tests/check-definition-skill.test.js` (or assertion within check-definition-skill.js audit test)
**Command:** `node --test tests/check-definition-skill.test.js` (or `npm test -- --grep "import path"`)

**Implementation note:** Read the contents of `tests/check-definition-skill.js`. Assert that at least one `require()` call references a path beginning with `../src/`. Assert that the file does NOT contain a full re-implementation of the definition-skill parse logic (no >20-line function block duplicating production code).

**Expected (FAIL before implementation):** Test file references `./fixtures/` or an inline implementation rather than `../src/`.
**Expected (PASS after implementation):** `require('../src/...')` present; no inline re-implementation block found.

---

### U2 — AC2a: watermark-gate refuses passRate below floor (0.60 case)

**File:** `tests/watermark-gate.test.js`
**Command:** `node --test tests/watermark-gate.test.js`
**Input:** `tests/fixtures/watermark-scenarios/below-floor.json` (3 of 5 passing, passRate = 0.60)

**Expected (FAIL before implementation):** `watermark-gate.js` writes the baseline regardless of passRate.
**Expected (PASS after implementation):** Function throws `Error` (or process exits 1); baseline file is NOT written to disk; error message references the passRate floor.

---

### U3 — AC2b: watermark-gate allows passRate exactly at floor (boundary — 0.70 case)

**File:** `tests/watermark-gate.test.js`
**Command:** `node --test tests/watermark-gate.test.js`
**Input:** `tests/fixtures/watermark-scenarios/at-floor.json` (7 of 10 passing, passRate = 0.70)

**Expected (FAIL before implementation):** If floor enforcement is missing, the 0.70 case might incorrectly fail due to wrong operator (`<` vs `<=`).
**Expected (PASS after implementation):** `watermark-gate.js` proceeds normally; baseline file IS written (or returned without error). Boundary condition is exactly at floor — must not be rejected.

---

### U4 — AC2c: watermark-gate allows passRate above floor (1.00 case)

**File:** `tests/watermark-gate.test.js`
**Command:** `node --test tests/watermark-gate.test.js`
**Input:** `tests/fixtures/watermark-scenarios/above-floor.json` (5 of 5 passing, passRate = 1.00)

**Expected (FAIL before implementation):** N/A for this case (no regression expected, but test must exist to lock behaviour).
**Expected (PASS after implementation):** Baseline IS written without error.

---

### U5 — AC2d: FLOOR_PASS_RATE named constant is documented in source

**File:** `tests/watermark-gate.test.js`
**Command:** `node --test tests/watermark-gate.test.js`

**Implementation note:** Read the contents of `src/improvement-agent/watermark-gate.js` (or equivalent path). Assert that a named constant called `FLOOR_PASS_RATE` (or similar naming convention) is present and assigned `0.70`.

**Expected (FAIL before implementation):** Floor value is either absent, inline (e.g. `if (passRate < 0.70)` hardcoded), or unnamed.
**Expected (PASS after implementation):** `const FLOOR_PASS_RATE = 0.70` (or equivalent) present in the source file.

---

### U6 — AC3a: failure-detector exits 1 when 1 check added and 1 check removed (net zero)

**File:** `tests/failure-detector.test.js`
**Command:** `node --test tests/failure-detector.test.js`
**Input:** `tests/fixtures/proposal-scenarios/add-one-remove-one.json`

**Expected (FAIL before implementation):** `failure-detector.js` only counts net total change; add-1-remove-1 = 0 net change → exits 0 (incorrectly passes).
**Expected (PASS after implementation):** Exits 1; output contains message indicating a removal of a previously-passing check was detected.

---

### U7 — AC3b: failure-detector reports pass/add/remove counts separately in output

**File:** `tests/failure-detector.test.js`
**Command:** `node --test tests/failure-detector.test.js`
**Input:** `tests/fixtures/proposal-scenarios/add-one-remove-one.json`

**Expected (FAIL before implementation):** Output reports only total count or net delta; individual pass/add/remove buckets not present.
**Expected (PASS after implementation):** Output contains three distinct labelled counts: something equivalent to `pass: N`, `added: N`, `removed: N` (exact label wording may vary — assert each appears in stdout).

---

### U8 — AC4: failure-detector exits 0 when 1 check added and 0 removed

**File:** `tests/failure-detector.test.js`
**Command:** `node --test tests/failure-detector.test.js`
**Input:** `tests/fixtures/proposal-scenarios/add-one-remove-zero.json`

**Expected (FAIL before implementation):** Exits 1 or throws if the counter fix also breaks legitimate additions.
**Expected (PASS after implementation):** Exits 0; no removal detected; output confirms the new check was added and nothing removed.

---

## Integration Tests

### I1 — AC3 integration: full proposal evaluation with chained fixture confirms removal is reported

**File:** `tests/failure-detector.integration.test.js`
**Command:** `node --test tests/failure-detector.integration.test.js`

**Scenario:** Write a baseline to a temp directory. Create an add-1-remove-1 proposal against that baseline. Run `failure-detector.js` end-to-end (not mocked). Capture stdout and exit code.

**Expected (FAIL before implementation):** Exit code 0 or removal count absent from stdout.
**Expected (PASS after implementation):** Exit code 1; stdout contains "removed" count of 1 and "added" count of 1.

---

### I2 — AC5: npm test suite count is unchanged or increases after all changes

**Command:** `npm test 2>&1 | tail -5`

**Scenario:** After the import path fix, passRate floor addition, and counter change are all in place, run the full suite. Assert no tests are skipped or removed that were previously passing, and the total test count is ≥ pre-story baseline count (captured in `workspace/suite.json` or equivalent).

**Expected (FAIL before implementation):** If any production code is broken by the import path change, one or more tests will fail.
**Expected (PASS after implementation):** All previously-passing tests still pass; total count ≥ pre-story baseline.

---

## NFR Tests

### N1 — Performance: import path fix and counter change have negligible runtime impact

**Command:** `npm test` (time the run)

**Assertion:** The total `npm test` duration does not increase by more than the time attributable to newly-added AC5 test cases. The import path swap and counter logic change add zero external I/O — runtime impact should be < 100ms.

**Expected (FAIL before implementation):** N/A — baseline not yet measured.
**Expected (PASS after implementation):** Runtime delta (against pre-story baseline) is accounted for only by new tests added under AC5; no unexpected slowdown.

---

## Gap Table

| Gap | Reason | Risk |
|-----|--------|------|
| None | All three changes have deterministic, unit-testable output. No infrastructure dependencies. | — |

**Layout-dependent gaps:** None — no browser or DOM involved.
**E2E tooling required:** No.

---

## Test Count Summary

| Type | Count |
|------|-------|
| Unit | 8 |
| Integration | 2 |
| NFR | 1 |
| **Total** | **11** |
