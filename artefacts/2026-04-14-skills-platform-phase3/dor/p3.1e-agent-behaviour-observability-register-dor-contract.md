# DoR Contract: Agent behaviour observability — document candidate approaches and register Phase 4 backlog item

**Story:** artefacts/2026-04-14-skills-platform-phase3/stories/p3.1e-agent-behaviour-observability-register.md
**DoR artefact:** artefacts/2026-04-14-skills-platform-phase3/dor/p3.1e-agent-behaviour-observability-register-dor.md
**Contract version:** 1
**Assessed:** 2026-04-14

---

## What will be built

1. **`docs/agent-behaviour-observability.md`** — new document. Contains at minimum 3 candidate observability approaches with defined structure per candidate: descriptive name, mechanism paragraph (2–4 sentences on how the approach works), effort rating (Low / Medium / High), and tradeoff note. Must include an explicit statement that no approach is implemented in Phase 3. Must include a forward reference to Phase 4 for implementation decisions.

2. **`workspace/phase4-backlog-agent-behaviour-observability.md`** — new backlog registration document. Contains at minimum: title, problem statement, reference to `docs/agent-behaviour-observability.md` by path slug, and status marker "NOT STARTED — Phase 4". Pattern follows existing `workspace/phase4-backlog-*.md` files if any exist in the workspace directory.

---

## What will NOT be built

- No implementation of any observability approach (no code, no scripts, no config)
- No changes to `.github/skills/`, `.github/templates/`, or any code file
- No changes to `pipeline-state.schema.json`
- No changes to `package.json` or any artefact under `artefacts/`

---

## AC verification mapping

| AC | Test | Type | Spec |
|----|------|------|------|
| AC1 | `docs/agent-behaviour-observability.md` exists and contains ≥ 3 candidate entries | Unit / filesystem | File present; assert ≥ 3 named section headings or structured entries |
| AC1 | Each entry has required fields: name, mechanism, effort, tradeoff | Unit / file-read | Assert presence of each required field per entry |
| AC2 | Document explicitly states no Phase 3 implementation | Unit / file-read | Assert phrase or equivalent text present |
| AC3 | `workspace/phase4-backlog-agent-behaviour-observability.md` exists with title, problem, reference, and NOT STARTED status | Unit / filesystem | File present; assert 4 required sections |

---

## Assumptions

- `docs/` directory exists at the root of the repository.
- `workspace/` directory exists at the root of the repository.
- The test plan specifies the exact count of required candidates and the exact field names — coding agent reads the test plan assertions before creating the documents, as the test assertions are authoritative for the content structure.
- No candidate approach is "selected" or recommended over others — the document is a neutral survey for Phase 4 decision-making.

---

## Estimated touch points

**Files:**
- `docs/agent-behaviour-observability.md` — new
- `workspace/phase4-backlog-agent-behaviour-observability.md` — new

**Services:** None
**APIs:** None

---

## Schema dependency declaration

`schemaDepends: []` — Upstream dependencies: None. No pipeline-state schema fields introduced or consumed.
