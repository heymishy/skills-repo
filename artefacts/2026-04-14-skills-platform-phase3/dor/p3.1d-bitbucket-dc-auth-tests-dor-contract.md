# DoR Contract: Resolve permanently-skipped Bitbucket DC auth tests — Option B (manual test record)

**Story:** artefacts/2026-04-14-skills-platform-phase3/stories/p3.1d-bitbucket-dc-auth-tests.md
**DoR artefact:** artefacts/2026-04-14-skills-platform-phase3/dor/p3.1d-bitbucket-dc-auth-tests-dor.md
**Contract version:** 1
**Assessed:** 2026-04-14
**Selected option:** Option B — manual test record

---

## What will be built

1. **`tests/check-bitbucket-dc.js`** — annotation update only. Replace the existing skip marker comments on the 4 permanently-skipped auth tests (app-password, OAuth, SSH key, PAT) with `// MANUAL: see smoke-tests.md`. No other changes to test logic, assertions, describe/it nesting, or imports. The tests remain skipped.

2. **`tests/smoke-tests.md`** — create this file. Add 4 entries, one per skipped auth test. Each entry must contain all 5 required fields:
   - `test name` — exact test title as it appears in `check-bitbucket-dc.js`
   - `last-run date` — ISO 8601 date string (placeholder: `2026-04-14`)
   - `result` — `SKIP — pending Docker infra`
   - `runner identity` — operator identity
   - `Docker dependency note` — one-sentence explanation of why Docker Bitbucket DC service is not available in CI

---

## What will NOT be built

- **`OPTION A — OPTION-NOT-CHOSEN`:** `.github/workflows/bitbucket-dc-auth.yml` scheduled CI workflow is NOT implemented. This path is deferred to Phase 4 when Docker infrastructure is available.
- No Bitbucket DC auth logic, credentials, or network calls
- No changes to test assertions or test structure beyond the annotation change
- No changes to `pipeline-state.schema.json`
- No changes to `.github/skills/`, `.github/templates/`, or any artefact under `artefacts/`

---

## AC verification mapping

| AC | Test | Type | Spec |
|----|------|------|------|
| AC1 | `tests/check-bitbucket-dc.js` runs without unhandled error | Unit / Integration | Existing test execution passes (skipped tests are expected) |
| AC2 | OPTION-NOT-CHOSEN — no test coverage required; `.github/workflows/bitbucket-dc-auth.yml` must NOT exist | Negative assertion | Option A deferred |
| AC3 | `tests/smoke-tests.md` exists and contains entries for all 4 auth tests | Unit / filesystem | File present; 4 named entries present, each with 5 required fields |
| AC4 | All 4 auth tests in `check-bitbucket-dc.js` carry `// MANUAL: see smoke-tests.md` annotation | Unit / file-read | Read file; assert pattern match for all 4 test titles |
| AC5 | `artefacts/2026-04-14-skills-platform-phase3/decisions.md` contains `DEC-P3-001` with Option B rationale | Unit / file-read | Pre-satisfied at DoR time — file already written; assert in test as a guard |

---

## Assumptions

- The 4 test names in `check-bitbucket-dc.js` are determinable from the file — coding agent reads the file before editing.
- The existing skip markers in `check-bitbucket-dc.js` are consistently formatted (e.g. `// PREREQ-DOCKER` or similar) — coding agent confirms the exact marker strings before replacing.
- `tests/smoke-tests.md` does not currently exist — coding agent creates it. If it already exists, append rather than overwrite.
- AC5 is pre-satisfied by the `decisions.md` entry written at DoR sign-off — the coding agent must not modify `decisions.md`.

---

## Estimated touch points

**Files:**
- `tests/check-bitbucket-dc.js` — modified (annotation change on 4 tests only)
- `tests/smoke-tests.md` — new

**Services:** None
**APIs:** None

---

## Schema dependency declaration

`schemaDepends: []` — Upstream dependencies: None. No pipeline-state schema fields introduced or consumed.
