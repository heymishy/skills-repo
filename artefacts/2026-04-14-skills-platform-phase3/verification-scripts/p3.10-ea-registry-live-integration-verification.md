# AC Verification Script: EA registry blast-radius integration at discovery time (p3.10)

**Story:** p3.10 — Integrate EA registry blast-radius and dependency graph at discovery time
**Reviewer:** Human operator
**Date:** 2026-04-18

---

## Setup

1. Checkout the implementation PR branch.
2. Run: `npm test` — must pass with zero failures before proceeding.
3. Open `.github/skills/discovery/SKILL.md` in an editor.

---

## Scenario 1 — AC1: Path A — blast-radius summary inline when ea_registry_authoritative: true

**Given:** Discovery SKILL.md has been updated and contains a Path A block

**Steps:**
1. Open `.github/skills/discovery/SKILL.md`.
2. Search for "ea_registry_authoritative" or "Path A".
3. Confirm a conditional block exists that says: **when `architecture.ea_registry_authoritative: true`**, the skill queries the EA registry (calls `getBlastRadius()` or equivalent) and presents the blast-radius summary inline — listing affected system count, downstream consumers, and domain owner.
4. Run: `npm test -- --grep "skill-md-path-a"` — verify test passes.

**Expected outcome:** Path A block present; blast-radius output described; SKILL.md test passes.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 2 — AC2: Path B — no-op when flag is false or absent

**Given:** Discovery SKILL.md has been updated

**Steps:**
1. In `.github/skills/discovery/SKILL.md`, find the Path B block.
2. Confirm it explicitly states: when `ea_registry_authoritative: false` or when the key is absent, the discovery skill behaves exactly as before — no blast-radius section, no error.
3. Run: `npm test -- --grep "skill-md-path-b"` — verify test passes.
4. (Optional) Simulate a Path B discovery run with `fixtures/context-path-b.yml` and confirm no error output.

**Expected outcome:** Path B described explicitly; no error on flag-false or flag-absent; SKILL.md test passes.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 3 — AC3: Missing registry entry — graceful "no entry found" message

**Given:** Discovery SKILL.md has been updated

**Steps:**
1. Search SKILL.md for the text "no EA registry entry found" (or semantically equivalent phrase).
2. Verify the text instructs the agent to display this message when registry lookup finds no matching system.
3. Verify the instruction confirms the discovery session continues without error after the message.
4. Run: `npm test -- --grep "missing-entry-message"` — verify test passes.

**Expected outcome:** "no EA registry entry found for [systemId]" message template present; session-continues language confirmed.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 4 — AC4: Integration is read-only — no write instructions in SKILL.md

**Given:** Discovery SKILL.md has been updated

**Steps:**
1. Search SKILL.md for any write operations on the EA registry: `registerSystem`, `updateRegistry`, `writeEntry` (or equivalent).
2. Confirm no such instruction is present.
3. Verify the SKILL.md explicitly says the EA registry call is read-only.
4. Run: `npm test -- --grep "skill-md-read-only"` — verify test passes.

**Expected outcome:** No write instructions found; read-only stated explicitly.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 5 — AC5: Both paths documented with their configuration conditions

**Given:** Discovery SKILL.md has been updated

**Steps:**
1. Open SKILL.md and confirm both paths have their `context.yml` condition stated explicitly:
   - Path A condition: `architecture.ea_registry_authoritative: true`
   - Path B condition: `ea_registry_authoritative: false` or key absent
2. Confirm a squad lead can determine which path applies by reading `context.yml` alone — no additional documentation required.
3. Run: `npm test -- --grep "both-paths-have-configuration-condition"` — verify test passes.

**Expected outcome:** Both conditions explicit in SKILL.md; squad self-service confirmed.

**Pass:** ☐ **Fail:** ☐

---

## Sign-off

All scenarios passed: ☐

Reviewer: _________________________ Date: _________________
