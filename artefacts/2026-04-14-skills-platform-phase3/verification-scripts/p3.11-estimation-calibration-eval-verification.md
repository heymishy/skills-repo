# AC Verification Script: Estimation calibration feedback dimension (p3.11)

**Story:** p3.11 — Add estimation calibration as a feedback dimension in the improvement agent
**Reviewer:** Human operator
**Date:** 2026-04-18

---

## Setup

1. Checkout the implementation PR branch.
2. Run: `npm test` — must pass with zero failures before proceeding.
3. Confirm the calibration module exists: `src/improvement-agent/calibration.js` (or equivalent path — check the PR).

---

## Scenario 1 — AC1: ≥3 features >30% underestimation generates a proposal

**Given:** Calibration module is implemented; `workspace/estimation-norms.md` can be read by the module

**Steps:**
1. Run the calibration unit tests: `npm test -- --grep "calibration-detects-three-features"`
2. Verify the test passes: 3 triggering features → proposal generated.
3. (Optional) Manually seed `workspace/estimation-norms.md` with 3 features all >30% underrun and run the improvement agent: `node src/improvement-agent/index.js` (or equivalent entry point).
4. Check `workspace/proposals/` for a file matching `estimation-calibration-[date].md`.

**Expected outcome:** Test passes; proposal file created at correct path.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 2 — AC2: Proposal contains all required fields

**Given:** A calibration proposal has been generated (from Scenario 1)

**Steps:**
1. Open the generated proposal file.
2. Verify it contains each of these five fields: (a) underestimation rate expressed as a percentage, (b) list of feature slugs that contributed to the signal, (c) extract of the current E1/E2 guidance section that is affected, (d) a proposed updated guideline, (e) a rationale for the proposed adjustment.
3. Run: `npm test -- --grep "proposal-file-contains-required-fields"` — verify test passes.

**Expected outcome:** All five fields present and non-empty.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 3 — AC3: Below-threshold input produces no proposal and no error

**Given:** Calibration module is implemented

**Steps:**
1. Run: `npm test -- --grep "calibration-silent-for-fewer-than-three"` — verify test passes (2 triggering features → no proposal).
2. Run: `npm test -- --grep "calibration-silent-for-rate-below-threshold"` — verify test passes (3 features but ≤30% underrun → no proposal).
3. Confirm neither test emits an error, warning, or exception.

**Expected outcome:** Both tests pass; no proposals generated for below-threshold input; no errors.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 4 — AC4: No auto-apply to SKILL.md, estimation-norms, or context.yml

**Given:** Calibration module is implemented and runs

**Steps:**
1. Run the calibration module with the trigger fixture.
2. After the run, check git status: `git status --short`.
3. Confirm no changes to `.github/skills/`, `workspace/estimation-norms.md`, or any context/config files.
4. Confirm only `workspace/proposals/estimation-calibration-[date].md` is created.
5. Run: `npm test -- --grep "calibration-does-not-write-to-skill-files"` — verify test passes.

**Expected outcome:** Only the proposal file is written; no SKILL.md, norms, or config files modified.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 5 — AC5: Named constants are exported with correct values

**Given:** Calibration module source exists

**Steps:**
1. Run: `npm test -- --grep "constant-estimation-underrun-threshold-is-0-30"` — verify passes.
2. Run: `npm test -- --grep "constant-estimation-min-feature-count-is-3"` — verify passes.
3. Open the calibration module source; confirm both constants are defined as named exports (not inline magic numbers):
   ```
   const ESTIMATION_UNDERRUN_THRESHOLD = 0.30;
   const ESTIMATION_MIN_FEATURE_COUNT = 3;
   ```

**Expected outcome:** Both constants present as named exports with correct values.

**Pass:** ☐ **Fail:** ☐

---

## Sign-off

All scenarios passed: ☐

Reviewer: _________________________ Date: _________________
