# AC Verification Script: Gate structural independence (p3.3)

**Story:** p3.3 — Move gate scripts to platform-infrastructure repo and wire checksum validation
**Reviewer:** Human operator
**Date:** 2026-04-18

---

## Pre-condition

ASSUMPTION-01 must be confirmed resolved. If the `platform-infrastructure` repository has not been created, this verification cannot proceed.

---

## Setup

1. Confirm ASSUMPTION-01 is resolved: `platform-infrastructure` repository exists.
2. Checkout the implementation PR branch.
3. Run: `npm test` — must pass with zero failures.
4. Confirm the checksum validator can be invoked locally.

---

## Scenario 1 — AC1: Delivery repo contains shim only

**Given:** Implementation PR is open

**Steps:**
1. Open the "Files changed" tab on the PR.
2. Search for `run-assurance-gate.js` or any governance check script (`check-*.js`) in the delivery repo.
3. Verify the delivery repo working tree does not contain the gate implementation (only a shim/wrapper entry point that calls the downloaded gate).
4. Confirm the `platform-infrastructure` repository has been created and contains the actual gate scripts.

**Expected outcome:** Delivery repo has shim only; gate implementation lives in platform-infrastructure.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 2 — AC2: CI workflow downloads gate at pinned immutable ref

**Given:** Implementation PR is open; CI workflow file exists

**Steps:**
1. Open the assurance gate CI workflow YAML (`.github/workflows/assurance-gate.yml` or equivalent).
2. Find the step that downloads or checks out the gate script from `platform-infrastructure`.
3. Read the `ref:` value.
4. Verify it is a full 40-character commit SHA or a version tag (e.g. `v1.2.3`) — NOT `main`, `master`, or `HEAD`.

**Expected outcome:** `ref:` is pinned to an immutable SHA or version tag.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 3 — AC3: Checksum validation exits 1 with correct message on mismatch

**Given:** Checksum validator module is implemented

**Steps:**
1. Run the checksum validator against a file with a deliberately incorrect expected hash:
   ```
   node src/gate-validator/checksum-validator.js ./path/to/gate.js sha256:DEADBEEF00000000...
   ```
2. Observe the exit code: `echo $LASTEXITCODE` (Windows) or `echo $?` (bash).
3. Observe the output message.
4. Verify exit code is `1` and output contains "Gate script checksum mismatch — aborting".

**Expected outcome:** Exit code 1; exact message text "Gate script checksum mismatch — aborting" printed.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 4 — AC3 (continued): Checksum validation passes on correct hash

**Given:** Checksum validator module is implemented; a known-good file and its hash are available

**Steps:**
1. Generate the SHA-256 hash of a valid gate script (e.g. `sha256sum gate.js` or equivalent).
2. Run the validator with the correct hash: `node src/gate-validator/checksum-validator.js ./gate.js sha256:[actual-hash]`
3. Verify exit code is 0.
4. Confirm no abort message is printed.

**Expected outcome:** Exit code 0; gate script is cleared for execution.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 5 — AC4: gateScriptRef and gateChecksumVerified fields in schema

**Given:** Implementation PR is open

**Steps:**
1. Open `pipeline-state.schema.json`.
2. Search for `gateScriptRef` and `gateChecksumVerified`.
3. Verify both fields are defined as properties in the schema.
4. Confirm they were added in the same commit as the checksum validation logic.

**Expected outcome:** Both fields present in schema.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 6 — AC5: CI service identity has read-only access to platform-infrastructure

**Given:** platform-infrastructure repository is created; CI is configured

**Steps:**
1. Review the CI job's permission block in the assurance gate workflow YAML.
2. Confirm `permissions:` does not include any write access to the `platform-infrastructure` repository.
3. Review the DoD artefact — confirm it contains a statement on the service identity's access model.

**Expected outcome:** Read-only or no access confirmed; DoD documents the permission model.

**Pass:** ☐ **Fail:** ☐

---

## Sign-off

Pre-condition confirmed (ASSUMPTION-01 resolved): ☐
All scenarios passed: ☐

Reviewer: _________________________ Date: _________________
