# AC Verification Script: Pipeline evolution dispatch — forward-pointer and Closes guidance (p3.6)

**Story reference:** artefacts/2026-04-14-skills-platform-phase3/stories/p3.6-pipeline-evolution-dispatch.md
**Technical test plan:** artefacts/2026-04-14-skills-platform-phase3/test-plans/p3.6-pipeline-evolution-dispatch-test-plan.md
**Script version:** 1
**Verified by:** ______________ | **Date:** ______________ | **Context:** [ ] Pre-code  [ ] Post-merge  [ ] Demo

---

## Setup

**Before you start:**
1. Open the `skills-repo` repository on your computer or on GitHub.
2. After the story is implemented, two SKILL.md files will have been updated:
   - `.github/skills/issue-dispatch/SKILL.md`
   - `.github/skills/definition-of-ready/SKILL.md`
3. No code to run — all verification steps are file content checks.

**Reset between scenarios:** Not applicable — read-only checks.

---

## Scenarios

---

### Scenario 1: Issue-dispatch output template includes `Closes #[issueNumber]`

**Covers:** AC1

**Steps:**
1. Open `.github/skills/issue-dispatch/SKILL.md`.
2. Find the section that describes or shows the issue body that gets generated for a dispatch.
3. Look for a line that says something like `Closes #[issueNumber]` (or `Closes #123` as an example). This should appear in the PR description guidance section.

**Expected outcome:**
> The dispatch output template in the issue-dispatch SKILL.md includes a `Closes #[issueNumber]` line in the section that guides the coding agent on what to include in the PR description. The placeholder `[issueNumber]` (or similar notation) is present — it is not a hardcoded number.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 2: Definition-of-ready sign-off block includes a forward-pointer to /issue-dispatch

**Covers:** AC2

**Steps:**
1. Open `.github/skills/definition-of-ready/SKILL.md`.
2. Find the sign-off section (the block that is produced at the end of a DoR run, confirming Proceed: Yes).
3. Look for a note or pointer that mentions `/issue-dispatch` and explains that the Closes keyword is generated automatically.

**Expected outcome:**
> The DoR sign-off section includes a forward-pointer that tells the operator: "run `/issue-dispatch` next" and notes that the generated issue body will include the `Closes #[issue]` line automatically — the operator does not need to add it manually.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 3: GitHub auto-close mechanism is documented (documentation review)

**Covers:** AC3

**Steps:**
1. Open `.github/skills/issue-dispatch/SKILL.md`.
2. Look for a note or reference explaining that the `Closes #[issueNumber]` keyword causes GitHub to automatically close the dispatch issue when the PR is merged.
3. Confirm this is consistent with GitHub's documented behaviour for the `Closes` keyword (reference: https://docs.github.com/en/issues/tracking-your-work-with-issues/linking-a-pull-request-to-an-issue).

**Expected outcome:**
> The SKILL.md includes an explanatory note or the output template itself contains a comment explaining the auto-close mechanism. The behaviour described is consistent with the GitHub documentation linked above.
> ⚠️ This scenario cannot be verified by running code — it is a documentation review. Record the URL reviewed in the Notes field.

**Result:** [ ] Pass  [ ] Fail
**Notes:** (record the documentation URL confirmed here)

---

### Scenario 4: The PR contains only the two expected file changes

**Covers:** AC4 (PAT-07 — no unrelated changes bundled)

**Steps:**
1. Open the pull request implementing this story on GitHub.
2. Click the "Files changed" tab.
3. Review the list of changed files.

**Expected outcome:**
> The Files changed tab shows exactly two files modified:
> - `.github/skills/issue-dispatch/SKILL.md`
> - `.github/skills/definition-of-ready/SKILL.md`
>
> No other files appear in the diff (no package.json, no test files, no scripts).

**Result:** [ ] Pass  [ ] Fail
**Notes:**
