# AC Verification Script: Eval suite anti-gaming controls (p3.4)

**Story:** p3.4 — Eval suite anti-gaming controls — traceId, failurePattern, human-approval staging
**Reviewer:** Human operator
**Date:** 2026-04-18

---

## Pre-condition

p3.3 must be DoD-complete before running this verification in a full production context. For local verification, `workspace/suite.json` is the validation target.

---

## Setup

1. Checkout the implementation PR branch.
2. Run: `npm test` — must pass with zero failures before proceeding.
3. Confirm the validation script/module is available.

---

## Scenario 1 — AC1: Missing traceId or failurePattern → exits 1 naming the missing field

**Given:** Suite validation script is implemented and wired into `npm test`

**Steps:**
1. Create a temporary JSON entry with `failurePattern` present but `traceId` absent.
2. Run the validation against it (or run: `npm test -- --grep "validation-fails-on-missing-trace-id"`).
3. Observe exit code and output message — confirm exit code 1 and message names "traceId".
4. Repeat with `traceId` present but `failurePattern` absent.
5. Confirm message names "failurePattern" specifically.

**Expected outcome:** Exit code 1; specific field named in error message for each missing case.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 2 — AC2: traceId referencing a non-existent file → validation fails

**Given:** Suite validation script exists

**Steps:**
1. Create a temporary JSON entry with a `traceId` value pointing to a trace file that does not exist (e.g. `traceId: "nonexistent-trace-20260418.jsonl"`).
2. Run the validation (or: `npm test -- --grep "validation-fails-on-trace-id-file-not-found"`).
3. Verify exit code 1; error message names the non-existent file path.
4. Create the same entry with a `traceId` pointing to a trace file that DOES exist; run again.
5. Verify it passes (exit code 0).

**Expected outcome:** Non-existent file path → exit 1 with path named; existing file → exit 0.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 3 — AC3: Improvement agent proposal is staged to workspace/proposals/suite-additions/ — not suite.json

**Given:** Improvement agent suite proposal module is implemented

**Steps:**
1. Trigger the improvement agent to generate a new scenario proposal (or run: `npm test -- --grep "improvement-agent-proposal-written-to-proposals"`).
2. After generation, check `workspace/proposals/suite-additions/` for a new JSON file.
3. Confirm `workspace/suite.json` is NOT modified.
4. Open the proposal JSON; verify it contains `traceId`, `failurePattern`, and `justification` fields.

**Expected outcome:** Proposal in `workspace/proposals/suite-additions/`; suite.json unchanged; required fields present.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 4 — AC4: No automated promotion pathway from proposals to suite.json

**Given:** Full codebase is available for review

**Steps:**
1. Search the source code for any function or script that reads files from `workspace/proposals/suite-additions/` and writes them to `workspace/suite.json` or `platform/suite.json`.
2. Confirm no such function exists.
3. Review the improvement agent source to confirm promotion is not mentioned as an automated step.

**Expected outcome:** No automated promotion code path found; suite.json can only be updated via a human PR.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 5 — AC5: Pre-existing entries (grandfathered) pass validation without errors

**Given:** Suite validation script is wired into `npm test`

**Steps:**
1. Run the full test suite including validation: `npm test`.
2. Verify the run completes with zero failures.
3. Open `workspace/suite.json`; identify any entries that predate this story (these should not have `traceId` or `failurePattern`).
4. Run the validation specifically against these existing entries (or run: `npm test -- --grep "grandfathered-entries-pass"`).
5. Confirm they pass without errors.

**Expected outcome:** All pre-existing entries pass validation; zero regressions introduced.

**Pass:** ☐ **Fail:** ☐

---

## Sign-off

Pre-condition confirmed (p3.3 DoD-complete): ☐
All scenarios passed: ☐

Reviewer: _________________________ Date: _________________
