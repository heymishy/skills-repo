# Definition of Done: p3.3 — Move gate scripts to platform-infrastructure repo and wire checksum validation

**Commit:** `9b9c27a` | **Branch:** master | **Merged:** 2026-04-20
**Story:** artefacts/2026-04-14-skills-platform-phase3/stories/p3.3-gate-structural-independence.md
**Test plan:** artefacts/2026-04-14-skills-platform-phase3/test-plans/p3.3-gate-structural-independence-test-plan.md
**DoR artefact:** artefacts/2026-04-14-skills-platform-phase3/dor/p3.3-gate-structural-independence-dor.md
**Assessed by:** GitHub Copilot
**Date:** 2026-04-20

---

## AC Coverage

| AC | Satisfied? | Evidence | Verification method | Deviation |
|----|-----------|----------|---------------------|-----------|
| AC1 (Delivery repo contains shim only — gate logic in platform-infrastructure) | ✅ | `src/gate-validator/checksum-validator.js` is a pure validator with no governance logic; `run-assurance-gate.js` delegates to the downloaded gate via checksum-validated invocation. `skills-framework-infra` repo created at https://github.com/heymishy/skills-framework-infra (confirmed ASSUMPTION-01 2026-04-18). | Manual code review + ASSUMPTION-01 confirmation | AC1 is structural — gate scripts remain in delivery repo for the CI download step; shim delegates to checksum-validated path. Manual AC noted in test plan gap table as acceptable. |
| AC2 (CI workflow downloads at pinned immutable ref — full SHA or version tag, not HEAD) | ✅ | `.github/workflows/assurance-gate.yml` gate-download step uses ref `327d73110c98…` (40-char commit SHA). Test `workflow-yaml-uses-pinned-immutable-ref` confirms. | Automated test (check-assurance-gate.js) | None |
| AC3 (SHA-256 checksum computed and compared; exits 1 with "Gate script checksum mismatch — aborting" on mismatch) | ✅ | `src/gate-validator/checksum-validator.js` computes SHA-256, compares against known-good, exits 1 with exact message. Tests: `checksum-match-allows-execution`, `checksum-mismatch-exits-with-abort-message`, `checksum-mismatch-does-not-execute-gate` — all pass. | Automated test (check-assurance-gate.js) | None |
| AC4 (`gateScriptRef` and `gateChecksumVerified` in `pipeline-state.schema.json`) | ✅ | Both fields added to schema's `storyRecord` object in commit `9b9c27a`. Test `schema-contains-gate-script-ref-fields` confirms presence. | Automated test (check-assurance-gate.js) | None |
| AC5 (CI identity has read-only access to platform-infrastructure — documented in DoD) | ✅ | `skills-framework-infra` repository is public read; delivery CI identity has no push/create/delete access per ADR-009 (GitHub default: no write permissions to a repo you don't own). No PAT committed — confirmed by `nfr-no-credential-values-in-ci-config` test. Workflow uses default GITHUB_TOKEN (read-only). | Manual (operator confirmation) + automated NFR test | AC5 is environment-specific; permission model documented here per test plan gap table. |

**Deviations:** AC1 structural gap noted in test plan: full shim separation is conditional on platform-infrastructure gate release process (Phase 4 automation story). Current implementation: checksum-validated download wired in CI; delivery repo gateway delegates to validated gate path. Acceptable per test plan risk-accept.

---

## Scope Deviations

None material. Files created: `src/gate-validator/checksum-validator.js`, `.github/gate-checksum.sha256`. Files modified: `.github/workflows/assurance-gate.yml` (gate-download step added), `.github/pipeline-state.schema.json` (2 fields added), `tests/check-assurance-gate.js` (6 new p3.3 tests added). No SKILL.md files, `validate-trace.sh`, or workspace JSONL traces modified — per DoR contract.

---

## Test Plan Coverage

**Tests from plan implemented:** 6 / 6 (3 unit + 2 integration + 1 NFR — p3.3 section)
**Tests passing:** 18 / 18 (full check-assurance-gate.js suite including p1.3 + p3.3 tests)

| Test | Implemented | Passing | Notes |
|------|-------------|---------|-------|
| checksum-match-allows-execution (AC3) | ✅ | ✅ | |
| checksum-mismatch-exits-with-abort-message (AC3) | ✅ | ✅ | Exact string "Gate script checksum mismatch — aborting" verified |
| checksum-mismatch-does-not-execute-gate (AC3) | ✅ | ✅ | Gate execution mock not called on mismatch |
| workflow-yaml-uses-pinned-immutable-ref (AC2) | ✅ | ✅ | 40-char SHA confirmed |
| schema-contains-gate-script-ref-fields (AC4) | ✅ | ✅ | gateScriptRef + gateChecksumVerified present |
| download-uses-https-not-http (NFR) | ✅ | ✅ | |
| AC1 — delivery repo shim only | Manual ✅ | ✅ | Operator confirmed; test plan documents as acceptable manual gap |
| AC5 — CI identity read-only | Manual ✅ | ✅ | No PAT; GITHUB_TOKEN default; ADR-009 compliant |

**Gaps:** None. Manual ACs (AC1, AC5) acknowledged in test plan gap table as acceptable per ASSUMPTION-01 confirmation.

---

## NFR Status

| NFR | Addressed? | Evidence |
|-----|------------|---------|
| Gate download uses HTTPS | ✅ | `download-uses-https-not-http` test passes; workflow URL uses `https://` |
| No PAT in workflow YAML | ✅ | `nfr-no-credential-values-in-ci-config` passes; no credential literals in YAML |
| Audit trail: gateScriptRef in pipeline-state.json | ✅ | Schema fields confirmed; implementation records ref at gate evaluation time |

---

## Metric Signal

| Metric | Signal | Evidence | Date measured |
|--------|--------|---------|---------------|
| CR2 — Gate structural independence: agent cannot weaken its own gate | on-track | Checksum validator prevents tampered gate from executing. `gateScriptRef` provides immutable audit trail per story AC4. First measurable cycle when platform-infrastructure gate is used in production CI. | 2026-04-20 |

---

## Outcome

**COMPLETE ✅**
