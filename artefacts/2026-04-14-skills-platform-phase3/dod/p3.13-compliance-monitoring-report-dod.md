# Definition of Done: p3.13 — Produce automated compliance monitoring report from cross-team trace registry

**Commit:** `c790ebb` | **Branch:** master | **Merged:** 2026-04-20
**Story:** artefacts/2026-04-14-skills-platform-phase3/stories/p3.13-compliance-monitoring-report.md
**Test plan:** artefacts/2026-04-14-skills-platform-phase3/test-plans/p3.13-compliance-monitoring-report-test-plan.md
**DoR artefact:** artefacts/2026-04-14-skills-platform-phase3/dor/p3.13-compliance-monitoring-report-dor.md
**Assessed by:** GitHub Copilot
**Date:** 2026-04-20

---

## AC Coverage

| AC | Satisfied? | Evidence | Verification method | Deviation |
|----|-----------|----------|---------------------|-----------|
| AC1 (Module calls `getTraces({ filter: { period: 'last-30-days' } })` and produces `compliance-[YYYY-MM].md`) | ✅ | `src/improvement-agent/compliance-report.js` exports `generateComplianceReport()` which calls `getTraces` with the required filter and writes `compliance-YYYY-MM.md`. `formatYearMonth` test confirms filename format. | Automated test (compliance-report.test.js) | Output module is `compliance-report.js` (not `audit-sampler.js`); naming deviation acknowledged in DoR amendment commit `ad4472b`. |
| AC2 (Per-squad: squad ID, trace count, gate verdicts, T3M1 fields, gap count, PASS/FAIL status) | ✅ | `analyseSquad()` produces all 6 required fields per squad. Test `report-contains-per-squad-analysis` validates structure. | Automated test | None |
| AC3 (Squad exceeding >10% gap threshold marked FAIL; specific gap pattern listed) | ✅ | `GAP_THRESHOLD = 0.10`; gap ratio `> 0.10` triggers FAIL. Tests `report-marks-squad-fail-above-threshold` (11% fails) and `report-marks-squad-pass-at-threshold` (exactly 10% passes). Exact threshold is strictly greater than (not ≥). | Automated test | None |
| AC4 (CI job uses `schedule:` trigger only; read-only token; not delivery pipeline trigger) | ✅ | `.github/workflows/compliance-report.yml` uses `on: schedule: - cron: '0 0 1 * *'` (monthly); `permissions: contents: read`; no `push:` or `pull_request:` trigger. Integration tests `ci-workflow-uses-schedule-trigger-only` and `ci-workflow-has-readonly-permissions` pass. | Automated test (check-compliance-report.js) | None |
| AC5 (Report is self-contained: policy threshold, sample size, period, registry path) | ✅ | Report header includes policy threshold (10%), sample size, period (last 30 days), registry path (`platform/traces/`). Test `report-is-self-contained-with-context-fields` passes. | Automated test | None |

**Deviations:** Output module named `compliance-report.js` (DoR originally specified `audit-sampler.js`). Naming aligned to test plan spec; DoR touch-point amended in commit `ad4472b` before implementation. No functional deviation.

---

## Scope Deviations

None material. Files created: `src/improvement-agent/compliance-report.js`, `src/improvement-agent/compliance-report.test.js`, `tests/check-compliance-report.js`, `.github/workflows/compliance-report.yml`, `workspace/compliance-reports/.gitkeep`. `src/improvement-agent/index.js` updated to register compliance-report dimension. `package.json` updated to add both test files to chain. No real-time monitoring, external governance platform integrations, or remediation automation added — per story out-of-scope section.

---

## Test Plan Coverage

**Tests from plan implemented:** 8 / 8 (6 unit + 2 integration)
**Tests passing:** 8 / 8

| Test | Implemented | Passing | Notes |
|------|-------------|---------|-------|
| report-filename-matches-yyyy-mm-format (AC1) | ✅ | ✅ | formatYearMonth tested directly |
| report-does-not-overwrite-existing-file (NFR retention) | ✅ | ✅ | Existing file preserved |
| report-contains-per-squad-analysis (AC2) | ✅ | ✅ | All 6 squad fields validated |
| report-marks-squad-fail-above-threshold (AC3) | ✅ | ✅ | 11/100 = 11% → FAIL |
| report-marks-squad-pass-at-threshold (AC3) | ✅ | ✅ | 10/100 = 10% → PASS (strictly > not ≥) |
| report-is-self-contained-with-context-fields (AC5) | ✅ | ✅ | threshold, sampleSize, period, registryPath present |
| ci-workflow-uses-schedule-trigger-only (AC4) | ✅ | ✅ | No push/PR triggers |
| ci-workflow-has-readonly-permissions (AC4) | ✅ | ✅ | permissions: contents: read |

**Gaps:** None.

---

## NFR Status

| NFR | Addressed? | Evidence |
|-----|------------|---------|
| Data classification: internal governance, no PII | ✅ | Output is compliance-YYYY-MM.md containing trace summary counts only; no raw trace data, no user identifiers |
| Retention: monthly files not overwritten | ✅ | `compliance-YYYY-MM.md` filename prevents collision; test `report-does-not-overwrite-existing-file` confirms implementation throws error rather than overwriting |

---

## Metric Signal

| Metric | Signal | Evidence | Date measured |
|--------|--------|---------|---------------|
| CR1 — Independent audit completion without platform team involvement | on-track | Compliance report module generates a self-contained attestation report that a risk reviewer can access without requesting raw data from the delivery team. First measurable cycle when the monthly CI schedule triggers the first real report. | 2026-04-20 |

---

## Outcome

**COMPLETE ✅**
