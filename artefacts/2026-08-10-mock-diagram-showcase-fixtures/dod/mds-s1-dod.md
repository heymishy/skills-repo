# Definition of Done: Add a richer mock-gateway scenario covering every diagram type a skill session can legitimately emit

**PR:** #708 (commit `05adb8a9`, "mds-s1: Add a diagram-showcase mock scenario for ideate/design/definition") | **Merged:** 2026-08-10
**Story:** artefacts/2026-08-10-mock-diagram-showcase-fixtures/stories/mds-s1-diagram-showcase-mock-scenario.md
**Assessed by:** Claude (agent) -- retroactive DoD backlog pass, 2026-08-17
**Date:** 2026-08-17

> Note: the task brief for this DoD pass cited "PR #716" as the merge PR. Git log shows PR #716 belongs to a later, unrelated story (isc-s1, "Make the default mock-gateway /ideate scenario actually cycle through lenses"). mds-s1's actual merge commit is `05adb8a9`, PR #708, merged 2026-08-10. This DoD uses the correct PR reference found in git history.

---

## AC Coverage

| AC | Satisfied? | Evidence | Verification method | Deviation |
|----|------------|----------|----------------------|-----------|
| AC1 -- `ideate` diagram-showcase has 3 valid markers (`cluster-tree`, `table`, `text`) | Yes | `AC1: getMockResponse_ideateDiagramShowcase_hasThreeValidMarkers` -- PASS | Unit test, `tests/check-mds-s1-diagram-showcase-fixtures.js` | None |
| AC2 -- `design` diagram-showcase has 2 valid markers (`system-architecture`, `data-model`) | Yes | `AC2: getMockResponse_designDiagramShowcase_hasTwoValidMarkers` -- PASS (asserts flowchart-shaped and `erDiagram`-shaped mermaid content) | Unit test | None |
| AC3 -- `definition` diagram-showcase has 2 valid markers (`program-design`, `data-model`) | Yes | `AC3: getMockResponse_definitionDiagramShowcase_hasTwoValidMarkers` -- PASS | Unit test | None |
| AC4 -- existing `success`/`failure` fixtures byte-for-byte unchanged | Yes | `AC4: existingFixtures_byteIdentical_afterNewFixturesAdded` -- PASS (SHA-256 checksum comparison, line-ending normalized) | Unit test | Checksum set covers the fixtures that genuinely exist (`design.success.json`, `definition.success.json`, `definition.failure.json`); the test's own comment notes `ideate.failure.json`/`design.failure.json` were never real files despite being named in the DoR contract -- a pre-existing contract inaccuracy, not a defect introduced by this story |
| AC5 -- `inventoryFixtures()` reports new files, existing counting unchanged | Yes | `AC5: inventoryFixtures_reportsNewDiagramShowcaseFiles` -- PASS | Unit test | None |
| AC6 -- completed stage view renders all markers as distinct diagram blocks | Yes | `AC6 (ideate)`, `AC6 (design)`, `AC6 (definition)` -- all PASS. Each drives the real production route handler (`handleGetJourneyStageView`) against a completed journey seeded with the actual `diagram-showcase` fixture text, and asserts the expected count of embedded blocks plus each block's real title text in the response body | Integration test | Evidence is via the production route handler rendering pre-seeded completed-stage turns, not a live multi-turn mock-gateway session driven end-to-end through the UI. This matches drh-s1's own established assertion pattern (embedded `BLOCKS` JSON, not client-rendered DOM) and exercises the real rendering path the AC cares about; it does not additionally prove a live session can reach stage completion via UI interaction, which is outside what AC6's text requires |

## Scope Deviations

None beyond what the story itself names as accepted. The story's Architecture Constraints explicitly rule out modifying `{ideate,design,definition}.success.json`, extending `getMockResponse` to per-turn cycling, and wiring a UI control for scenario selection -- all confirmed still out of scope and untouched. The `drift-signal` diagram type is explicitly out of scope per the story text (generated by the separate as-built-comparison mechanism) and is not present in any new fixture, as expected.

One implementation-time fix is noted honestly rather than hidden: the merge commit's own message records that a JSON-escaping bug (an extra closing brace, `}}}---` instead of `}}---`) was found and fixed in the `design`/`definition` fixture content during this story's own development, caught by the extraction round-trip test -- this was corrected before merge, not a residual defect.

The story's stated upstream dependency, `mgtc-s1` (mock-gateway turn-index-aware response cycling), was "DoR-ready, not yet implemented" at story-authoring time. Git history confirms `mgtc-s1` merged separately (PR #707, commit `d13589b0`) before mds-s1's dependency was blocking; a later story `isc-s1` (PR #716) further built on both. No open dependency gap remains.

## Test Plan Coverage

`check-mds-s1-diagram-showcase-fixtures.js`, freshly re-run this session: **8 passed, 0 failed** (AC1, AC2, AC3, AC4, AC5, AC6-ideate, AC6-design, AC6-definition -- one test per AC, two extra for AC6's three stages). The task brief for this DoD pass cited a stale "null passed, null failed" result for this same script; re-running it directly produced the clean 8/0 result above, which is the evidence relied on here.

## NFR Status

| NFR | Status |
|-----|--------|
| Correctness -- new fixture markers parseable by existing, unmodified `parseCanvasBlock`/`extractCanvasBlocksFromTurns` | Met -- AC1-AC3 tests parse every new marker through the real, unmodified extraction functions; no new parsing logic was added |
| Test isolation -- AC4's byte-identical guarantee must not be compromised | Met -- AC4 checksum test passes; `check-a3-ideate-artefact-disk-match.js`'s single-marker assertion on `ideate.success.json` is unaffected since that file was never touched |

## Metric Signal

No formal benefit-metric artefact exists for this story -- it is explicitly short-track (test-infrastructure enhancement), and the story text states the benefit-metric step was skipped per the short-track process. The stated benefit ("mock-driven diagram rendering coverage across every legitimate skill-emitted type, without spending real LLM tokens") is directly evidenced by the 8/8 passing AC-mapped tests above rather than a tracked metric.

## Outcome

**COMPLETE**
**Follow-up actions:** None.

## DoD Observations

Shipped 2026-08-10 (PR #708) and still in place unmodified as of this assessment (2026-08-17); two later stories (`isc-s1`, `isc-s2`) built directly on top of the mock-gateway infrastructure this story extended, without needing to touch its fixtures. The task brief's PR reference (#716) was incorrect and has been corrected in this DoD using git history.
