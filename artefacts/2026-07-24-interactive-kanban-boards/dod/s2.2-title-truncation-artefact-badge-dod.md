# Definition of Done: Port title truncation and artefact-count badge to the live kanban boards

**PR:** #585 | **Merged:** 2026-07-24
**Story:** artefacts/2026-07-24-interactive-kanban-boards/stories/s2.2-title-truncation-artefact-badge.md
**Test plan:** artefacts/2026-07-24-interactive-kanban-boards/test-plans/s2.2-title-truncation-artefact-badge-test-plan.md
**DoR artefact:** artefacts/2026-07-24-interactive-kanban-boards/dor/s2.2-title-truncation-artefact-badge-dor.md
**Assessed by:** Claude (agent), operator-directed
**Date:** 2026-07-24

---

## AC Coverage

| AC | Satisfied? | Evidence | Verification method | Deviation |
|----|-----------|----------|---------------------|-----------|
| AC1 | ✅ | `longTitleTruncatesAt48Chars` — a 60-char title truncates to 48 chars + ellipsis | `tests/check-s2.2-title-truncation-artefact-badge.js` | None |
| AC2 | ✅ | `shortTitleDisplaysUnmodified` — a 30-char title displays unmodified | `tests/check-s2.2-title-truncation-artefact-badge.js` | None |
| AC3 | ✅ | `fullTitleAvailableViaHoverAttribute` — full untruncated title present in `title=` attribute | `tests/check-s2.2-title-truncation-artefact-badge.js` | None |
| AC4 | ✅ | Artefact-count data WAS obtainable within scope — `getArtefactCountsForJourneys` (a real, indexed batched `GROUP BY` query, `journey-store-pg.js`) confirmed and wired; `artefactBadgeShown_IF_dataAvailable` + `artefactCountDataFetchIntegration` | `tests/check-s2.2-title-truncation-artefact-badge.js` | None — the harder half of this story's open question resolved positively, not just the AC5 fallback |
| AC5 | ✅ | `titleTruncationShipsIndependentOfBadgeOutcome` — graceful-degradation path is real code (try/catch), verified independently of AC4's positive outcome | `tests/check-s2.2-title-truncation-artefact-badge.js` | None |

---

## Scope Deviations

None — `getArtefactsForJourney()`'s own implementation untouched; no schema migration introduced; legacy `renderKanban({features,ideas})` path untouched.

---

## Test Plan Coverage

**Tests from plan implemented:** 7 / 7
**Tests passing in CI:** 7 / 7

| Test | Implemented | Passing | Notes |
|------|-------------|---------|-------|
| longTitleTruncatesAt48Chars | ✅ | ✅ | AC1 |
| shortTitleDisplaysUnmodified | ✅ | ✅ | AC2 |
| fullTitleAvailableViaHoverAttribute | ✅ | ✅ | AC3 |
| artefactBadgeShown_IF_dataAvailable | ✅ | ✅ | AC4 |
| artefactCountDataFetchIntegration | ✅ | ✅ | AC4 |
| titleTruncationShipsIndependentOfBadgeOutcome | ✅ | ✅ | AC5 |
| artefactCountLookupNotUnboundedN1 | ✅ | ✅ | NFR-Performance |

**Gaps (tests not implemented):** None.

---

## NFR Status

| NFR | Addressed? | Evidence |
|-----|------------|---------|
| Performance — artefact-count lookup bounded, not N+1 (nfr-profile.md) | ✅ | `getArtefactCountsForJourneys` — single batched `GROUP BY` query per render, `artefactCountLookupNotUnboundedN1` confirms constant call count as N grows |
| Security — none new | ✅ | `escHtml` usage unchanged, no new user input |
| Accessibility — full title available via hover/focus | ✅ | `title=` attribute confirmed |
| Audit — not applicable | ✅ | |

No CSS-layout-dependent test-plan gaps were flagged for this story.

---

## Metric Signal

| Metric | Baseline available? | First signal measurable | Notes |
|--------|--------------------|-----------------------|-------|
| M3 (WIP visibility retained, kfd1 M1 no regression) | ✅ (kfd1's M1 currently passes) | Now | Title truncation + artefact badge both reached the live board renderer today; kfd1's original UX promise is now fulfilled on the actual live boards, not just the legacy path |

**Signal:** on-track
**Evidence:** Both title truncation and the artefact-count badge (the harder AC4 case) are live on `_renderKanbanColumns`, verified by 7/7 passing tests including a real batched-query integration test.
**Date measured:** 2026-07-24

---

## Outcome

**COMPLETE**

**Follow-up actions:** None.

---

## DoD Observations

1. AC4's own investigation (documented in `decisions.md`) found a real, already-indexed `artefacts` Postgres table with `journey_id` as an FK — this made the "harder half" of the story resolvable within scope rather than needing the AC5 fallback, a good example of the DoR-time RISK-ACCEPT (accepting the investigation as an implementation-time decision) paying off with a full resolution rather than a partial one.
2. The `_getArtefactCountsBulk`/`setGetArtefactCountsBulk` injectable seam mirrors S1.1's `_getHtmlSessionsBulk` pattern exactly — **tag as `/improve` candidate**: this "bulk per-board-render lookup seam" pattern has now been used twice (S1.1, S2.2) with the same shape and deserves a named, documented pattern in this repo's standards rather than being reinvented ad hoc a third time in a future story.

---

## Operator Verification Prompt

```
Review this Definition of Done artefact for "Port title truncation and artefact-count badge to the live kanban boards".
Check:
1. Does every AC row have a concrete evidence reference (test name, observable behaviour, or CI run)?
2. Are any ACs marked satisfied with no evidence, or deferred without a recorded trigger?
3. Does the metric signal row name a real measurement event, or just say "TBD"?
4. Are any scope deviations or follow-up actions that should block release not flagged?
5. Is the outcome verdict (COMPLETE / COMPLETE WITH DEVIATIONS / INCOMPLETE) consistent with the AC and deviation rows?
Report findings as HIGH / MEDIUM / LOW.
```
