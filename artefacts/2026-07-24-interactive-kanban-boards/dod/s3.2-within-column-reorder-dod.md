# Definition of Done: Reorder cards within a column for local prioritisation

**PR:** #590 | **Merged:** 2026-07-24
**Story:** artefacts/2026-07-24-interactive-kanban-boards/stories/s3.2-within-column-reorder.md
**Test plan:** artefacts/2026-07-24-interactive-kanban-boards/test-plans/s3.2-within-column-reorder-test-plan.md
**DoR artefact:** artefacts/2026-07-24-interactive-kanban-boards/dor/s3.2-within-column-reorder-dor.md
**Assessed by:** Claude (agent), operator-directed
**Date:** 2026-07-24

---

## AC Coverage

| AC | Satisfied? | Evidence | Verification method | Deviation |
|----|-----------|----------|---------------------|-----------|
| AC1 | ✅ | E2E "dragging a card onto another card in the SAME column reorders them, no advance call made" | `tests/e2e/s3.2-within-column-reorder.spec.js` | None |
| AC2 | ✅ | E2E "a within-column reorder survives a page reload" — verified across a genuine fresh navigation, not just the reorder's own reload | `tests/e2e/s3.2-within-column-reorder.spec.js` | None |
| AC3 | ✅ | E2E "dragging BETWEEN columns still uses the real advance endpoint" — proves the two drag types are correctly, structurally distinguished | `tests/e2e/s3.2-within-column-reorder.spec.js` + source-level "mutual exclusivity" test | None |
| AC4 | ✅ | Real up/down move buttons built (not a logged exception) — `kbMoveCard` shares the exact same `_kbPersistColumnOrder` helper the drag path uses | `tests/check-s3.2-within-column-reorder.js` | None |

---

## Scope Deviations

None — no server-side schema migration, no cross-scope reorder, no change to S3.1's between-column logic.

---

## Test Plan Coverage

**Tests from plan implemented:** 12 / 12
**Tests passing in CI:** 12 / 12

| Test | Implemented | Passing | Notes |
|------|-------------|---------|-------|
| 9 unit/integration tests (`check-s3.2-within-column-reorder.js`) | ✅ | ✅ | Move buttons, shared-persistence proof, mutual-exclusivity-with-S3.1 proof |
| 3 Playwright E2E tests | ✅ | ✅ | Real drag simulation, AC1/AC2/AC3 |

**Gaps (tests not implemented):** None.

---

## NFR Status

| NFR | Addressed? | Evidence |
|-----|------------|---------|
| Performance — optimistic UI update even if persistence async | ✅ | `localStorage.setItem` is synchronous; DOM move + persist happen before the reload that reapplies state |
| Security — server-side persistence would need tenant checks; N/A for client-only choice | ✅ | Client-only decision documented in decisions.md; no new server data-access path |
| Accessibility — non-drag alternative (AC4, binding requirement) | ✅ | Real up/down buttons built, not a logged exception |
| Audit — not applicable, no real state mutation | ✅ | Confirmed: no gate-confirm/advance call ever made for a within-column reorder |

**CSS-layout-dependent gap audit:** AC1/AC3 were classified E2E-tooling-required at DoR; E2E coverage was delivered. A genuine E2E test-design boundary condition was found and fixed during implementation (dropping at the exact pixel center of a target card is ambiguous by construction) — documented in decisions.md as a test-fragility fix, not a product defect; confirmed via direct browser-side instrumentation that the product logic itself was correct throughout.

---

## Metric Signal

| Metric | Baseline available? | First signal measurable | Notes |
|--------|--------------------|-----------------------|-------|
| M1 (Board-to-CLI parity) | ✅ | Indirectly — this story doesn't move a real stage transition itself | Story's own Benefit Linkage frames this as making the board "a more complete substitute" for manual prioritisation, not a direct M1 contributor |

**Signal:** not-yet-measured
**Evidence note:** This story doesn't itself move M1 directly (per its own Benefit Linkage) — it's a supporting UX improvement; no dedicated measurement event applies beyond M1's own overall usage tracking.
**Date measured:** null

---

## Outcome

**COMPLETE**

**Follow-up actions:** None.

---

## DoD Observations

1. Completes the last remaining story in this feature's Epic 3 — Epic 3 (S3.1, S3.2, S3.3, S3.4) is now fully DoD-complete as of this session, alongside Epic 1 (S1.1, S1.2) and Epic 2 (S2.1, S2.2). The full `2026-07-24-interactive-kanban-boards` feature is now complete across all three epics.
2. The client-only `localStorage` persistence decision (vs. a new server-side schema field) is a good example of a DoR-flagged "genuinely open" architecture decision resolving cleanly without over-building — **tag as `/improve` candidate**: if this repo ever needs multi-device/multi-operator board ordering, this decision's own reasoning (documented in decisions.md) already anticipates the upgrade path (server field supersedes client state per-browser, no migration needed).
3. The E2E boundary-condition finding (exact-center drops are ambiguous) is itself flagged as a `/improve`-worthy general testing pattern (already logged to `workspace/capture-log.md` this session) — any future story adding a similar positional drag-drop decision should reference this finding rather than rediscovering it.

---

## Operator Verification Prompt

```
Review this Definition of Done artefact for "Reorder cards within a column for local prioritisation".
Check:
1. Does every AC row have a concrete evidence reference (test name, observable behaviour, or CI run)?
2. Are any ACs marked satisfied with no evidence, or deferred without a recorded trigger?
3. Does the metric signal row name a real measurement event, or just say "TBD"?
4. Are any scope deviations or follow-up actions that should block release not flagged?
5. Is the outcome verdict (COMPLETE / COMPLETE WITH DEVIATIONS / INCOMPLETE) consistent with the AC and deviation rows?
Report findings as HIGH / MEDIUM / LOW.
```
