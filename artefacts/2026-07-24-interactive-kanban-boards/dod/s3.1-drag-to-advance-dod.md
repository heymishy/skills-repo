# Definition of Done: Drag a ready card to its next column to advance its stage

**PR:** #586 | **Merged:** 2026-07-24
**Story:** artefacts/2026-07-24-interactive-kanban-boards/stories/s3.1-drag-to-advance.md
**Test plan:** artefacts/2026-07-24-interactive-kanban-boards/test-plans/s3.1-drag-to-advance-test-plan.md
**DoR artefact:** artefacts/2026-07-24-interactive-kanban-boards/dor/s3.1-drag-to-advance-dor.md
**Assessed by:** Claude (agent), operator-directed
**Date:** 2026-07-24

---

## AC Coverage

| AC | Satisfied? | Evidence | Verification method | Deviation |
|----|-----------|----------|---------------------|-----------|
| AC1 | ✅ | E2E "dragging a ready card onto its valid next-stage column advances it for real" — real drag, real advance call, card moves to confirmed new stage | `tests/e2e/s3.1-drag-to-advance.spec.js` | None |
| AC2 | ✅ | E2E "dragging onto an invalid column is rejected client-side, no server call made" — network interception confirms zero requests | `tests/e2e/s3.1-drag-to-advance.spec.js` | None |
| AC3 | ✅ | E2E "a not-ready card is not draggable at all" + `notReadyCardIsNotDraggable`/`validationFailedCardIsNotDraggable` | E2E + `tests/check-s3.1-drag-to-advance.js` | None |
| AC4 | ⚠️ | `dragAndClickShareTheSameAdvanceAndErrorFunctions` — source-level proof that drag and click share the exact same `_kbTriggerAdvance`/`_kbAdvanceErrorMessage` functions, combined with S1.1's own passing integration test of that shared mechanism | `tests/check-s3.1-drag-to-advance.js` (source-level, not E2E) | Documented, accepted test-environment limitation (decisions.md): `server.js` hardcodes `setValidate()` to always succeed under `NODE_ENV=test` (the env this entire E2E harness runs under), so a genuine validation failure can never be produced through a real browser session — identical, pre-existing constraint to S1.1's own AC5 |
| AC5 | ✅ | `clickAlternativeStillWorksAlongsideDrag` — a ready card carries both the draggable attribute and the existing clickable Advance button | `tests/check-s3.1-drag-to-advance.js` | None |

---

## Scope Deviations

None — no within-column reorder (S3.2's own scope), no custom drag-preview styling, no touch/mobile gesture support.

---

## Test Plan Coverage

**Tests from plan implemented:** 10 / 10
**Tests passing in CI:** 10 / 10

| Test | Implemented | Passing | Notes |
|------|-------------|---------|-------|
| readyCardIsDraggableWithDragStartWired | ✅ | ✅ | |
| notReadyCardIsNotDraggable | ✅ | ✅ | AC3 |
| validationFailedCardIsNotDraggable | ✅ | ✅ | AC3 |
| cardWithNoReadinessDataIsNotDraggable | ✅ | ✅ | Zero behaviour change for legacy callers |
| columnsAreWiredAsDropTargets | ✅ | ✅ | |
| dragAndClickShareTheSameAdvanceAndErrorFunctions | ✅ | ✅ | AC4 (source-level proof) |
| clickAlternativeStillWorksAlongsideDrag | ✅ | ✅ | AC5 |
| E2E AC1 (real advance) | ✅ | ✅ | |
| E2E AC2 (invalid-column rejection) | ✅ | ✅ | |
| E2E AC3 (not-ready not draggable) | ✅ | ✅ | |

**Gaps (tests not implemented):** AC4's real validation-failure path is not E2E-testable in this repo's current harness (see deviation above) — mitigated by source-level proof of shared-function reuse plus S1.1's own passing integration coverage of the identical mechanism.

---

## NFR Status

| NFR | Addressed? | Evidence |
|-----|------------|---------|
| Performance — no additional cost beyond S1.1's mechanism | ✅ | Reuses `_kbTriggerAdvance` verbatim |
| Security — none new, reuses S1.1's tenant-ownership enforcement | ✅ | Same endpoint, same guard |
| Accessibility — drag is an enhancement, click always available (AC5) | ✅ | `clickAlternativeStillWorksAlongsideDrag` |
| Audit — not applicable beyond S1.1's existing logging | ✅ | |

No CSS-layout-dependent test-plan gaps requiring a RISK-ACCEPT were flagged — AC1-AC3 were classified E2E-tooling-required at DoR and E2E coverage was in fact delivered.

---

## Metric Signal

| Metric | Baseline available? | First signal measurable | Notes |
|--------|--------------------|-----------------------|-------|
| M1 (Board-to-CLI parity) | ✅ | Not yet — requires real operator usage over time | Story's own Benefit Linkage expects this to be the DOMINANT contributor to M1 once real usage begins |

**Signal:** not-yet-measured
**Evidence note:** Drag-and-drop mechanism shipped and verified today (2026-07-24); no real operator usage window has elapsed yet to observe which interaction path (click vs. drag) dominates.
**Date measured:** null

---

## Outcome

**COMPLETE WITH DEVIATIONS**

**Follow-up actions:**
1. No action required — AC4's test-environment limitation is a repo-wide, pre-existing constraint (also affects S1.1's AC5) rather than a gap specific to this story; no realistic fix exists without changing how `NODE_ENV=test` gates `setValidate()` repo-wide, which is out of this story's scope.

---

## DoD Observations

1. This story's explicit extension-point comment in `kbColumnDrop` (for S3.2 to extend, not reimplement) worked exactly as designed — S3.2 (delivered later the same day) did extend this exact function rather than building a second drag-and-drop system, confirmed in S3.2's own decisions.md.
2. The `_kbTriggerAdvance`/`_kbAdvanceErrorMessage` shared-function refactor (extracting these out of `kbAdvanceCard` so both click and drag call the identical code) is a clean example of proving code reuse via a source-level test rather than asserting it only in prose — worth this pattern being named/documented as a `/improve` candidate: "shared-mechanism proof via source assertion" for future stories facing the same test-environment limitation (real validation failures not producible under `NODE_ENV=test`).

---

## Operator Verification Prompt

```
Review this Definition of Done artefact for "Drag a ready card to its next column to advance its stage".
Check:
1. Does every AC row have a concrete evidence reference (test name, observable behaviour, or CI run)?
2. Are any ACs marked satisfied with no evidence, or deferred without a recorded trigger?
3. Does the metric signal row name a real measurement event, or just say "TBD"?
4. Are any scope deviations or follow-up actions that should block release not flagged?
5. Is the outcome verdict (COMPLETE / COMPLETE WITH DEVIATIONS / INCOMPLETE) consistent with the AC and deviation rows?
Report findings as HIGH / MEDIUM / LOW.
```
