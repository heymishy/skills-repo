# Definition of Done: Redesign the product/org/tenant kanban board CSS to use the shared design-system tokens

**PR:** #582 | **Merged:** 2026-07-24
**Story:** artefacts/2026-07-24-interactive-kanban-boards/stories/s2.1-shared-token-redesign.md
**Test plan:** artefacts/2026-07-24-interactive-kanban-boards/test-plans/s2.1-shared-token-redesign-test-plan.md
**DoR artefact:** artefacts/2026-07-24-interactive-kanban-boards/dor/s2.1-shared-token-redesign-dor.md
**Assessed by:** Claude (agent), operator-directed
**Date:** 2026-07-24

---

## AC Coverage

| AC | Satisfied? | Evidence | Verification method | Deviation |
|----|-----------|----------|---------------------|-----------|
| AC1 | ✅ | Zero-raw-hex assertion across `_renderKanbanColumns`'s CSS block | `tests/check-s2.1-shared-token-redesign.js` | None |
| AC2 | ⚠️ | Manual code comparison — all 12 tokens confirmed identical to `html-shell.js`'s `:root` values (decisions.md, "RISK-ACCEPT — S2.1 AC2") | Manual verification, no visual-regression tooling in this repo | RISK-ACCEPTed at DoR per CLAUDE.md's B2 rule — no automated screenshot diff exists in this repo |
| AC3 | ✅ | `darkThemeOverrideBlockPresent` — kanban CSS references tokens redefined in `html-shell.js`'s existing dark-theme override, no locally-authored override | `tests/check-s2.1-shared-token-redesign.js` | None |
| AC4 | ⚠️ | `psh-s7-org-kanban.spec.js` passes unmodified. `psh-s6-product-kanban.spec.js` fails — confirmed via `git stash` re-run against the pre-change baseline that the failure is identical before and after this story (an E2E auth-fixture gap: no `storageState`/login stub, redirected to the signed-out marketing page, finds 0 of 8 columns) | `tests/e2e/psh-s6-product-kanban.spec.js`, `tests/e2e/psh-s7-org-kanban.spec.js` | Pre-existing gap, not caused by this story — documented in decisions.md, flagged as a PR comment, not fixed here (out of this CSS-only story's scope) |

---

## Scope Deviations

None — no new interactive elements, no legacy `renderKanban({features,ideas})` changes, no data-fetching/health-computation changes.

---

## Test Plan Coverage

**Tests from plan implemented:** 9 / 9
**Tests passing in CI:** 9 / 9

| Test | Implemented | Passing | Notes |
|------|-------------|---------|-------|
| Zero-raw-hex check (AC1) | ✅ | ✅ | |
| Dark-theme-override-reuse check (AC3) | ✅ | ✅ | |
| Accessibility (colour-plus-label) regression check | ✅ | ✅ | |
| `psh-s7-org-kanban.spec.js` (AC4) | ✅ | ✅ | |
| `psh-s6-product-kanban.spec.js` (AC4) | ✅ | ❌ | Pre-existing failure, confirmed identical before/after via `git stash` — auth-fixture gap unrelated to this story's CSS change |

**Gaps (tests not implemented):** None implemented-vs-planned gap; `psh-s6`'s failure is a pre-existing E2E auth-fixture gap outside this story's scope (risk: none — the gap predates this story and this story didn't touch the affected code path).

---

## NFR Status

| NFR | Addressed? | Evidence |
|-----|------------|---------|
| Performance — no new runtime cost | ✅ | CSS-only change, same inline `<style>` pattern |
| Security — none, no new user input rendered | ✅ | `escHtml` usage unchanged |
| Accessibility — colour-plus-label preserved, contrast checked both themes | ✅ | Accessibility regression test passes; AC3's dark-theme token reuse confirmed |
| Audit — not applicable | ✅ | |

**CSS-layout-dependent gap audit (AC2):** RISK-ACCEPTed in `decisions.md` **before** coding started (per the story's own DoR-time flag, s2.1-1-M1 review finding) — the manual verification (direct `var()`-token comparison) was executed as part of implementation, documented in `decisions.md`. No layout bug shipped undetected; the RISK-ACCEPT process was followed correctly end-to-end.

---

## Metric Signal

| Metric | Baseline available? | First signal measurable | Notes |
|--------|--------------------|-----------------------|-------|
| M2 (Visual consistency, 3 of 3 board scopes) | ✅ (0 of 3 baseline) | Now — this story converts 1 of 3 scopes' CSS (shared renderer used by all 3, so effectively all 3 scopes at once) | |

**Signal:** on-track
**Evidence:** All 3 board scopes (product/org/tenant) share the same `_renderKanbanColumns` renderer; this story's token substitution applies to all 3 simultaneously. Combined with S2.2's later additions, M2's target (3 of 3 conform) is met as of today.
**Date measured:** 2026-07-24

---

## Outcome

**COMPLETE WITH DEVIATIONS**

**Follow-up actions:**
1. `psh-s6-product-kanban.spec.js`'s pre-existing auth-fixture gap (missing `storageState`/login stub) should get its own short-track fix — flagged, not yet actioned. Owner: unassigned.

---

## DoD Observations

1. AC2's RISK-ACCEPT (manual visual verification, no automated tooling) is a legitimate, correctly-followed process per CLAUDE.md's B2 rule — worth confirming this pattern continues to be honoured as this repo eventually grows toward having real visual-regression tooling, at which point AC2-shaped ACs in future stories should upgrade to automated coverage rather than continuing to RISK-ACCEPT indefinitely.
2. The `psh-s6` pre-existing gap was independently rediscovered and re-confirmed as unrelated by at least one later story's DoR/DoD work too (`s2.1` here) — worth actually filing the follow-up fix rather than leaving it as a recurring "flagged, not fixed" note across multiple stories' artefacts. **Tag as `/improve` candidate:** this gap has now been noted 2+ times without being scheduled as its own story.

---

## Operator Verification Prompt

```
Review this Definition of Done artefact for "Redesign the product/org/tenant kanban board CSS to use the shared design-system tokens".
Check:
1. Does every AC row have a concrete evidence reference (test name, observable behaviour, or CI run)?
2. Are any ACs marked satisfied with no evidence, or deferred without a recorded trigger?
3. Does the metric signal row name a real measurement event, or just say "TBD"?
4. Are any scope deviations or follow-up actions that should block release not flagged?
5. Is the outcome verdict (COMPLETE / COMPLETE WITH DEVIATIONS / INCOMPLETE) consistent with the AC and deviation rows?
Report findings as HIGH / MEDIUM / LOW.
```
