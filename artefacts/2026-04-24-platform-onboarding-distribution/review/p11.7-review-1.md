# Review Report: Extend `/start` SKILL.md with brownfield detection and routing — Run 1

**Story reference:** `artefacts/2026-04-24-platform-onboarding-distribution/stories/p11.7-start-brownfield-routing.md`
**Date:** 2026-04-28
**Categories run:** A — Traceability, B — Scope discipline, C — AC quality, D — Completeness, E — Architecture compliance
**Outcome:** PASS

---

## HIGH findings — must resolve before /test-plan

None.

---

## MEDIUM findings — resolve or acknowledge in /decisions

- **1-M1** [C — AC quality] — The priority order rule "Entry C (DoD artefacts present) takes precedence over Entry A (stories present)" is stated in the Out-of-Scope section but has no corresponding AC. If a repo has both `artefacts/*/stories/` (Entry A signal) and `artefacts/*/dod/` (Entry C signal) simultaneously — a common real-world scenario after a story has completed — no AC verifies that Entry C wins. The story defines the priority order as a design decision but leaves it untested. This is a coverage gap on a stated behavioural rule.
  Risk if proceeding: The test plan writer will have no AC to anchor a priority-conflict test, increasing the probability the priority rule is never verified. Post-implementation behaviour when both signals are present is undefined by any AC.
  To acknowledge: run /decisions, category RISK-ACCEPT. Or add a 6th AC: "Given a repo where both `artefacts/*/stories/` and `artefacts/*/dod/` exist, when `/start` is invoked, then the output routes to the retrospective story template (Entry C takes precedence over Entry A)."

---

## LOW findings — note for retrospective

None.

---

## Score Summary

| Criterion | Score | Pass/Fail | Justification |
|-----------|-------|-----------|---------------|
| A — Traceability | 5 | PASS | All three artefact references present. "So that..." names MM3 (brownfield consumers served without manual workaround) and M1 (extended to brownfield scope). Both metrics in coverage matrix. Benefit linkage explains the survey signal that motivated brownfield routing as a separate story. |
| B — Scope integrity | 5 | PASS | Out-of-scope has 4 named exclusions: fourth entry pattern (with explicit fallback to `/workflow`), automated artefact creation from routing, partial state detection, and multi-codebase routing. Priority disambiguation when multiple signals co-exist is addressed in Out-of-Scope ("C takes precedence over A, A takes precedence over B") which is appropriate as a design rule, but see 1-M1 for the testability gap this creates. |
| C — AC quality | 4 | PASS | 5 ACs, all Given/When/Then. AC1 (Entry B → reverse-engineer), AC2 (Entry A → tdd), AC3 (Entry C → retrospective template), AC4 (greenfield no false positive), AC5 (contract still passes). Each entry case is a separate AC — correctly structured. Missing priority-conflict AC (see 1-M1). |
| D — Completeness | 5 | PASS | All template fields. Named persona "platform consumer with existing codebase (brownfield team)." Benefit linkage with survey-signal motivation. Out-of-scope with 4 named exclusions. NFRs: no external services, file-system detection only, reading patterns stated. Complexity 2. Scope stability "Unstable — entry patterns may evolve" with rationale. |
| E — Architecture compliance | 5 | PASS | Architecture Constraints: ADR-011, check-skill-contracts.js contract for `/start` must still pass after extension (critical regression gate). File-system detection pattern stated. No Active ADR violations. |

---

## Verdict

**Review PASSED ✅ — Run 1**

0 HIGH | 1 MEDIUM (1-M1: no AC for Entry C vs Entry A priority conflict — add AC6 or RISK-ACCEPT in /decisions)

Ready to run /test-plan for p11.7?
