# Review Report: Implement lockfile schema and `pin()` / `verify()` in `cli-adapter.js` — Run 1

**Story reference:** `artefacts/2026-04-24-platform-onboarding-distribution/stories/p11.4-lockfile-schema-pin-verify.md`
**Date:** 2026-04-28
**Categories run:** A — Traceability, B — Scope discipline, C — AC quality, D — Completeness, E — Architecture compliance
**Outcome:** PASS

---

## HIGH findings — must resolve before /test-plan

None.

---

## MEDIUM findings — resolve or acknowledge in /decisions

- **1-M1** [C — AC quality] — AC5 is not independently testable in the current story's scope. The Given is "the lockfile schema document at `docs/skill-lockfile.schema.json`"; the When is "read by any future WS4.3 implementation." A future actor ("any future WS4.3 implementation") cannot be invoked in a test written for this story. The testable intent is valid — each lockfile entry must carry a `sha256` field — but should be expressed as a property of the lockfile written by `pin()`, not as a forward-looking compatibility contract. Suggested rewrite: "Given a repo with SKILL.md files in `.github/skills/`, when `pin()` runs, then the written lockfile has each entry in the `skills` array containing a `sha256` field that is a 64-character hex string."
  Risk if proceeding: The test writer will either skip AC5 as untestable or write a schema-document inspection test that does not verify the actual behaviour of `pin()`. Either outcome reduces coverage quality.
  To acknowledge: run /decisions, category RISK-ACCEPT.

---

## LOW findings — note for retrospective

None.

---

## Score Summary

| Criterion | Score | Pass/Fail | Justification |
|-----------|-------|-----------|---------------|
| A — Traceability | 5 | PASS | All three artefact references present. "So that..." names M4 (WS4.3 hash verification readiness) with a concrete mechanism (stub → real lockfile schema). M4 in coverage matrix. Benefit linkage explains the forward-compatibility motivation clearly. |
| B — Scope integrity | 5 | PASS | Out-of-scope has 5 named exclusions: `init`/`fetch` (p11.5), WS4.3 trust-chain integration, upgrade command (WS0.2), non-SKILL.md files, lockfile signing. All appropriately bounded. No scope creep detected. |
| C — AC quality | 4 | PASS | 6 ACs, all Given/When/Then. AC1 (write lockfile), AC2 (verify pass), AC3 (verify fail with drift), AC4 (pin idempotent), AC6 (npm test passes) are all independently testable with clear observable output. AC5 has a testability issue (see 1-M1) but the intent is valid and AC6 provides a safety net. |
| D — Completeness | 5 | PASS | All template fields populated. Named persona "platform consumer." Benefit linkage with mechanism. Out-of-scope with 5 named exclusions. NFRs: no npm deps (crypto/fs built-ins only). Complexity 2. Scope stability "Stable." |
| E — Architecture compliance | 5 | PASS | Architecture Constraints: ADR-003 (schema doc required before implementation), no npm deps, G19 explicitly deferred with rationale. WS4.3 forward compatibility framed as constraint, not scope. All Active ADRs respected. |

---

## Verdict

**Review PASSED ✅ — Run 1**

0 HIGH | 1 MEDIUM (1-M1: AC5 uses hypothetical future actor — rephrase or RISK-ACCEPT in /decisions)

Ready to run /test-plan for p11.4?
