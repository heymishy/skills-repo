# AC Verification Script: Add required attribution fields to `/benefit-metric` template and SKILL.md

**Story reference:** `artefacts/2026-04-24-platform-onboarding-distribution/stories/p11.2-benefit-metric-attribution-fields.md`
**Technical test plan:** `artefacts/2026-04-24-platform-onboarding-distribution/test-plans/p11.2-test-plan.md`
**Script version:** 1
**Verified by:** ____________ | **Date:** ____________ | **Context:** [ ] Pre-code  [ ] Post-merge  [ ] Demo

---

## Setup

**Before you start:**
1. Ensure the implementation PR for p11.2 has been merged (Post-merge) — or the branch is checked out (Pre-code).
2. Have `.github/templates/benefit-metric.md` open in your editor.
3. Have `.github/skills/benefit-metric/SKILL.md` open in your editor.
4. No running services needed.

**Reset between scenarios:** Each scenario is independent. No shared state.

---

## Scenarios

---

### Scenario 1: Benefit-metric template contains `Metric owner` field with HTML comment

**Covers:** AC1

**Steps:**
1. Open `.github/templates/benefit-metric.md`.
2. Search (Ctrl+F / Cmd+F) for `Metric owner` (or `metric-owner`).
3. Locate the field label and read any HTML comment (`<!-- ... -->`) near it.

**Expected outcome:**
> The field `Metric owner` is present. The HTML comment beside or below it instructs the operator that this should be filled in by a non-engineering role (for example, "Product Manager", "Business Analyst", or similar) — not by the engineering team member writing the stories.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 2: Benefit-metric template contains `Reviewers` field with Tier 1 guidance

**Covers:** AC2

**Steps:**
1. Open `.github/templates/benefit-metric.md`.
2. Search for `Reviewers` as a field label (not just any occurrence of the word in prose).
3. Read the HTML comment associated with this field.

**Expected outcome:**
> The `Reviewers` field is present. Its HTML comment specifies that for Tier 1 metrics, at least one reviewer must be from a non-engineering discipline. The comment should be concrete enough that an operator reading cold would know what to put in the field.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 3: Benefit-metric SKILL.md completion step prompts attribution

**Covers:** AC3

**Steps:**
1. Open `.github/skills/benefit-metric/SKILL.md`.
2. Find the final section — the section that marks the benefit-metric artefact as "Active" / ready to hand off to `/definition`.
3. Read the check-list or instructions in that step carefully.

**Expected outcome:**
> The completion step explicitly tells the operator to confirm that `Metric owner` **and** `Reviewers` fields have been populated before marking the artefact Active. It should be a visible, specific prompt — not a generic "fill in all fields" note.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 4: Contract marker `metric-owner-required` present in SKILL.md

**Covers:** AC4 (contract marker)

**Steps:**
1. Open `.github/skills/benefit-metric/SKILL.md`.
2. Search for `metric-owner-required`.

**Expected outcome:**
> The string `metric-owner-required` is present (typically as an HTML comment like `<!-- metric-owner-required -->`). This marker enables structural enforcement by `check-skill-contracts.js`.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 5: Governance gate — npm test passes with no new contract failures

**Covers:** AC4 (full gate)

**Steps:**
1. In a terminal at the repo root, run: `npm test`
2. Wait for the test suite to complete.
3. Check the output for any failure associated with `benefit-metric` contracts.

**Expected outcome:**
> All contract checks pass including `benefit-metric`. Exit code is 0 (no test failures except the two pre-existing p3.3 assurance-gate failures, which are unrelated to this story).

**Result:** [ ] Pass  [ ] Fail
**Notes:**
