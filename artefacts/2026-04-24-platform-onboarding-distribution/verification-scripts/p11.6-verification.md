# AC Verification Script: Create `/start` skill — greenfield orientation

**Story reference:** `artefacts/2026-04-24-platform-onboarding-distribution/stories/p11.6-start-skill.md`
**Technical test plan:** `artefacts/2026-04-24-platform-onboarding-distribution/test-plans/p11.6-test-plan.md`
**Script version:** 1
**Verified by:** ____________ | **Date:** ____________ | **Context:** [ ] Pre-code  [ ] Post-merge  [ ] Demo

---

## Setup

**Before you start:**
1. Ensure the implementation PR for p11.6 has been merged (or the branch is checked out).
2. Open `.github/skills/start/SKILL.md` in your editor.
3. A Copilot chat session is required for Scenarios 3 and 4 (live-agent scenarios).

**Reset between scenarios:** Scenarios 1 and 2 are file-inspection checks. Scenarios 3–4 are live-agent. Run each in a fresh chat for clean output.

---

## Scenarios

---

### Scenario 1: SKILL.md structure and contract marker

**Covers:** AC3, AC4, AC5

**Steps:**
1. Open `.github/skills/start/SKILL.md`.
2. Confirm it exists (the file was created as part of this story).
3. Check the YAML frontmatter (the block between `---` at the top). Confirm `triggers` is a list containing all five values:
   - `start`
   - `where am I`
   - `what do I do`
   - `help me get started`
   - `I just cloned this`
4. Search for `single-turn-constraint` to confirm the contract marker is present.
5. Read the instruction body. Confirm there is no multi-step Q&A interview that asks the operator multiple questions before producing orientation output. The skill should read `workspace/state.json` (single file read) and immediately produce the orientation response.

**Expected outcome:**
> All 5 triggers present. `single-turn-constraint` marker present. No multi-step interview pattern before orientation output.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 2: SKILL.md instruction text encodes greenfield and brownfield paths

**Covers:** AC1 (instruction text), AC2 (instruction text)

**Steps:**
1. Open `.github/skills/start/SKILL.md`.
2. Read the greenfield (no state.json) instruction block. Confirm it describes:
   a. A one-sentence description of the pipeline.
   b. A message equivalent to "new installation — no pipeline state found".
   c. A concrete next action directing the operator to run `/discovery`.
3. Read the brownfield (state.json exists) instruction block. Confirm it describes:
   a. Reading `currentPhase` from `workspace/state.json`.
   b. Reporting the current phase and the last skill run.
   c. Recommending the next skill to run.

**Expected outcome:**
> Both blocks are present and describe the required outputs. The greenfield block mentions `/discovery`. The brownfield block references `workspace/state.json` and `currentPhase`.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 3 (live-agent): Greenfield — fresh repo with no `workspace/state.json`

**Covers:** AC1

**Steps:**
1. Temporarily rename `workspace/state.json` to `workspace/state.json.bak` (or use a test repo without state.json).
2. Open a Copilot chat session. Type: `start` (or "I just cloned this").
3. Observe the response.
4. Restore `workspace/state.json.bak` to `workspace/state.json`.

**Expected outcome:**
> The response is a single turn (no clarifying questions asked first). It contains:
> (a) A one-sentence description of the pipeline.
> (b) A message like "new installation — no pipeline state found" or equivalent.
> (c) A concrete instruction to run `/discovery` as the next step.
> The response is produced immediately — the AI does not ask "what are you trying to do?" or similar before orienting.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 4 (live-agent): Brownfield — repo with existing `workspace/state.json`

**Covers:** AC2

**Steps:**
1. Ensure `workspace/state.json` exists and has a `currentPhase` value set.
2. Open a Copilot chat session. Type: `where am I` (or "what do I do").
3. Observe the response.

**Expected outcome:**
> The response names the current pipeline phase (read from `currentPhase` in state.json), identifies the last skill run, and recommends the next skill — without requiring the operator to explain the project or answer setup questions first.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 5: Contract check and npm test pass

**Covers:** AC3 (full gate)

**Steps:**
1. In a terminal: `node .github/scripts/check-skill-contracts.js`
2. Confirm exit code 0 and no failure for `start` contract.
3. Run: `npm test`
4. Check for `check-p11-start.js` output lines — all `✓`.

**Expected outcome:**
> Contract check exits 0. npm test exits 0 (pre-existing p3.3 failures acceptable). All start skill tests pass.

**Result:** [ ] Pass  [ ] Fail
**Notes:**
