# Design: Mock Fixture Feature

**Status:** Approved (mock fixture — bri-s3.2)
**Feature slug:** mock-fixture-feature
**Date:** 2026-07-10
**Skill version:** /design

## Design summary

This is a canned design artefact used by the `@mocked` E2E journey suite (bri-s3.2) so the signup-to-onboarding-to-first-feature spec can drive the design stage deterministically, alongside bri-s3.1's discovery/benefit-metric/definition/test-plan/definition-of-ready fixtures. A CANVAS-JSON system-architecture marker (csd-s3, added 2026-07-26) precedes the artefact block, so the design stage's canvas panel also has real content to render -- this fixture predated csd-s3/csd-s4's marker-emission convention by two weeks, which meant staging's mocked /design sessions never rendered a diagram until this fix.

## Out of scope

Real-world design content — this is a fixture, not production output.