# Decisions: Kanban Card and Detail Page CX (kfd1)

---
**[2026-09-11] | RISK-ACCEPT-RESOLVED | post-merge verification (kfd1, closing kvvg-s1)**
**Decision:** `kfd1`'s DoR-accepted mitigation for AC3/AC4's CSS-layout-dependent qualitative requirements — "manual verification scenarios cover the visual gaps" — was never actually executed before this feature's original DoD was written (2026-08-17). A dedicated short-track story, `kvvg-s1` (`artefacts/2026-08-17-kfd1-visual-verification-gap/`), was created to close this gap but itself never progressed past `definition`. This entry records that the underlying verification has now been performed and the gap is closed.
**Alternatives considered:** Write an automated Playwright visual-regression spec covering `.sw-card`/`.sw-doc`/`.sw-section-title` instead of a manual pass, per the second option `kvvg-s1`'s own AC1 allows. Not pursued — a repo-wide DoD-verification-method stocktake surfaced this as one of several open items to close via live Chrome verification in the same pass as other stories, and the manual scenarios were already fully specified and ready to run; adding a permanent automated spec remains a reasonable separate follow-up if this design system sees frequent change, not a blocker to closing this specific gap now.
**Rationale:** Ran Scenarios 5, 6, and 7 from `verification-scripts/kfd1-kanban-card-and-detail-page-cx-verification.md` live against `wuce-staging.fly.dev`, using `getComputedStyle` rigor (not markup-string presence alone, per this repo's own `sob-s4` DoD lesson): Scenario 5 (feature detail page, `/features/2026-06-17-kanban-feature-detail-cx`) confirmed a real sidebar/topbar/`.sw-card`/`.sw-section-title` with resolved styles. Scenario 6 (artefact page, `/artefact/2026-06-17-kanban-feature-detail-cx/nfr-profile`) confirmed a real serif body font, a real rendered heading and table, and — via a synthetic `<pre><code>` element injected into the same live `.sw-doc` container, since that specific artefact had no code block of its own — real monospace styling with a resolved background colour. Scenario 7 (404 path, `/artefact/does-not-exist/discovery`) confirmed a shell-wrapped, sidebar-present 404 page with a real 404 status code. All three passed with no defects found. Scenarios 1–4 and both edge cases in the same script were not re-run: they target `/features?view=board`, a route `kbc-s1` removed entirely after `kfd1` merged (superseded by the product-scoped kanban board) — moot, not open. Full detail in `dod/kfd1-dod.md` DoD Observation #2 and `../2026-08-17-kfd1-visual-verification-gap/dod/kvvg-s1-dod.md`.
**Made by:** Claude (agent), per the repo-wide DoD-triage pass authorised by the operator (Hamish King) — "Yes please and update dod with result" / "Yes good idea. Continue on and go into further waves as needed"
**Revisit trigger:** If this design system's shared CSS (`sw-card`/`sw-doc`/`sw-section-title`/code-block styling) changes in a future story, re-run these same scenarios rather than assuming this closure still holds — this entry verifies the state as of 2026-09-11, not a permanent guarantee.
---
