# DoR Contract: AGENTS.md adapter in assembly script — ADR-005

**Story:** p2.4-agents-md-adapter
**Feature:** 2026-04-11-skills-platform-phase2
**DoR assessment date:** 2026-05-01

---

## What will be built

| Component | File | Notes |
|-----------|------|-------|
| vcs.type format branch | `scripts/assemble-copilot-instructions.sh` | Read `vcs.agent_instructions.format`; branch on value |
| Test — GitHub path | `tests/check-assembly.js` | Assert `.github/copilot-instructions.md` output |
| Test — non-GitHub path | `tests/check-assembly.js` | Assert `AGENTS.md` at root output |
| Fixture — GitHub config | `tests/fixtures/context-github.yml` | vcs.type: github |
| Fixture — Bitbucket config | `tests/fixtures/context-bitbucket.yml` | vcs.type: bitbucket |
| Schema documentation | `contexts/personal.yml`, `contexts/work.yml` | Comment documenting `vcs.agent_instructions.format` and permitted values |

## What will NOT be built

- Content transformation by VCS type
- Stale AGENTS.md cleanup / conflict resolution
- Bitbucket Pipelines CI adapter (p2.10)
- Multiple simultaneous output paths

## AC → Test mapping

| AC | Test(s) | Verification method |
|----|---------|---------------------|
| AC1 | check-assembly-github-default-copilot-instructions, check-assembly-github-no-agents-md | Automated test |
| AC2 | check-assembly-bitbucket-outputs-agents-md, check-assembly-non-github-no-copilot-instructions | Automated test |
| AC3 | check-assembly-explicit-agents-md-override, check-assembly-invalid-format-value-errors | Automated test |
| AC4 | check-assembly-content-parity | Automated test (byte comparison) |
| AC5 | check-assembly-unconfigured-default | Automated test |
| AC6 | check-assembly-github-integration, check-assembly-bitbucket-integration | Integration test (check-assembly.js) |

## Assumptions

- Key path `vcs.agent_instructions.format` under `vcs:` block is confirmed (post-review resolution)
- Permitted values: `copilot-instructions`, `agents-md`; unknown value = assembly error
- Assembly script is bash; test file is Node.js

## Estimated touch points

| File | Change type |
|------|-------------|
| `scripts/assemble-copilot-instructions.sh` | Format-branch logic |
| `tests/check-assembly.js` | Two new test cases |
| `tests/fixtures/context-github.yml` | New fixture |
| `tests/fixtures/context-bitbucket.yml` | New fixture |
| `contexts/personal.yml` | Schema comment addition |
| `contexts/work.yml` | Schema comment addition |

## Out-of-scope signals

If the implementing agent modifies `pipeline-state.json`, `pipeline-state.schema.json`, any SKILL.md file, or any template file, that is a scope violation — add a PR comment.
