# Definition of Done: Deploy assurance agent as automated CI gate on PR open/update

**Story reference:** artefacts/2026-04-09-skills-platform-phase1/stories/p1.3-assurance-agent-ci-gate.md
**DoR reference:** artefacts/2026-04-09-skills-platform-phase1/dor/p1.3-assurance-agent-ci-gate-dor.md
**Test plan reference:** artefacts/2026-04-09-skills-platform-phase1/test-plans/p1.3-assurance-agent-ci-gate-test-plan.md
**PR:** #15 (merged 2026-04-11) — `4cc7c4b`
**DoD assessed by:** Copilot / Hamish
**Date:** 2026-04-11

---

## Entry check

- [x] PR merged to main/master: PR #15 merged 2026-04-11 (commit `4cc7c4b`)
- [x] Story artefact accessible: `stories/p1.3-assurance-agent-ci-gate.md`
- [x] Test plan artefact accessible: `test-plans/p1.3-assurance-agent-ci-gate-test-plan.md`
- [x] DoR artefact and sign-off accessible: `dor/p1.3-assurance-agent-ci-gate-dor.md` — signed off 2026-04-10
- [x] `npm test` baseline confirmed: 138 checks passing (0 failed) — confirmed 2026-04-11

---

## AC Coverage

| AC | Description | Status | Evidence |
|----|-------------|--------|----------|
| AC1 | Gate starts without manual operator action when a PR is opened or updated — trigger event is `pull_request` not `workflow_dispatch` | ✅ Satisfied | Integration test `ci-trigger-starts-gate-without-manual-action` passing — validates workflow YAML is configured with `pull_request` trigger event (opened, synchronize). No `workflow_dispatch` trigger on the gate job. Structural verification complete; live CI log evidence will be produced at first Phase 2 inner loop PR (M2 acceptance test vehicle). |
| AC2 | `inProgress` trace entry written to `workspace/traces/` before any evaluation check runs | ✅ Satisfied | Unit test `trace-inprogress-written-before-evaluation` passing — gate runner writes `inProgress` as first I/O before any evaluation function call. Integration test `gate-writes-inprogress-then-completed` passing end-to-end. |
| AC3 | `completed` trace entry directly follows `inProgress` — valid state transition; error mid-run still produces `completed` with `status: failed` | ✅ Satisfied | Unit test `trace-completed-follows-inprogress` passing. Edge case test `trace-completed-follows-inprogress-on-error` passing — error path confirmed to write `completed` with `status: failed`, not leave trace in `inProgress`-only state. Integration `gate-writes-inprogress-then-completed` passing. |
| AC4 | Gate verdict and trace hash visible in PR interface without opening a separate tool | ✅ Satisfied | Integration test `gate-verdict-surface-to-pr` passing — verifies verdict-surfacing step exists in the CI workflow configuration and produces output including verdict value and trace hash. Visual confirmation in GitHub PR pending first Phase 2 inner loop PR. |
| AC5a | Both manually-run and CI-triggered trace entries exist independently after a CI-triggered gate run | ✅ Satisfied | Integration test `two-trace-entries-on-ci-trigger` passing — CI-triggered run adds a new trace entry alongside the pre-existing manual-run entry; no overwrite. |
| AC5b | CI-triggered run trace metadata contains `trigger: ci` not `trigger: manual` | ✅ Satisfied | Unit test `trace-ci-trigger-metadata` passing — completed trace entry with CI-synthetic payload confirmed to contain `trigger: "ci"`. |

**All 6 ACs (5 distinct + AC5 split) satisfied.**

---

## Out-of-Scope Check

Verified against the DoR contract out-of-scope list. No out-of-scope items were shipped:

- Challenger pre-check automation: not present ✅
- Cross-team trace registry: not present — traces written to `workspace/traces/` only ✅
- Gate results pushed to monitoring dashboard: not present ✅
- Staleness checks on standards model: not present ✅
- GitHub Checks API deep integration (custom annotations on specific lines): not present ✅

**No out-of-scope violations.**

---

## Test Plan Coverage

| Test | Type | AC | Status |
|------|------|----|--------|
| `trace-inprogress-written-before-evaluation` | Unit | AC2 | ✅ Passing |
| `trace-completed-follows-inprogress` | Unit | AC3 | ✅ Passing |
| `trace-completed-follows-inprogress-on-error` | Unit | AC3 (edge case) | ✅ Passing |
| `trace-ci-trigger-metadata` | Unit | AC5b | ✅ Passing |
| `ci-trigger-starts-gate-without-manual-action` | Integration | AC1 | ✅ Passing |
| `gate-writes-inprogress-then-completed` | Integration | AC2, AC3 | ✅ Passing |
| `gate-verdict-surface-to-pr` | Integration | AC4 | ✅ Passing |
| `two-trace-entries-on-ci-trigger` | Integration | AC5a | ✅ Passing |
| `nfr-gate-completes-within-10-minutes` | NFR | NFR-perf | ✅ Passing |
| `nfr-no-credential-values-in-ci-config` | NFR | NFR-sec | ✅ Passing |
| `nfr-trace-audit-fields-present` | NFR | NFR-audit | ✅ Passing |

**11 automated tests passing.**

---

## NFR Status

| NFR | Requirement | Status | Evidence |
|-----|-------------|--------|----------|
| Performance — gate run timeout | Gate must complete within 10 minutes on CI platform | ✅ Pass | `nfr-gate-completes-within-10-minutes` passing. CI workflow job has `timeout-minutes: 10` configured. A timeout is treated as a gate failure equivalent. |
| Security — no credential values | CI configuration must reference credential names only, not values | ✅ Pass | `nfr-no-credential-values-in-ci-config` passing. Workflow YAML scanned for credential-value patterns; zero matches. Secrets referenced by name (`${{ secrets.COPILOT_TOKEN }}`). |
| Audit — trace audit fields | Every gate run must write ISO timestamps, PR ref + commit SHA, trigger type | ✅ Pass | `nfr-trace-audit-fields-present` passing. `completed` entry contains `startedAt`, `completedAt` (ISO 8601), `prRef`, `commitSha` (40-char hex), `trigger` (`ci` or `manual`). |

> Note: p1.3 NFR guardrails are tested by automated tests in `check-assurance-gate.js` but the pipeline-state.json guardrail registry does not have dedicated `NFR-p1.3-*` entries (no entry was added during inner loop). The automated tests provide sufficient evidence. Candidate improvement: add `NFR-p1.3-*` guardrail entries to pipeline-state.json. **Candidate /levelup finding.**

---

## Metric Signal

**M2 — CI-triggered assurance gate (primary metric for this story):**

All four M2 sub-conditions are the ACs of this story:
1. Gate fired automatically on `pull_request` event — structurally verified by workflow YAML config + automated test (AC1). Live CI log evidence pending first Phase 2 PR.
2. `inProgress`→`completed` transition — fully verified by automated tests (AC2, AC3).
3. Gate verdict and trace hash visible in PR — structurally verified by workflow config + automated test (AC4). Visual GitHub confirmation pending first Phase 2 PR.
4. No human initiation — structurally verified by no `workflow_dispatch` trigger on gate job (AC1).

M2 minimum validation signal: **MET via automated tests** — mechanism implemented and tested end-to-end with synthetic payloads. Full acceptance test (live PR in GitHub CI) is the M2 and T3M1 acceptance test vehicle for Phase 2; the first Phase 2 inner loop PR will confirm all four sub-conditions with a real CI log.

**Signal: on-track** (minimum signal met; live acceptance test pending first Phase 2 inner loop PR)

**Note for p1.8 DoD:** p1.8 AC3 requires the T3M1 evidence record, which depends on a real inner loop story running through this gate. This is explicitly deferred at p1.8 DoD until the first Phase 2 PR produces the evidence.

---

## DoD Observations

1. **p1.3 NFR guardrails absent from pipeline-state.json:** The guardrail registry has `NFR-p1.1-*`, `NFR-p1.2-*`, `NFR-p1.6-*`, `NFR-p1.7-*`, `NFR-p1.8-*` entries but no `NFR-p1.3-*` entries. The p1.3 NFRs are validated by the automated tests; the gap is in the guardrail registry tracking only. **Candidate /levelup finding:** pipeline-state.json guardrail entries should be created at inner loop planning time for every story in scope.

2. **AC1 and AC4 structural-only at Phase 1 DoD:** Both ACs require a live GitHub CI environment for full evidence (CI log showing `pull_request` trigger, visual PR confirmation). The automated tests validate the mechanism structurally. The first Phase 2 inner loop PR is the M2 full acceptance test. This is the expected gap at Phase 1 self-referential dogfood — the platform cannot open a real PR against itself to prove CI fires. Correctly scoped.
