# Definition of Done: p1.7 — Implement standards model Phase 1

**Story:** artefacts/2026-04-09-skills-platform-phase1/stories/p1.7-standards-model-phase1.md
**DoR artefact:** artefacts/2026-04-09-skills-platform-phase1/dor/p1.7-standards-model-phase1-dor.md
**Test plan:** artefacts/2026-04-09-skills-platform-phase1/test-plans/p1.7-standards-model-phase1-test-plan.md
**PR:** #18 — merged 2026-04-11 (commit `2366a9a`)
**DoD completed:** 2026-04-11
**Completed by:** Hamish — platform maintainer / operator

---

## AC verification

### AC1 — Three anchor discipline core.md files exist with required metadata and 5+ requirements

**Status: SATISFIED**

Evidence: 31 automated tests passing via `node .github/scripts/check-standards-model.js`.

- I1 (`i1-all-six-standards-files-exist`): All six standards files confirmed present at the required paths.
- I2 per discipline (15 assertions): Each `core.md` confirms: markdown heading title, owning discipline field, `lastReviewedBy`, `lastReviewedDate`, and ≥ 5 MUST/SHOULD/MAY outcome-oriented requirements. All three disciplines pass.

Paths confirmed present: `standards/software-engineering/core.md`, `standards/security-engineering/core.md`, `standards/quality-assurance/core.md`.

### AC2 — POLICY.md floors declare binary pass/fail requirements

**Status: SATISFIED**

Evidence: U1 tests (`u1-software-engineering-policy-requirements-are-binary`, `u1-security-engineering-policy-requirements-are-binary`, `u1-quality-assurance-policy-requirements-are-binary`) — all three POLICY.md files pass the gradient-language check. No "should", "generally", "aim to", "where possible", or "as far as possible" present in requirement lines.

I1 also confirms all three POLICY.md files exist at the required paths.

### AC3 — standards/index.yml returns correct paths for all three discipline IDs; extensible to fourth discipline

**Status: SATISFIED**

Evidence:
- U2 (`u2-software-engineering-lookup-returns-correct-paths`): PASS
- U3 (`u3-security-engineering-lookup-returns-correct-paths`): PASS
- U4 (`u4-quality-assurance-lookup-returns-correct-paths`): PASS
- U5 (`u5-adding-fourth-discipline-does-not-break-parser`): PASS — fourth discipline entry appended in test; all three original disciplines still resolve correctly; fourth discipline resolves correctly. No parser modification required.

### AC4 — standardsInjected array in trace contains discipline ID, file path, and hash

**Status: DEFERRED — Phase 2 acceptance test required**

This AC requires a real inner loop story to run through the P1.3 CI gate after P1.7 is DoD-complete, producing a trace with a populated `standardsInjected` array. The mechanism is implemented and validated structurally: `standards/index.yml` returns correct paths (AC3 SATISFIED), all three `core.md` files exist and are committed (AC1 SATISFIED). The assurance agent hash injection step will operate against these files when the first Phase 2 PR runs through the gate.

Per the test plan (Section: Dependency-gated ACs): I3 and I4 do not block P1.7 DoR; they are scheduled as joint-delivery acceptance tests. P1.3 is now DoD-complete, clearing the upstream dependency. Phase 2 is the trigger.

### AC5 — Recomputed hashes match standardsInjected array values in trace

**Status: DEFERRED — same Phase 2 acceptance test as AC4**

Dependent on AC4's live trace evidence. Hash recomputation can only be validated against a real trace. Once the first Phase 2 inner loop PR produces a trace with `standardsInjected` populated, this AC can be spot-checked by recomputing hashes of the three anchor discipline `core.md` files at that PR's commit SHA.

### AC6 — Each standards requirement passes outcome-orientation diagnostic; reviewedBy and reviewDate fields populated

**Status: SATISFIED**

Evidence: N3 tests — six assertions (`n3-software-engineering-core-md-has-review-record`, `n3-software-engineering-POLICY-md-has-review-record`, `n3-security-engineering-core-md-has-review-record`, `n3-security-engineering-POLICY-md-has-review-record`, `n3-quality-assurance-core-md-has-review-record`, `n3-quality-assurance-POLICY-md-has-review-record`) — all passing.

The outcome-orientation review ("if model behaviour improved past this, would this requirement still be correct?") was performed by the authoring maintainer at PR time. The observable artefact of the review is the populated `lastReviewedBy` and `lastReviewedDate` fields, which N3 confirms are present and populated in all six files.

---

## NFR verification

### Performance NFR — standards injection completes within 30 seconds

**Status: SATISFIED (by design; N1 dependency-gated)**

The injection step reads three files and computes their hashes. File I/O for three markdown files is well within a 30-second budget in any CI environment. N1 (timed gate run) is dependency-gated on P1.3 operational — same as AC4/AC5. The structural design ensures this NFR is met; the timed acceptance test will confirm during Phase 2.

### Security NFR — core security POLICY.md includes OWASP check + secrets-never-committed floor

**Status: SATISFIED**

Evidence: N2 tests — `n2-security-policy-has-owasp-check` (PASS) and `n2-security-policy-has-secrets-check` (PASS). Both binary floor requirements are present in `standards/security-engineering/POLICY.md` and pass the automated checks.

### Audit NFR — every standards file has lastReviewedBy and lastReviewedDate

**Status: SATISFIED**

Evidence: N3 — 6 assertions, all passing. Both fields are populated in all three `core.md` and all three `POLICY.md` files.

---

## Test run evidence

```
[standards-model-check] Results: 31 passed, 0 failed
```

Full test suite: `npm test` → 138 checks passing, 0 failed (confirmed on `d1ff45a`, immediately before p1.7 DoD work begins; p1.7 implementation is unchanged since PR merge).

---

## AC coverage summary

| AC | Description | Status |
|----|-------------|--------|
| AC1 | Three core.md files with metadata + 5+ requirements | SATISFIED |
| AC2 | POLICY.md floors are binary pass/fail | SATISFIED |
| AC3 | standards/index.yml correct paths + extensible schema | SATISFIED |
| AC4 | standardsInjected array in real trace | Deferred — Phase 2 acceptance test |
| AC5 | Hash recompute validates standardsInjected values | Deferred — Phase 2 acceptance test |
| AC6 | Outcome-orientation review + reviewedBy/Date fields populated | SATISFIED |

ACs satisfied: 4 of 6 at Phase 1 scope. ACs 4 and 5 are dependency-gated on Phase 2's first CI gate run — not Phase 1 blockers per the test plan and DoR.

---

## Observations and /levelup candidates

**AC4/AC5 deferred pattern (recurring):** This is the same deferred live-trace evidence pattern seen in p1.3 DoD (AC1/AC4 live CI pending). Phase 1 establishes all mechanisms; Phase 2 first inner loop PR will resolve multiple deferred ACs across p1.3, p1.7, and p1.8 in a single gate run. The pipeline handles this correctly — the deferred ACs are explicitly flagged in the test plan and DoR. **Candidate /levelup:** establish a Phase 2 acceptance test checklist covering all deferred ACs (p1.3-AC1, p1.3-AC4, p1.7-AC4, p1.7-AC5, p1.8-AC3) to be run against the first real Phase 2 PR.

**Standards authoring outcome-orientation principle:** The authoring constraint — "a standard that encodes a current model weakness produces staleness signal; a standard that encodes required outcomes ages well" — is observable in the six committed standards files. All requirements are stated as observable outcomes (MUST/SHOULD/MAY), not as model-weakness workarounds. This pattern should be captured as an authoring standard for Phase 2 discipline expansion.

**M3 signal:** M3 (verifiable standardsInjected hashes in inner loop trace) cannot be marked on-track until the first Phase 2 gate run produces a real trace. The metric definition is sound; the measurement opportunity is Phase 2.
