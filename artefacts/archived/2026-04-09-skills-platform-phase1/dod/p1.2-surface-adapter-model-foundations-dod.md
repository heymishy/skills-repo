# Definition of Done: Implement surface adapter model foundations — Path B git-native reference

**Story reference:** artefacts/2026-04-09-skills-platform-phase1/stories/p1.2-surface-adapter-model-foundations.md
**DoR reference:** artefacts/2026-04-09-skills-platform-phase1/dor/p1.2-surface-adapter-model-foundations-dor.md
**Test plan reference:** artefacts/2026-04-09-skills-platform-phase1/test-plans/p1.2-surface-adapter-model-foundations-test-plan.md
**PR:** #13 (merged 2026-04-10) — `0455fe6`
**DoD assessed by:** Copilot / Hamish
**Date:** 2026-04-11

---

## Entry check

- [x] PR merged to main/master: PR #13 merged 2026-04-10 (commit `0455fe6`)
- [x] Story artefact accessible: `stories/p1.2-surface-adapter-model-foundations.md`
- [x] Test plan artefact accessible: `test-plans/p1.2-surface-adapter-model-foundations-test-plan.md`
- [x] DoR artefact and sign-off accessible: `dor/p1.2-surface-adapter-model-foundations-dor.md` — signed off 2026-04-10
- [x] `npm test` baseline confirmed: 138 checks passing (0 failed) — confirmed 2026-04-11

---

## AC Coverage

| AC | Description | Status | Evidence |
|----|-------------|--------|----------|
| AC1 | git-native adapter invoked with `surface: "git-native"` returns conforming result object with `status`, `surface`, `findings`, `trace`, `adapterVersion` | ✅ Satisfied | 3 automated tests: `git-native-adapter-returns-status-field` (unit), `git-native-adapter-returns-surface-echoed` (unit), `git-native-adapter-end-to-end-result-contract` (integration). All passing. Result schema confirmed complete. |
| AC2 | Path B `context.yml` declaration with `type: "git-native"` selects git-native adapter without triggering Path A | ✅ Satisfied | 2 automated unit tests: `path-b-selects-git-native-adapter`, `path-b-does-not-trigger-path-a`. Resolver short-circuits before any registry lookup; no Path A code path exists in `resolver.js`. |
| AC3 | Multi-surface declaration selects correct adapter per type | ✅ Satisfied | 2 automated tests: `multi-surface-selects-correct-adapter-per-type` (unit), `multi-surface-integration-all-adapters-invoked` (integration). Multi-surface fixture with two types verified end-to-end. |
| AC4 | `execute` interface extensible for Path A without signature change | ✅ Satisfied (design review) | Code review: `execute(surface, context)` signature contains no resolution-path parameter. `execute` body dispatches via `adapterRegistry[surface]` — no internal Path A/Path B branching. Path A = write a new resolver that returns `surface: string` → call existing `execute()`. Zero changes to `execute` required. |
| AC5 | Five Phase 2 surface types (IaC, SaaS-API, SaaS-GUI, M365-admin, manual) not foreclosed by interface shape | ✅ Satisfied (design review) | Code review: `adapterRegistry` is a plain object keyed by string; `execute` dispatches by string lookup. No closed enum, no type-specific branching in `execute` body. Each Phase 2 surface type added by calling `registerAdapter(type, adapter)` — no changes to existing code. |
| AC6 | A `context.yml` conforming to Path B format is valid for both P1.1 assembly and P1.2 resolution (one shared file) | ✅ Satisfied | 1 automated integration test: `context-yml-valid-for-p11-and-p12`. Shared fixture `context-path-b-shared.yml` accepted by both the assembly configuration path and the Path B resolver without modification. |

**All 6 ACs satisfied (4 automated tests, 2 design-review).**

---

## Out-of-Scope Check

Verified against the DoR contract out-of-scope list. No out-of-scope items were shipped:

- IaC, SaaS-API, SaaS-GUI, M365-admin, manual adapter implementations: not present — only `git-native.js` adapter registered ✅
- Path A (EA registry) resolution: not present — `resolver.js` implements Path B only ✅
- DoD criteria variant generation by surface type: not present ✅
- `context.yml` MCP credentials section: not modified ✅

**No out-of-scope violations.**

---

## Test Plan Coverage

| Test | Type | AC | Status |
|------|------|----|--------|
| `git-native-adapter-returns-status-field` | Unit | AC1 | ✅ Passing |
| `git-native-adapter-returns-surface-echoed` | Unit | AC1 | ✅ Passing |
| `path-b-selects-git-native-adapter` | Unit | AC2 | ✅ Passing |
| `path-b-does-not-trigger-path-a` | Unit | AC2 | ✅ Passing |
| `multi-surface-selects-correct-adapter-per-type` | Unit | AC3 | ✅ Passing |
| `git-native-adapter-end-to-end-result-contract` | Integration | AC1 | ✅ Passing |
| `multi-surface-integration-all-adapters-invoked` | Integration | AC3 | ✅ Passing |
| `context-yml-valid-for-p11-and-p12` | Integration | AC6 | ✅ Passing |
| `nfr-path-b-resolution-completes-within-1s` | NFR | NFR-perf | ✅ Passing |
| `nfr-adapter-result-no-credential-values` | NFR | NFR-sec | ✅ Passing |
| `nfr-result-contains-surface-and-adapter-version` | NFR | NFR-audit | ✅ Passing |
| AC4 design review — Path A extensibility | Manual (code review) | AC4 | ✅ Pass — `execute` signature and body confirmed extensible |
| AC5 design review — Phase 2 surface types not foreclosed | Manual (code review) | AC5 | ✅ Pass — registry-based dispatch; no closed enum or type-specific branching |

**11 automated tests passing. 2 design-review items satisfied.**

---

## NFR Status

| NFR | Requirement | Status | Evidence |
|-----|-------------|--------|----------|
| Performance — Path B resolution latency | Path B surface type resolution must complete within 1 second | ✅ Pass | `nfr-path-b-resolution-completes-within-1s` passing. Resolution reads `context.yml` inline; no outbound calls. |
| Security — no credential values | git-native adapter must not log or persist credential values | ✅ Pass | `nfr-adapter-result-no-credential-values` passing. Adapter result scanned for credential patterns; none found. |
| Audit — surface and adapter version in result | Result object must include `surface` and `adapterVersion` | ✅ Pass | `nfr-result-contains-surface-and-adapter-version` passing. `result.surface` echoed from input; `result.adapterVersion` present in all results. |

---

## Metric Signal

**MM1 — Solo operator outer loop (indirect contribution):**

This story delivers the `execute(surface, context) → result` interface and Path B resolution, providing the canonical surface type vocabulary that downstream stories (p1.3-assurance-agent, p1.7-standards-model) scope their ACs against. Without a defined surface contract, outer loop story ACs would be surface-agnostic (lower precision) or hardcoded (future debt), increasing blocking lookups during definition. Contribution is structural/enabling; direct MM1 measurement not taken at Phase 1.

**Signal: on-track (enabling/structural)**

---

## DoD Observations

1. **Design-review ACs at DoD:** AC4 and AC5 are both designated `[Verification: code review]` in the story, and the DoR RISK-ACCEPT (W4) acknowledged that design review scenarios were not independently reviewed before DoD. Code review at DoD confirms both ACs satisfied. The registry-based dispatch pattern for extensibility is sound. No finding.

2. **context-path-b-shared.yml as integration boundary:** The shared fixture validates the P1.1/P1.2 `context.yml` format agreement boundary. This pattern — a shared fixture tested by both check-assembly.js and check-surface-adapter.js — is a good integration test convention for shared-boundary stories. **Candidate /levelup finding.**
