# Definition of Done: Implement watermark gate for eval regression detection

**Story reference:** artefacts/2026-04-09-skills-platform-phase1/stories/p1.4-watermark-gate.md
**DoR reference:** artefacts/2026-04-09-skills-platform-phase1/dor/p1.4-watermark-gate-dor.md
**Test plan reference:** artefacts/2026-04-09-skills-platform-phase1/test-plans/p1.4-watermark-gate-test-plan.md
**PR:** #16 (merged 2026-04-11) — `e741be5`
**DoD assessed by:** Copilot / Hamish
**Date:** 2026-04-11

---

## Entry check

- [x] PR merged to main/master: PR #16 merged 2026-04-11 (commit `e741be5`)
- [x] Story artefact accessible: `stories/p1.4-watermark-gate.md`
- [x] Test plan artefact accessible: `test-plans/p1.4-watermark-gate-test-plan.md`
- [x] DoR artefact and sign-off accessible: `dor/p1.4-watermark-gate-dor.md` — signed off 2026-04-10
- [x] `npm test` baseline confirmed: 138 checks passing (0 failed) — confirmed 2026-04-11

---

## AC Coverage

| AC | Description | Status | Evidence |
|----|-------------|--------|----------|
| AC1 | First gate run creates `results.tsv` with baseline row containing all six required columns (ISO timestamp, skill-set hash, surface type, pass rate, full test score, verdict `baseline`) | ✅ Satisfied | Unit tests `first-run-creates-results-tsv-with-baseline` and `first-run-baseline-row-schema` passing. Both schema completeness and correct typing verified. |
| AC2 | Gate verdict is `pass` and new row appended when current pass rate ≥ watermark AND score ≥ best | ✅ Satisfied | Multiple unit and integration tests — `gate-pass-when-at-watermark` and related tests passing. At-watermark and above-watermark cases both covered. |
| AC3 | Gate verdict is `blocked` (no human step) when either pass rate or full score drops below watermark | ✅ Satisfied | Unit tests for pass-rate-drop and score-drop cases passing. `gate-blocks-on-score-drop` and `gate-blocks-on-pass-rate-drop` tests passing. No human intervention required — block is automatic. |
| AC4 | Blocked verdict and reason visible in PR interface without opening a separate tool | ✅ Satisfied | Integration test `gate-verdict-surface-to-pr` passing — verifies verdict-surfacing step in CI workflow configuration produces output including verdict and reason. Visual PR confirmation pending first live CI run (same pattern as p1.3 AC4). |
| AC5 | Controlled acceptance test: add failing scenario → gate blocks; remove scenario → gate returns to pass; no permanent artefacts except `results.tsv` rows | ✅ Satisfied | Integration test `gate-acceptance-test-add-remove-scenario` passing — exercises the add/remove cycle against synthetic suite fixture. Live acceptance test (against real `workspace/suite.json`) is the M4 full acceptance evidence; mechanism confirmed by automated tests. |
| AC6 | `results.tsv` blocked row records the trigger condition: `pass-rate-below-watermark`, `score-below-best`, or `both` | ✅ Satisfied | Unit tests `gate-blocks-records-trigger-condition-*` passing. Blocked rows confirmed to include trigger field with the correct classification for each drop scenario. |

**All 6 ACs satisfied.**

---

## Out-of-Scope Check

Verified against the DoR contract out-of-scope list. No out-of-scope items were shipped:

- Automated remediation on regression detection: not present ✅
- Rolling-window or percentile watermark (Phase 2 variant): not present — high watermark model only ✅
- Per-story watermarks: not present — per skill/surface combination ✅
- Alert routing (PagerDuty, Slack, email): not present ✅
- Watermark decay or planned-regression support: not present ✅

**No out-of-scope violations.**

---

## Test Plan Coverage

| Test | Type | AC | Status |
|------|------|----|--------|
| `first-run-creates-results-tsv-with-baseline` | Unit | AC1 | ✅ Passing |
| `first-run-baseline-row-schema` | Unit | AC1 | ✅ Passing |
| `gate-pass-when-at-watermark` | Unit | AC2 | ✅ Passing |
| `gate-pass-when-above-watermark` | Unit | AC2 | ✅ Passing |
| `gate-blocks-on-pass-rate-drop` | Unit | AC3 | ✅ Passing |
| `gate-blocks-on-score-drop` | Unit | AC3 | ✅ Passing |
| `gate-blocks-records-trigger-condition-pass-rate` | Unit | AC6 | ✅ Passing |
| `gate-blocks-records-trigger-condition-score` | Unit | AC6 | ✅ Passing |
| `gate-blocks-records-trigger-condition-both` | Unit | AC6 | ✅ Passing |
| Integration test set (AC2, AC3, AC4, AC5) | Integration ×multiple | AC2–AC5 | ✅ Passing |
| `nfr-watermark-gate-completes-within-2-minutes` | NFR | NFR-perf | ✅ Passing |
| `nfr-results-tsv-no-secrets` | NFR | NFR-sec | ✅ Passing |
| `nfr-results-tsv-append-only` | NFR | NFR-audit | ✅ Passing |

**32 automated tests passing (check-watermark-gate.js).**

---

## NFR Status

| NFR | Requirement | Status | Evidence |
|-----|-------------|--------|----------|
| Performance — gate computation time | Gate verdict computation must complete within 2 minutes | ✅ Pass | `nfr-watermark-gate-completes-within-2-minutes` passing. Gate reads `results.tsv`, scores suite, compares watermarks — no outbound calls; well within 2-minute budget. |
| Security — `results.tsv` contains no credentials | `results.tsv` must contain only scores, hashes, and timestamps | ✅ Pass | `nfr-results-tsv-no-secrets` passing. `results.tsv` column set confirmed: timestamp, skillSetHash, surfaceType, passRate, fullTestScore, verdict + optional trigger field. No credential-pattern fields. |
| Audit — append-only `results.tsv` | Every gate run appends a row; rows never deleted or modified | ✅ Pass | `nfr-results-tsv-append-only` passing. Gate implementation appends new rows and never calls delete/truncate on `results.tsv`. |

---

## Metric Signal

**M4 — Watermark gate blocks regression (primary metric for this story):**

Both M4 sub-conditions are the ACs of this story (AC3 = automatic rejection, AC4 = visible in PR). The M4 controlled acceptance test procedure:

1. Add a deliberately failing scenario to `workspace/suite.json` ← automated tests exercise this via synthetic fixture
2. Run gate → observe `blocked` verdict ← automated tests confirm `blocked` produced correctly
3. Confirm rejection visible in PR ← CI workflow configuration validated; visual confirmation pending live run
4. Remove the scenario ← automated `add/remove` test confirms gate returns to `pass`
5. Confirm `results.tsv` rows remain (no permanent artefact removal) ← row-append NFR test confirms

M4 minimum validation signal: **MET via automated tests** — gate detects degraded score and writes `blocked` verdict to `results.tsv`. PR visibility sub-condition validated structurally. Full live acceptance test (real `workspace/suite.json` add/remove, visual PR check) to be executed by operator before Phase 2 planning.

**Signal: on-track** (mechanism fully validated; live acceptance test execution recommended before Phase 2)

---

## DoD Observations

1. **M4 live acceptance test should be run before Phase 2:** The full M4 acceptance run (add deliberate failing scenario to real `workspace/suite.json`, observe `blocked`, remove, confirm `pass` restored) has not been executed against the live workspace. The automated tests validate the mechanism with synthetic fixtures. A quick live run on master will confirm M4 signal with real evidence and is low risk. Recommended before Phase 2 planning session.

2. **`workspace/results.tsv` state:** The `results.tsv` file exists in the workspace with rows from the Phase 1 dogfood run. The watermark is based on the highest scores recorded. Phase 2 stories that add new scenarios to `suite.json` will need to handle watermark advancement on first run. **No action needed at DoD — noted as Phase 2 operational awareness.**
