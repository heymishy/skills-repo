# Test Plan: p1.8 — Author and review MODEL-RISK.md before non-dogfood adoption

**Story reference:** artefacts/2026-04-09-skills-platform-phase1/stories/p1.8-model-risk-documentation.md
**Feature:** 2026-04-09-skills-platform-phase1
**Review verdict:** PASS — Run 1 (0H, 1M resolved — 1-M4 AC5 reworded to test artefact content; 0L)
**Test plan authored:** 2026-04-10
**TDD discipline:** Tests authored to fail before implementation. Each test describes the required observable outcome; no test encodes a procedure.

---

## Decision carry-overs

| Finding | Decision | Impact on test plan |
|---------|----------|-------------------|
| 1-M4 — AC5 future adoption event | AC5 reworded (decisions.md entry 2026-04-10): tests onboarding documentation content, not a future squad onboarding event | AC5 test verifies that onboarding documentation lists MODEL-RISK.md as a required pre-read (not an optional reference) |

---

## Test inventory

### Unit tests

| ID | Test description | AC covered | Status |
|----|-----------------|------------|--------|
| U1 | Given `MODEL-RISK.md` exists, when it is parsed for the five named risks from the discovery artefact, then all five are present: (i) three-agent independence is procedural not structural; (ii) hash verification is an audit signal not tamper prevention; (iii) anti-overfitting gate is self-assessed; (iv) token budget may constrain delivery pace; (v) Copilot Agent mode inter-session limitation | AC1 | 🔴 FAIL (not yet implemented) |
| U2 | Given `MODEL-RISK.md` is parsed for each of the five risks, when each risk entry is inspected, then each has either a stated mitigation or an explicit acceptance rationale — no risk entry is bare (undocumented risk position) | AC1 | 🔴 FAIL (not yet implemented) |
| U3 | Given `MODEL-RISK.md` contains the eight audit question mapping, when a non-engineering reviewer reads the mapping table, then each of the eight audit questions maps to a specific named trace field — no mapping is absent or described as "TBD" | AC2 | 🔴 FAIL (not yet implemented) |
| U4 | Given the sign-off record block in `MODEL-RISK.md`, when it is inspected, then it contains all required fields: reviewer name, review date, verdict (approved for adoption / approved with conditions / not approved), and conditions if verdict is "approved with conditions" | AC4 | 🔴 FAIL (not yet implemented) |
| U5 | Given the onboarding documentation for the platform, when it is read, then `MODEL-RISK.md` is listed as a required pre-read — not an optional reference — and appears before the first inner loop instruction | AC5 | 🔴 FAIL (not yet implemented) |

### Integration tests

| ID | Test description | AC covered | Status |
|----|-----------------|------------|--------|
| I1 | Given a real inner loop story trace produced by the P1.3 CI gate after P1.7 is DoD-complete, when the eight audit questions from the platform operating model (§9.8) are applied to the trace file, then all eight questions are answerable from the trace file alone — and the evaluation result is recorded in `MODEL-RISK.md` as the T3M1 acceptance test record | AC3 | 🔴 FAIL (not yet implemented) |
| I2 | Given the T3M1 acceptance record in `MODEL-RISK.md`, when it is inspected, then it contains: story ID used as evidence, trace file path, each question's answer (Y/N), and an overall verdict (pass/fail) — no field is absent | AC3 | 🔴 FAIL (not yet implemented) |

### NFR tests

| ID | Test description | NFR | Status |
|----|-----------------|-----|--------|
| N1 | Given `MODEL-RISK.md` and all referenced trace evidence files are scanned for credential patterns, operation-detail specifics (internal port numbers, admin endpoints, infrastructure topology), and adversarially useful content, when the security check runs, then no match is found | Security NFR | 🔴 FAIL (not yet implemented) |
| N2 | Given the git log for `MODEL-RISK.md`, when a substantive update (risk added, evidence updated, sign-off appended) is expected, then the commit message for that update is traceable — it names the change type (e.g. "doc: add T3M1 evidence record to MODEL-RISK.md") | Audit NFR | 🔴 FAIL (not yet implemented) |

---

## Dependency-gated ACs

**AC3 (I1, I2)** requires:
1. `p1.3-assurance-agent-ci-gate` to be DoD-complete and running in CI
2. `p1.7-standards-model-phase1` to be DoD-complete (standards injected into the evidence trace)
3. A real inner loop story run through the P1.3 gate to produce the evidence trace

These tests do not block P1.8 DoR — they are scheduled as final-acceptance tests at P1.8 DoD time after both upstream stories are complete.

---

## AC2 — non-engineering reviewer navigability

AC2 states that a non-engineering reviewer can identify, for each audit question, which trace field answers it — without engineering assistance. This is verified by the U3 test (audit question → trace field mapping present and complete in MODEL-RISK.md) plus a manual review step at DoD time: the platform maintainer reads the document as a stand-in non-engineering reviewer and confirms navigability.

---

## AC4 — sign-off record

AC4 requires a human reviewer to sign off on `MODEL-RISK.md`. In the Phase 1 dogfood context, Hamish acts as stand-in for all reviewer roles. The sign-off record is the observable artefact: reviewer name, review date, verdict, conditions (if any). U4 tests the structural completeness of the sign-off record. The act of sign-off itself is a human action at DoD time.

---

## Gap register

No gaps. All ACs are testable at the appropriate stage. AC3 is dependency-gated but not untestable — it requires P1.3 and P1.7 DoD-complete first. AC4 includes a human sign-off action and a structural completeness automated check (U4).

---

## Test count summary

| Category | Count |
|----------|-------|
| Unit | 5 |
| Integration | 2 |
| NFR | 2 |
| **Total** | **9** |

**ACs with automated coverage:** AC1 (U1, U2), AC2 (U3), AC3 (I1, I2), AC4 (U4 + human sign-off at DoD), AC5 (U5)
**ACs verified by inspection / acceptance test:** AC3 (joint with I1/I2 — also manual navigability review), AC4 (human sign-off action)
**AC total:** 5

---

## e2e tooling

`e2eToolingRequired: false` — no layout-dependent or browser-interaction ACs. All acceptance paths use document content inspection, JSON trace file evaluation, and git log checks.
