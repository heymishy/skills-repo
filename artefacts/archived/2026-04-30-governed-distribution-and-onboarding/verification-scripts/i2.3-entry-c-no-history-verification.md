# AC Verification Script: `/orient` detects Entry C (no-history brownfield) and routes to retrospective `/discovery`

**Story reference:** artefacts/2026-04-30-governed-distribution-and-onboarding/stories/i2.3-entry-c-no-history.md
**Technical test plan:** artefacts/2026-04-30-governed-distribution-and-onboarding/test-plans/i2.3-entry-c-no-history-test-plan.md
**Script version:** 1
**Verified by:** _______________ | **Date:** _______________ | **Context:** [ ] Pre-code  [ ] Post-merge  [ ] Demo

---

## Setup

**Before you start:**
1. Open `.github/skills/orient/SKILL.md`.
2. Read the Entry C routing block (the fallback brownfield case).

---

## Scenarios

### Scenario 1 — Repo with git history and ops files but no code or artefacts: routes to Entry C (AC1)

**How to verify (SKILL.md inspection):**
Find the Entry C routing block.

**Expected outcome (check all):**
- `[ ]` The routing block is labelled "Entry C — no-history brownfield" (or equivalent canonical phrase)
- `[ ]` The block explains `/discovery` as a retrospective exercise (documenting what was built, not starting fresh)
- `[ ]` The `retrospective-story.md` template at `.github/templates/retrospective-story.md` is explicitly surfaced
- `[ ]` `/discovery` is named as the next skill with a "retrospective" note
- `[ ]` The word "retrospective" appears literally in the routing instruction (NFR)

**Pass:** All five checkboxes satisfied.
**Fail:** Any checkbox unchecked, including the NFR literal-word check.

---

### Scenario 2 — Entry C signal distinguishes itself from Entry B (AC2)

**How to verify (SKILL.md inspection):**
Read the Entry C detection signal description.

**Expected outcome (check all):**
- `[ ]` The Entry C signal description mentions absence of application logic files (the Entry B test was run and did not match)
- `[ ]` The Entry C signal description includes at least one brownfield indicator that is present — git history, infrastructure files, ops configs, or similar
- `[ ]` The classification output does NOT conflate Entry C with Entry B — it is clear the codebase was checked for app logic and found none

**Pass:** All three checkboxes satisfied.
**Fail:** Entry C signal does not distinguish itself from Entry B.

---

### Scenario 3 — Truly empty new repo routes to greenfield, NOT Entry C (AC3)

**How to verify (SKILL.md inspection):**
Read the Entry C detection condition and compare to the greenfield (i1.1 AC1) condition.

**Expected outcome (check all):**
- `[ ]` Entry C detection requires at least one brownfield indicator (git history, ops files, or infrastructure files)
- `[ ]` The greenfield / empty-repo case is handled by i1.1 AC1, before Entry C is ever reached in the routing order
- `[ ]` There is no pathway where a repo with no git history AND no files reaches Entry C
- `[ ]` If running live against an empty directory: the response names the new-repo greenfield state (i1.1 AC1), not Entry C

**Pass:** All four checkboxes satisfied.
**Fail:** Entry C can fire on a truly empty repo.
