# Definition of Ready: Entry B — `/orient` routes brownfield engineer with existing application code but no pipeline artefacts

**Story reference:** artefacts/2026-04-30-governed-distribution-and-onboarding/stories/i2.2-entry-b-code-first.md
**Test plan reference:** artefacts/2026-04-30-governed-distribution-and-onboarding/test-plans/i2.2-entry-b-code-first-test-plan.md
**Verification script:** artefacts/2026-04-30-governed-distribution-and-onboarding/verification-scripts/i2.2-entry-b-code-first-verification.md
**Assessed by:** Copilot (GitHub Copilot, Claude Sonnet 4.6)
**Date:** 2026-05-01

---

## Hard Blocks

| # | Check | Status | Notes |
|---|-------|--------|-------|
| H1 | User story in As / Want / So format with named persona | ✅ PASS | "As a brownfield engineer with an existing codebase but no pipeline artefacts / I want… / So that…" |
| H2 | At least 3 ACs in Given / When / Then format | ✅ PASS | 3 ACs (AC1, AC2, AC3) all in Given/When/Then format |
| H3 | Every AC has at least one test in the test plan | ✅ PASS | AC1: 4 tests; AC2: 2 tests; AC3: 1 unit + 1 integration; NFR: 1. Total: 9 tests. |
| H4 | Out-of-scope section populated | ✅ PASS | 3 items: auto-running /reverse-engineer, language-specific detection, artefacts-without-codebase state |
| H5 | Benefit linkage references a named metric | ✅ PASS | I2-M1 (brownfield engineers receive a non-generic routing message), I2-MM1 (unassisted correct routing rate) |
| H6 | Complexity rated | ✅ PASS | Rating: 2, Stable for MVP |
| H7 | No unresolved HIGH findings from review | ✅ PASS | Review: 0 HIGH, 0 MEDIUM, 1 LOW (no action required) |
| H8 | Test plan — no uncovered ACs | ✅ PASS | All 3 ACs covered; no gaps |
| H8-ext | Cross-story schema dependency check | ✅ PASS | Dependencies: i1.1 (ordering), i2.1 (recommended but non-blocking ordering). schemaDepends: [] |
| H9 | Architecture Constraints populated; no Category E HIGH findings | ✅ PASS | Orient SKILL.md extension; detection heuristic uses file extensions in src/app/lib dirs; Entry A takes precedence over Entry B (AC3); ADR-011. No Category E HIGH findings. |
| H-E2E | No CSS-layout-dependent AC without E2E coverage | ✅ PASS | No CSS-layout-dependent ACs |
| H-NFR | NFR profile exists or story has "None — reviewed" | ✅ PASS | NFR profile: artefacts/2026-04-30-governed-distribution-and-onboarding/nfr-profile.md |
| H-NFR2 | Compliance NFRs with regulatory clauses have human sign-off | ✅ PASS | No compliance regulatory clauses — N/A |
| H-NFR3 | Data classification in NFR profile not blank | ✅ PASS | Data classification: Public |
| H-NFR-profile | NFR profile present | ✅ PASS | nfr-profile.md exists |
| H-GOV | Discovery `Approved By` has ≥1 non-blank named entry | ✅ PASS | "Hamish — Platform Maintainer — 2026-04-30" in `## Approved By` section of discovery.md |

**Hard blocks result: 16/16 PASS — no blocks failed**

---

## Warnings

| # | Check | Status | Risk if proceeding | Acknowledged by |
|---|-------|--------|--------------------|-----------------|
| W1 | NFRs identified or "None — confirmed" | ✅ | — | 1 NFR declared: Entry B detection must not fire when Entry A conditions are present |
| W2 | Scope stability declared | ✅ | — | Stable for MVP |
| W3 | MEDIUM review findings acknowledged | ✅ | — | 0 MEDIUM findings on i2.2 review — N/A |
| W4 | Verification script reviewed by domain expert | ⚠️ WARN | Verification script produced by same session as test plan; no independent expert review — risk of blind spots particularly around Entry B detection false-positive threshold | Risk accepted by operator — 3-scenario script covers primary path (code-first detected), precedence (Entry A fires before Entry B), and config-only repos (Entry B does NOT fire); low-risk routing logic extension |
| W5 | No UNCERTAIN test plan gaps | ✅ | — | No UNCERTAIN gaps |

**Warnings result: W4 acknowledged and risk accepted — proceeding**

---

## Oversight

**Level: Medium**
Source: Epic (i2-brownfield-routing-epic.md) — "Brownfield routing extends a core skill file; changes should be reviewed against the orient skill's routing consistency contract before PR merge."

Share the DoR artefact with the platform maintainer before assigning. No formal sign-off required — awareness required.

---

## Contract Proposal — i2.2

**What will be built:**
An extension to `.github/skills/orient/SKILL.md` adding an Entry B detection section:

1. Detection heuristic: Entry B fires when the repo contains application logic files (`.js`, `.ts`, `.py`, `.java`, `.rb`, `.go`, `.cs`, `.cpp`, `.rs`, `.swift`, `.kt`) under `src/`, `app/`, or `lib/` directories AND has no `artefacts/` folder or an empty `artefacts/` folder.
2. Routing output for Entry B: Acknowledges the existing codebase, recommends running `/reverse-engineer` to extract business rules and build artefacts before continuing with `/discovery`.
3. Priority rule: Entry A (story artefacts present) takes precedence over Entry B. If both signals are present, Entry A fires.
4. Boundary: repos containing only config/infrastructure files (`.yml`, `.json`, Dockerfile, `terraform/`, `k8s/`) do NOT trigger Entry B.

**What will NOT be built:**
- Auto-running /reverse-engineer
- Language-specific detection (same extension list for all stacks)
- Detection when artefacts/ is present but non-empty (Entry A handles this)

**How each AC will be verified:**

| AC | Test approach | Type |
|----|---------------|------|
| AC1 — Entry B detected + codebase-specific routing output | Assert orient SKILL.md has Entry B section; routing output acknowledges existing codebase; output references /reverse-engineer | Unit |
| AC2 — routing output references both /reverse-engineer and /discovery | Assert output references both commands with correct ordering guidance | Unit |
| AC3 — Entry A fires before Entry B when both signals present | Assert Entry A routing fires when story artefacts are present even if code-first signals also exist; assert Entry B does NOT fire | Unit + Integration |
| NFR — config-only repo does NOT trigger Entry B | Assert Dockerfile/yml/json-only repo falls through to Entry C path | Unit |

**Assumptions:**
- `.github/skills/orient/SKILL.md` exists at time of implementation (created by i1.1)
- "No artefacts/ folder" means neither directory nor any files in it — i.e., a repo where the pipeline has never been run

**Estimated touch points:**
Files modified: `.github/skills/orient/SKILL.md` only

---

## Contract Review

No mismatches found between proposed implementation and stated ACs. Contract review PASSED.

---

## Coding Agent Instructions

**Story:** i2.2 — Entry B routing for brownfield engineer with existing code
**Story artefact:** artefacts/2026-04-30-governed-distribution-and-onboarding/stories/i2.2-entry-b-code-first.md
**Test plan:** artefacts/2026-04-30-governed-distribution-and-onboarding/test-plans/i2.2-entry-b-code-first-test-plan.md
**Verification script:** artefacts/2026-04-30-governed-distribution-and-onboarding/verification-scripts/i2.2-entry-b-code-first-verification.md

### Scope

Modify 1 existing file:
- Modify `.github/skills/orient/SKILL.md` — add Entry B detection and routing section

Do NOT modify:
- Any other SKILL.md file
- Any file under `artefacts/`
- `pipeline-state.json`
- `.github/templates/`
- Any test file
- Any `scripts/` file

### Dispatch constraint

Do not dispatch until i1.1 is merged (Entry B extends the orient SKILL.md which i1.1 creates). i2.1 is also recommended to be merged first but i2.1 is currently blocked — if i2.1 remains blocked when i2.2 is dispatched, implement Entry B with the notation that Entry A detection will be added when i2.1 is unblocked.

### TDD approach

All 9 tests in `tests/check-i2.2-entry-b-code-first.js` must be RED before you start.
Make each test GREEN by extending `.github/skills/orient/SKILL.md`.
Run `node tests/check-i2.2-entry-b-code-first.js` after each change.
Do not modify the test file.

### Implementation guidance

1. Read `.github/skills/orient/SKILL.md` in full before making any changes.
2. Add a new section "## Entry B — Brownfield: Existing code, no pipeline artefacts" with:
   - Detection signal: application logic files (`.js`, `.ts`, `.py`, `.java`, `.rb`, `.go`, `.cs`, `.cpp`, `.rs`, `.swift`, `.kt`) under `src/`, `app/`, or `lib/`; AND no `artefacts/` folder or empty `artefacts/`
   - Priority note: "Entry A takes precedence — if story artefacts are present, Entry A routing applies"
   - Routing instruction: acknowledge existing codebase, recommend `/reverse-engineer` to extract rules and build artefacts, then continue with `/discovery`
   - Boundary: config-only files (`.yml`, `.json`, Dockerfile) do NOT trigger Entry B — these fall through to Entry C
3. Ensure the Entry B section is positioned after Entry A and before Entry C (fallback) in the skill file.

### Constraints

- SKILL.md is a natural language instruction file — modify using Markdown, not code
- Do not change the structure of any existing section in orient SKILL.md
- Maintain routing order: Entry A → Entry B → Entry C
- ADR-011: open a draft PR — never commit directly to master

### Completion check

Run `npm test` from the repo root. The following must be true:
- `node tests/check-i2.2-entry-b-code-first.js` — all 9 tests pass
- No other tests that were passing before your change now fail

Open a draft PR targeting `master`. Never mark ready for review. Never merge.
