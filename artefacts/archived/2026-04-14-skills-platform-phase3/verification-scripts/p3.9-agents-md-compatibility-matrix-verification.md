# AC Verification Script: AGENTS.md compatibility matrix (p3.9)

**Story:** p3.9 — AGENTS.md compatibility matrix
**Reviewer:** Human operator
**Date:** 2026-04-18

---

## Setup

1. Ensure the story implementation PR is open.
2. Checkout or view the PR branch.
3. Run: `npm test` — must pass with zero failures before you proceed.

---

## Scenario 1 — AC1: compatibility matrix file exists with correct rows and columns

**Given:** Story PR is merged or checked out locally

**Steps:**
1. Open `docs/agent-compatibility-matrix.md` in an editor.
2. Verify the file exists.
3. Count the tool rows — confirm at least 4 tools are listed: GitHub Copilot, Cursor, Claude Code, and one additional named enterprise tool.
4. Read the column headers — confirm all 6 columns are represented: tool name, instruction surface resolution, system prompt delivery, DoR handoff compatibility, trace generation, known gaps.

**Expected outcome:** File exists; 4+ tool rows; 6 columns present.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 2 — AC2: "partial"/"no" cells have actionable gap descriptions

**Given:** `docs/agent-compatibility-matrix.md` is open

**Steps:**
1. Scan all cells; identify any that read "partial" or "no" under any capability column.
2. For each such row, read the "known gaps" cell.
3. Verify the cell is not empty and does not say only "unknown."
4. Confirm each gap description is specific and actionable (e.g. names a file, mechanism, or limitation).

**Expected outcome:** All "partial"/"no" rows have specific gap descriptions.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 3 — AC3: Phase 4 backlog file exists with required content

**Given:** Story PR is merged or checked out locally

**Steps:**
1. Open `workspace/phase4-backlog-agent-live-delivery-run.md`.
2. Verify the file exists.
3. Confirm it contains: (a) a story title, (b) the text "ASSUMPTION-04" and the gate condition, (c) a prerequisites section.

**Expected outcome:** File exists with all required sections.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 4 — AC4: PR diff contains only the two expected files

**Given:** PR is open on GitHub

**Steps:**
1. Navigate to the PR on GitHub.
2. Open the "Files changed" tab.
3. Verify exactly two files are changed: `docs/agent-compatibility-matrix.md` and `workspace/phase4-backlog-agent-live-delivery-run.md`.
4. Confirm no SKILL.md files, no scripts, no CI YAML files are in the diff.

**Expected outcome:** Exactly two files in diff — both expected files.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 5 — AC5: Matrix satisfies M5 minimum signal tier

**Given:** Benefit-metric artefact is available at `artefacts/2026-04-14-skills-platform-phase3/benefit-metric.md`

**Steps:**
1. Open `benefit-metric.md` and locate M5 (or the metric covering agent compatibility evidence).
2. Confirm the matrix in `docs/agent-compatibility-matrix.md` constitutes the Phase 3 minimum evidence for that metric.
3. Confirm the Phase 4 live delivery run backlog item is noted as the next evidence tier.

**Expected outcome:** M5 minimum evidence (matrix) is documented; next tier (Phase 4 run) is named.

**Pass:** ☐ **Fail:** ☐

---

## Sign-off

All scenarios passed: ☐

Reviewer: _________________________ Date: _________________
