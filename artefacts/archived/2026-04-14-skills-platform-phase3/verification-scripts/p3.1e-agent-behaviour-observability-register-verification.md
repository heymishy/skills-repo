# AC Verification Script: Register agent behaviour observability as Phase 4 backlog item (p3.1e)

**Story reference:** artefacts/2026-04-14-skills-platform-phase3/stories/p3.1e-agent-behaviour-observability-register.md
**Technical test plan:** artefacts/2026-04-14-skills-platform-phase3/test-plans/p3.1e-agent-behaviour-observability-register-test-plan.md
**Script version:** 1
**Verified by:** ______________ | **Date:** ______________ | **Context:** [ ] Pre-code  [ ] Post-merge  [ ] Demo

---

## Setup

**Before you start:**
1. Open a terminal at the repo root.
2. Run `npm test` — confirm it passes before proceeding.

---

## Scenarios

---

### Scenario 1: The candidate analysis document exists with exactly three approaches

**Covers:** AC1 (name and count)

**Steps:**
1. Check that the file exists: `Test-Path docs/agent-behaviour-observability.md` (Windows) or `test -f docs/agent-behaviour-observability.md && echo "exists"` (Linux/Mac).
2. Open the file. Count the distinct candidate approaches described.

**Expected outcome:**
> The file exists at `docs/agent-behaviour-observability.md`. Exactly three candidate approaches are described — no more, no fewer. Each candidate has a visible name (heading or bold title).

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 2: Each candidate has a mechanism, effort level, and tradeoff

**Covers:** AC1 (mechanism / effort / tradeoff)

**Steps:**
1. Open `docs/agent-behaviour-observability.md`.
2. For each of the three candidates, check:
   - A paragraph of at least one sentence describes how the approach works (the mechanism).
   - An effort level appears: `Low`, `Medium`, or `High`.
   - A tradeoff is noted (look for "tradeoff", "trade-off", or a **Tradeoff** label).

**Expected outcome:**
> All three candidates have all three required sub-fields. Nothing is left as a placeholder like `TBD` or `[fill in]`.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 3: Document explicitly declares no Phase 3 implementation

**Covers:** AC2

**Steps:**
1. Open `docs/agent-behaviour-observability.md`.
2. Look for a section or statement near the top that confirms no code is being implemented in Phase 3.
3. Look for a reference to Phase 4 and the backlog registration as the only deliverable in this phase.

**Expected outcome:**
> The document contains an explicit statement such as "No implementation is included in Phase 3" (or equivalent wording). Phase 4 is named as the point where a selection will be made and implementation will begin.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 4: A Phase 4 backlog entry exists in the workspace directory

**Covers:** AC3

**Steps:**
1. List files in the `workspace/` directory matching the naming pattern: `dir workspace\phase4-backlog-*.md` (Windows) or `ls workspace/phase4-backlog-*.md` (Linux/Mac).
2. Open the file.

**Expected outcome:**
> At least one file exists matching `workspace/phase4-backlog-*.md` and it relates to agent behaviour observability (the filename or title makes this clear).

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 5: Phase 4 backlog entry has all required content

**Covers:** AC3 (content completeness)

**Steps:**
1. Open the `workspace/phase4-backlog-*.md` file found in Scenario 4.
2. Check that all four required fields are present:
   - A title (clearly names the work item).
   - A problem statement (at least one explanatory sentence describing the problem or gap).
   - A reference back to `docs/agent-behaviour-observability.md` for the candidate details.
   - A status marker of `NOT STARTED` (or equivalent phrasing indicating Phase 4 has not begun).

**Expected outcome:**
> All four fields are present and non-empty. A Phase 4 team reading this file can immediately understand: what the work is, why it matters, where the candidate analysis lives, and that it has not been started.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 6: npm test passes cleanly after the documentation files are added

**Covers:** AC1, AC2, AC3 (via automated assertions)

**Steps:**
1. Run `npm test`.
2. Check the output for any new test failures.

**Expected outcome:**
> `npm test` exits 0. Any new tests added to verify the documentation files (file existence + content checks) are green. No previously-passing tests have regressed.

**Result:** [ ] Pass  [ ] Fail
**Notes:**
