# AC Verification Script: Teams and Jira enterprise approval channel adapters (p3.8)

**Story:** p3.8 — Deliver Teams and Jira enterprise approval channel adapters
**Reviewer:** Human operator
**Date:** 2026-04-18

---

## Setup

1. Checkout the implementation PR branch.
2. Run: `npm test` — must pass with zero failures before proceeding.
3. Confirm the approval-channel adapter directory exists: `src/approval-channel/adapters/`.

---

## Scenario 1 — AC1: Teams adapter processes valid payload and writes dorStatus: "signed-off"

**Given:** `src/approval-channel/adapters/teams-adapter.js` is implemented

**Steps:**
1. Run the Teams adapter unit tests: `npm test -- --grep "teams-adapter"`
2. Verify the happy-path test passes: valid Adaptive Card payload → `process-dor-approval.js` mock called with correct storyId → `dorStatus: "signed-off"` produced.
3. Verify the HMAC rejection test passes: tampered signature → 401 → no approval written.

**Expected outcome:** Both Teams adapter tests pass.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 2 — AC2: Jira adapter processes transition and /approve-dor comment payloads

**Given:** `src/approval-channel/adapters/jira-adapter.js` is implemented

**Steps:**
1. Run Jira adapter unit tests: `npm test -- --grep "jira-adapter"`
2. Verify transition payload test passes: Jira issue transitioned to "Approved" → `process-dor-approval.js` mock called → `dorStatus: "signed-off"` produced.
3. Verify comment payload test passes: comment body `/approve-dor` → same outcome.

**Expected outcome:** Both Jira adapter paths pass.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 3 — AC3: No real credentials or network access in CI test suite

**Given:** Both adapters are implemented and their tests pass

**Steps:**
1. Review the test files for both adapters and confirm all HTTP clients are mocked (jest.mock, nock, or equivalent).
2. Run the full test suite in an environment with no internet access (or with a network interceptor that throws on any real outbound HTTP call).
3. Verify the suite still passes completely.

**Expected outcome:** Test suite passes with zero real network connections.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 4 — AC4: Failure cases return 4xx/5xx and log the error

**Given:** Both adapters are implemented

**Steps:**
1. Run failure handling unit tests: `npm test -- --grep "missing-story-id"` and `npm test -- --grep "write-failure"`
2. Verify: missing storyId in payload → 4xx response (400 or 422); no approval written.
3. Verify: `process-dor-approval.js` write throws → 5xx response; error is logged (check test spy on logger or `console.error`).
4. Confirm neither failure case produces an unhandled promise rejection or silent 200 response.

**Expected outcome:** Correct error status codes; errors are always logged.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 5 — AC5: Credentials are read from environment variables — no hardcoded values

**Given:** Both adapter source files exist

**Steps:**
1. Open `src/approval-channel/adapters/teams-adapter.js` and search for any hardcoded string values that look like URLs, tokens, or secrets.
2. Open `src/approval-channel/adapters/jira-adapter.js` and do the same.
3. Verify both adapters read credentials via `process.env.TEAMS_WEBHOOK_SECRET` (or named equivalent) and `process.env.JIRA_SERVICE_TOKEN` (or named equivalent).
4. Run the grep-based credential test: `npm test -- --grep "credentials-read-from-env-vars"`

**Expected outcome:** No hardcoded credentials found in source; env var reads confirmed.

**Pass:** ☐ **Fail:** ☐

---

## Sign-off

All scenarios passed: ☐

Reviewer: _________________________ Date: _________________
