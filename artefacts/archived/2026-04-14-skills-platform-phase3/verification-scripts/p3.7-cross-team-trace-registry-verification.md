# AC Verification Script: Cross-team trace registry (p3.7)

**Story:** p3.7 — Establish cross-team trace registry with 24-hour aggregation SLA
**Reviewer:** Human operator
**Date:** 2026-04-18

---

## Setup

1. Checkout the implementation PR branch.
2. Run: `npm test` — must pass with zero failures before proceeding.
3. Seed the local trace registry with the test fixtures if needed.

---

## Scenario 1 — AC1: Aggregation pipeline writes to correct path

**Given:** Implementation PR is open; aggregation script or workflow is present

**Steps:**
1. Inspect the aggregation script or CI workflow for the output path logic.
2. Confirm the output path follows `platform/traces/[squadId]/[storySlug].jsonl`.
3. (Optional, if you can run a test aggregation locally): Seed a dummy squad trace at `workspace/traces/test-squad/story-x.jsonl`; run the aggregation; verify output appears at `platform/traces/test-squad/story-x.jsonl`.

**Expected outcome:** Output path follows the required schema; `[squadId]` and `[storySlug]` are substituted correctly.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 2 — AC2: getTraces() returns matching entries and empty array on no-match

**Given:** `src/trace-registry/getTraces.js` (or equivalent) is implemented

**Steps:**
1. Run the unit tests for getTraces: `npm test -- --grep getTraces`
2. Inspect test results for: (a) squadId filter returns only that squad's traces, (b) date range filter works, (c) passRate filter works, (d) no-match filter returns `[]` not an error.
3. Verify all 4 getTraces unit tests pass.

**Expected outcome:** All 4 tests pass; exact return types verified in test output.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 3 — AC3: 24-hour SLA enforced by daily (or more frequent) schedule trigger

**Given:** Aggregation CI workflow YAML exists

**Steps:**
1. Open the aggregation workflow YAML.
2. Find the `schedule:` trigger block.
3. Verify the cron expression runs at most once per 24 hours (e.g. `0 0 * * *` = daily midnight, `0 */12 * * *` = twice daily — both satisfy ≤24h).
4. Confirm the workflow is NOT triggered only on push or pull_request events.

**Expected outcome:** Schedule trigger present; cadence is ≤24 hours.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 4 — AC4: Separate trigger + read-only token (ADR-009)

**Given:** Aggregation CI workflow YAML exists

**Steps:**
1. Verify the aggregation workflow is a separate file from the delivery pipeline CI (not in the same workflow YAML as the assurance gate or test run jobs).
2. Read the `permissions:` block; confirm it shows `contents: read` or equivalent read-only permission.
3. Confirm no write permission to squad repos is granted.

**Expected outcome:** Separate workflow file; read-only permissions block.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 5 — AC5: Unregistered squad returns empty array — not an error

**Given:** getTraces() is implemented

**Steps:**
1. Run: `node -e "const { getTraces } = require('./src/trace-registry/getTraces'); getTraces({ squadId: 'squad-that-does-not-exist' }).then(r => console.log(r, typeof r, r.length)).catch(e => console.error('ERROR:', e))"`
2. Observe output.

**Expected outcome:** `[]` printed; `length` is `0`; no error or exception thrown.

**Pass:** ☐ **Fail:** ☐

---

## Sign-off

All scenarios passed: ☐

Reviewer: _________________________ Date: _________________
