# AC Verification Script: Automated compliance monitoring report (p3.13)

**Story:** p3.13 — Produce automated compliance monitoring report from cross-team trace registry
**Reviewer:** Human operator
**Date:** 2026-04-18

---

## Pre-conditions

- p3.7 (cross-team trace registry) must be DoD-complete.
- p3.2a (T3M1 trace fields) must be DoD-complete.

---

## Setup

1. Checkout the implementation PR branch.
2. Run: `npm test` — must pass with zero failures before proceeding.
3. Confirm the compliance report module exists (check the PR for path).

---

## Scenario 1 — AC1: Monthly schedule triggers getTraces() and produces report at correct path

**Given:** Compliance report module is implemented; CI workflow exists

**Steps:**
1. Run the compliance report unit tests: `npm test -- --grep "compliance-report"`
2. Verify the filename format test passes: a report generated for date 2026-04-01 produces `workspace/compliance-reports/compliance-2026-04.md`.
3. Open the CI workflow YAML for the compliance report job; verify it has a `schedule:` trigger with monthly cadence (e.g. `cron: '0 0 1 * *'` or similar).

**Expected outcome:** Filename format correct; monthly schedule trigger confirmed.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 2 — AC2: Report contains per-squad fields for each sampled squad

**Given:** Compliance report module is implemented

**Steps:**
1. Run: `npm test -- --grep "report-contains-per-squad-fields"` — verify test passes.
2. (Optional) Generate a local test report using the fixture data.
3. Open the generated report; for each squad block, verify all six fields are present: squad ID, trace count sampled, gate verdicts present (yes/no), T3M1 fields populated (yes/no with count if any missing), gap count, compliance status (PASS/FAIL).

**Expected outcome:** All six fields present in every squad block.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 3 — AC3: Squad with >10% missing gate verdicts is marked FAIL with gap details

**Given:** Compliance report module is implemented

**Steps:**
1. Run: `npm test -- --grep "squad-above-gap-threshold-is-marked-fail"` — verify test passes.
2. Run: `npm test -- --grep "squad-at-gap-threshold-is-not-marked-fail"` — verify boundary test passes (exactly 10% → PASS).
3. (Optional) Generate a report with the `squad-failing` fixture (15/100 missing gate verdicts).
4. Verify the squad-failing block shows FAIL status, lists the count of missing-verdict traces, and names the specific missing field ("gate verdict").

**Expected outcome:** >10% → FAIL with details; exactly 10% → PASS; boundary condition correct.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 4 — AC4: CI compliance job uses separate schedule trigger and read-only token

**Given:** Compliance monitoring CI workflow YAML exists

**Steps:**
1. Open the compliance monitoring CI workflow YAML.
2. Verify it is a separate file from the delivery pipeline CI and from the trace aggregation workflow (p3.7).
3. Verify the trigger is `schedule:` only — NOT `push:` or `pull_request:`.
4. Verify the `permissions:` block shows `contents: read` or read-only equivalent; no write permissions.

**Expected outcome:** Separate workflow file; schedule trigger only; read-only permissions.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 5 — AC5: Report is self-contained with all context fields

**Given:** A compliance report has been generated (test fixture or actual)

**Steps:**
1. Open the generated report.
2. Verify it contains in its header or footer section: (a) the policy gap threshold (10%), (b) the total sample size (total traces reviewed), (c) the period covered (e.g. "last 30 days" or specific date range), (d) the trace registry path (`platform/traces/`).
3. Run: `npm test -- --grep "report-is-self-contained"` — verify test passes.
4. Read the report as if you are a risk reviewer with no other context — confirm you can interpret every finding without asking the platform team for clarification.

**Expected outcome:** All four context fields present; report is interpretable by a cold reader.

**Pass:** ☐ **Fail:** ☐

---

## Scenario 6 — NFR: Monthly reports are not overwritten

**Given:** Two months of report generation are simulated

**Steps:**
1. Run: `npm test -- --grep "report-filename-does-not-overwrite"` — verify test passes.
2. (Optional) Generate reports for two different months; confirm both files exist at distinct paths.

**Expected outcome:** `compliance-YYYY-MM.md` filename ensures no collision between months.

**Pass:** ☐ **Fail:** ☐

---

## Sign-off

Pre-conditions confirmed (p3.7 DoD-complete: ☐, p3.2a DoD-complete: ☐)
All scenarios passed: ☐

Reviewer: _________________________ Date: _________________
