# AC Verification Script: Resolve permanently-skipped Bitbucket DC auth tests (p3.1d)

**Story reference:** artefacts/2026-04-14-skills-platform-phase3/stories/p3.1d-bitbucket-dc-auth-tests.md
**Technical test plan:** artefacts/2026-04-14-skills-platform-phase3/test-plans/p3.1d-bitbucket-dc-auth-tests-test-plan.md
**Script version:** 1
**Verified by:** ______________ | **Date:** ______________ | **Context:** [ ] Pre-code  [ ] Post-merge  [ ] Demo

---

## Setup

**Before you start:**
1. Confirm which option was chosen: record here: **Option chosen: [ ] A (scheduled CI)  [ ] B (smoke-tests.md)**
2. Open a terminal at the repo root.
3. Run `npm test` — confirm it passes before proceeding.

---

## Scenarios

---

### Scenario 1: The four auth tests are no longer permanently skipped

**Covers:** AC1, AC4

**Steps:**
1. Run `npm test 2>&1 | grep -i "bitbucket-dc\|app-password\|oauth\|ssh key\|pat"` (or on Windows: `npm test 2>&1 | Select-String "bitbucket-dc|app-password|oauth|ssh key|PAT"`).
2. Look at the output for each of the four auth tests.

**Expected outcome (Option A — scheduled CI):**
> The four auth tests do not appear with a `# SKIP` or `skipped` marker in standard `npm test` output. They are moved to the scheduled workflow and no longer annotated with `// PREREQ-DOCKER` in `tests/check-bitbucket-dc.js`.

**Expected outcome (Option B — smoke-tests.md):**
> The four auth tests appear as skipped in `npm test` output BUT each has a `// MANUAL: see smoke-tests.md` annotation — not the old `// PREREQ-DOCKER` permanent skip. The output makes clear where to find the manual record.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 2 (Option A only): Scheduled workflow file exists with correct trigger

**Covers:** AC2

**Applies when:** Option A was chosen. Skip this scenario if Option B was chosen.

**Steps:**
1. Check that `.github/workflows/bitbucket-dc-auth.yml` exists: `Test-Path .github/workflows/bitbucket-dc-auth.yml` (Windows) or `test -f .github/workflows/bitbucket-dc-auth.yml && echo "exists"` (Linux/Mac).
2. Open the file and look at the `on:` section.

**Expected outcome:**
> The file exists. The `on:` section contains `schedule:` (with a cron entry). Neither `pull_request` nor `push` appears under `on:`. The auth tests therefore cannot block a standard PR merge.

**Result:** [ ] Pass  [ ] Fail  [ ] N/A — Option B chosen
**Notes:**

---

### Scenario 3 (Option B only): smoke-tests.md contains complete entries for all four auth tests

**Covers:** AC3

**Applies when:** Option B was chosen. Skip this scenario if Option A was chosen.

**Steps:**
1. Open `tests/smoke-tests.md`.
2. Find the entries for `app-password`, `OAuth`, `SSH key`, and `PAT`.
3. For each entry, check all five fields are present: test name, last-run date (e.g. `2026-04-14`), result (`pass` or `fail`), runner identity, and a note about the Docker dependency.

**Expected outcome:**
> All four entries exist. Each has all five fields filled in. No entry is blank or uses a placeholder like `TBD`.

**Result:** [ ] Pass  [ ] Fail  [ ] N/A — Option A chosen
**Notes:**

---

### Scenario 4: Decision between Option A and Option B is on record

**Covers:** AC5

**Steps:**
1. Open `artefacts/2026-04-14-skills-platform-phase3/decisions.md`.
2. Find the entry for the Bitbucket DC auth test resolution.

**Expected outcome:**
> An entry exists naming which option was chosen (A or B) and explaining why. The rationale is at least one sentence — it is not a blank placeholder.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 5: npm test passes cleanly after the change

**Covers:** AC4, AC5

**Steps:**
1. Run `npm test` at the repo root.
2. Check that the total number of passing tests is equal to or greater than before this story was implemented.
3. Confirm no test that was passing before is now failing.

**Expected outcome:**
> `npm test` exits 0 (all tests pass). The four auth tests are either passing (Option A) or cleanly skipped with the correct annotation (Option B). No regressions.

**Result:** [ ] Pass  [ ] Fail
**Notes:**
