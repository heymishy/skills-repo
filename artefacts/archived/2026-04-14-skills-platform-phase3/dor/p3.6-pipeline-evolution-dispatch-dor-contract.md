# DoR Contract: Add forward-pointer and Closes guidance to issue-dispatch SKILL.md

**Story:** artefacts/2026-04-14-skills-platform-phase3/stories/p3.6-pipeline-evolution-dispatch.md
**DoR artefact:** artefacts/2026-04-14-skills-platform-phase3/dor/p3.6-pipeline-evolution-dispatch-dor.md
**Contract version:** 1
**Assessed:** 2026-04-18

---

## What will be built

1. **`.github/skills/issue-dispatch/SKILL.md`** — modified to add a section (or update the dispatch output template) that includes a `Closes #[issueNumber]` line in the PR description guidance. The `[issueNumber]` placeholder is replaced by the issue number of the open dispatch issue. The template section must also include a reference comment to the GitHub auto-close documentation (URL or doc name). Existing headings and content are preserved.

2. **`.github/skills/definition-of-ready/SKILL.md`** — modified to add a forward-pointer in the sign-off block section. The forward-pointer says that after sign-off, the operator runs `/issue-dispatch`, and that the dispatch skill generates the `Closes #[issue]` guidance automatically in the PR body — the operator does not need to add it manually.

---

## What will NOT be built

- No changes to `pipeline-state.json` recording of dispatch
- No changes to any other skill or SKILL.md beyond the two listed above
- No automated testing of the GitHub auto-close mechanism (Out of Scope)

---

## AC verification mapping

| AC | Test | Type | Spec |
|----|------|------|------|
| AC1 | `.github/skills/issue-dispatch/SKILL.md` contains `Closes #` | Unit / file-read | Read file; assert `Closes #` string present in template section |
| AC2 | `.github/skills/definition-of-ready/SKILL.md` contains forward-pointer to `/issue-dispatch` | Unit / file-read | Read file; assert `/issue-dispatch` forward-pointer present in sign-off section |
| AC3 | Dispatch SKILL.md contains reference to GitHub auto-close documentation | Manual / PR review | Reviewer confirms URL or doc-name reference present in template section per 1-L1 clarification |
| AC4 | PR diff shows exactly two SKILL.md files changed (PAT-07 compliance) | Manual / PR diff | Reviewer confirms no unrelated files in diff |

---

## Assumptions

- The existing content of both SKILL.md files is preserved — insertions only, no deletions of existing content.
- The issue number placeholder `[issueNumber]` is the literal token used in the template; it is replaced at runtime by the dispatch output.

---

## Estimated touch points

**Files modified (2):**
- `.github/skills/issue-dispatch/SKILL.md`
- `.github/skills/definition-of-ready/SKILL.md`

**Files NOT touched:**
- Any other `.github/skills/` file
- `scripts/` (any file)
- `pipeline-state.json`

---

## schemaDepends

`schemaDepends: []` — Dependencies.Upstream: None. No cross-story pipeline-state.json schema field consumption.
