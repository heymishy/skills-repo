# AC Verification Script: Extend `/start` skill — brownfield signal routing

**Story reference:** `artefacts/2026-04-24-platform-onboarding-distribution/stories/p11.7-start-brownfield-routing.md`
**Technical test plan:** `artefacts/2026-04-24-platform-onboarding-distribution/test-plans/p11.7-test-plan.md`
**Script version:** 1
**Verified by:** ____________ | **Date:** ____________ | **Context:** [ ] Pre-code  [ ] Post-merge  [ ] Demo

---

## Setup

**Before you start:**
1. Ensure the implementation PR for p11.7 has been merged (or the branch is checked out).
2. Confirm p11.6 is also delivered (this story extends the `/start` SKILL.md created in p11.6).
3. Open `.github/skills/start/SKILL.md` in your editor.
4. A Copilot chat session is required for Scenarios 2–4 (live-agent).

**Reset between scenarios:** Scenarios 2–4 involve live-agent invocations. Run each in a fresh chat for clean output. File-system changes are not needed — the skill reads the actual repo structure.

---

## Scenarios

---

### Scenario 1: SKILL.md brownfield routing instruction text and priority order

**Covers:** AC1–AC5 (instruction text level)

**Steps:**
1. Open `.github/skills/start/SKILL.md`.
2. Locate the brownfield routing section (added by this story).
3. Confirm each of the following is present:
   a. **Entry C** (check first): detection of `artefacts/[feature]/dod/` → routes to `.github/templates/retrospective-story.md` + one-sentence explanation.
   b. **Entry A** (check second): detection of `artefacts/[feature]/stories/` story files → routes to `/tdd [story-slug]` + one-sentence explanation.
   c. **Entry B** (check third): detection of `src/*.js` non-test files with no `stories/` or `dod/` → routes to `/reverse-engineer` + one-sentence explanation.
   d. **No-signal fallback**: explicit instruction that when none of C, A, or B are detected, fall back to the greenfield path from p11.6.
   e. **Priority order C > A > B**: explicitly stated (e.g. "check DoD first", "DoD record takes priority over story files").
4. Confirm `brownfield-routing` contract marker is present.
5. Confirm `single-turn-constraint` marker is still present (not removed by the extension).

**Expected outcome:**
> All five items (a–e) are present. Both markers present. No multi-step interview added.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 2 (live-agent): Entry A — story files present, no DoD

**Covers:** AC1

**Steps:**
1. Confirm this repo has story files in `artefacts/[feature]/stories/` (it does — every feature has them).
2. Confirm there is no DoD in `artefacts/[feature]/dod/` for the same feature — or use a feature that is pre-DoD (still in progress).
3. Open a Copilot chat session. Type: `start` (or "I just cloned this").
4. Observe the routing output.

**Expected outcome:**
> The response routes to `/tdd [story-slug]` where `story-slug` is the slug of a story in the detected `stories/` directory. A one-sentence explanation is included explaining why this route was chosen (e.g. "Found an active story — use `/tdd` to begin test-driven implementation.").

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 3 (live-agent): Entry B — `src/` has JS files, no `artefacts/[feature]/stories/` detected

**Covers:** AC2

**Steps:**
1. This scenario requires a test repo that has `src/*.js` files but no `artefacts/[feature]/stories/` directory. To simulate: either use a worktree branch where `artefacts/` is empty, or note the behaviour when `/start` is run in a repo that matches Entry B's pattern.
2. (If simulating is not feasible, note as "manual scenario pending — requires specific repo state" and confirm from the instruction text that the routing is defined.)
3. Run `/start` or `what do I do` in the matching context.

**Expected outcome:**
> The response routes to `/reverse-engineer` with a one-sentence explanation (e.g. "Detected existing code without pipeline artefacts — use `/reverse-engineer` to extract business rules before discovery.").

**Result:** [ ] Pass  [ ] Fail  [ ] Pending — requires specific repo state
**Notes:**

---

### Scenario 4 (live-agent): Entry C — DoD record present (takes priority over Entry A)

**Covers:** AC3, and priority C > A

**Steps:**
1. Identify a feature in this repo that has both a DoD record (`artefacts/[feature]/dod/*.md`) AND active story files (`artefacts/[feature]/stories/*.md`). If none exists, simulate by checking after any Phase 4 or Phase 5 feature with merged stories.
2. Run `/start` or "where am I" in a chat session.
3. Observe whether the routing picks DoD (Entry C) over stories (Entry A).

**Expected outcome:**
> The response routes to the retrospective story template (`.github/templates/retrospective-story.md`), NOT to `/tdd`. A one-sentence explanation is included (e.g. "Found completed DoD records — consider capturing retrospective stories for completed features."). This confirms Entry C takes priority over Entry A.

**Result:** [ ] Pass  [ ] Fail  [ ] Pending — requires specific repo state
**Notes:**

---

### Scenario 5: No brownfield signals → greenfield path (no false positive)

**Covers:** AC4

**Steps:**
1. To verify the no-signal fallback from the instruction text, open `.github/skills/start/SKILL.md` and find the fallback condition.
2. Alternatively, run `/start` in a fresh directory with no `artefacts/`, no `src/`, and no `workspace/state.json`. Observe the output.

**Expected outcome:**
> When no brownfield signals are present, the greenfield path from p11.6 is taken — one-sentence pipeline description, "no pipeline state found", run `/discovery` next. No false routing to `/tdd`, `/reverse-engineer`, or retrospective template.

**Result:** [ ] Pass  [ ] Fail
**Notes:**

---

### Scenario 6: Contract check and npm test pass

**Covers:** AC5

**Steps:**
1. In a terminal: `node .github/scripts/check-skill-contracts.js`
2. Confirm exit code 0 and no failure for `start` contract (both `single-turn-constraint` and `brownfield-routing` markers checked).
3. Run: `npm test`
4. Check for `check-p11-start.js` output — all `✓` including brownfield routing tests.

**Expected outcome:**
> Contract check exits 0. npm test exits 0 (pre-existing p3.3 failures acceptable). All `/start` skill tests pass including brownfield routing assertions.

**Result:** [ ] Pass  [ ] Fail
**Notes:**
