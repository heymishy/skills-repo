# Definition of Ready: Implement `init` and `fetch` commands in CLI adapter

**Story reference:** `artefacts/2026-04-24-platform-onboarding-distribution/stories/p11.5-cli-init-fetch.md`
**Test plan reference:** `artefacts/2026-04-24-platform-onboarding-distribution/test-plans/p11.5-test-plan.md`
**Contract reference:** `artefacts/2026-04-24-platform-onboarding-distribution/dor/p11.5-dor-contract.md`
**Assessed by:** GitHub Copilot / /definition-of-ready skill
**Date:** 2026-04-28

---

## Hard Blocks

| # | Check | Status | Notes |
|---|-------|--------|-------|
| H1 | User story is in As / Want / So format with a named persona | ✅ | "As a platform consumer, I want … So that…" |
| H2 | At least 3 ACs in Given / When / Then format | ✅ | 5 ACs, all Given/When/Then |
| H3 | Every AC has at least one test in the test plan | ✅ | 10 tests across 5 ACs |
| H4 | Out-of-scope section is populated — not blank or N/A | ✅ | 4 named exclusions including skill content validation (p11.4) |
| H5 | Benefit linkage field references a named metric | ✅ | M1 — Time-to-first-skill-run metric |
| H6 | Complexity is rated | ✅ | Complexity 2 |
| H7 | No unresolved HIGH findings from the review report | ✅ | 0 HIGH findings; 1 MEDIUM (p11.5-review-1.md) |
| H8 | Test plan has no uncovered ACs | ✅ | All 5 ACs covered; execSync mocking approach documented |
| H8-ext | Upstream dependency declared (p11.4) → schemaDepends declaration | ✅ | schemaDepends: [] — init() and fetch() add no new pipeline-state.json fields; delivery sequencing constraint noted |
| H9 | Architecture Constraints field populated; no Category E HIGH findings | ✅ | WS0.6 no-npm-deps, ADR-004 config read, no hardcoded URLs |
| H-E2E | No CSS-layout-dependent ACs | ✅ | CLI/file system operations only |
| H-NFR | NFR profile present | ✅ | nfr-profile.md EXISTS ✅; Performance: init() < 1s; Security: no credentials written |
| H-NFR2 | Compliance NFRs have human sign-off | ✅ | Security NFR is a platform constraint, not regulatory |
| H-NFR3 | Data classification in NFR profile not blank | ✅ | NFR profile: classification "Internal" |
| H-NFR-profile | nfr-profile.md exists for feature | ✅ | Confirmed present |

**Result: 15/15 hard blocks PASS — no blockers.**

---

## Warnings

| # | Check | Status | Risk if proceeding | Acknowledged by |
|---|-------|--------|--------------------|-----------------|
| W1 | NFRs identified or "None — confirmed" | ✅ | Performance and Security NFRs from nfr-profile.md | — |
| W2 | Scope stability declared | ✅ | Stable | — |
| W3 | MEDIUM findings acknowledged in /decisions | ⚠️ | 1-M1: AC4 names implementation mechanism (`child_process.execSync`). Naming the mechanism is acceptable here — fetch() is a git command invocation and the mechanism is the point. Test plan mocks execSync. Risk: if a future implementation uses a different mechanism, AC4 may need revision. Accepted for MVP. RISK-ACCEPT logged in decisions.md (2026-04-28). | /definition-of-ready skill |
| W4 | Verification script reviewed by domain expert | ✅ | Test plan covers the happy paths and key failure modes thoroughly. Accepted. | — |
| W5 | Delivery sequencing constraint | ⚠️ | Story Dependencies block states: p11.4 must be DoD-complete before coding agent is dispatched. This is a delivery sequencing constraint (not a DoR hard block) — the lockfile schema and path from p11.4 are consumed by init(). **Do not dispatch until p11.4 is DoD-complete.** RISK-ACCEPT logged in decisions.md (2026-04-28). | /definition-of-ready skill |

---

## NFR Compliance

| NFR | Requirement | Assessment |
|-----|-------------|------------|
| Performance | init() < 1s | init() creates a directory and writes a minimal stub JSON file — sub-millisecond. Passes by design. |
| Security | No credentials written by init() or fetch() | init() writes a stub lockfile (empty JSON object); fetch() runs a git clone/fetch command. Neither writes authentication tokens to disk. No hardcoded URLs. Remote URL read from context.yml only. NFR met by design. |

---

## Standards Injection

No `domain:` field on story — standards injection skipped.

---

## Oversight

**Level:** Medium.

---

## ⚠️ Dispatch Gate

**Do not dispatch this story to the coding agent until:** p11.4 is DoD-complete. The `init()` stub lockfile format and the lockfile path (`.github/skills/skill-lockfile.json`, per ADR-016) must be established by p11.4 before `init()` is implemented.

**Dispatch gate status:** 🔴 Not cleared — awaiting p11.4 DoD-complete.

---

## Coding Agent Instructions

```
## Coding Agent Instructions

Proceed: Yes — AFTER dispatch gate is cleared (p11.4 must be DoD-complete)
Story: Implement init and fetch commands in CLI adapter
Story artefact: artefacts/2026-04-24-platform-onboarding-distribution/stories/p11.5-cli-init-fetch.md
Test plan: artefacts/2026-04-24-platform-onboarding-distribution/test-plans/p11.5-test-plan.md
Verification script: artefacts/2026-04-24-platform-onboarding-distribution/verification-scripts/p11.5-verification.md

Goal:
Make every test in tests/check-p11-init-fetch.js pass.
Do not add scope, behaviour, or structure beyond what the tests and ACs specify.

Files to modify (p11.5 scope):
  src/enforcement/cli-adapter.js
    — Replace init() stub with real implementation:
        * Create .github/skills/ directory if it does not exist (fs.mkdirSync recursive)
        * Write a stub lockfile at .github/skills/skill-lockfile.json if no lockfile exists:
          { "version": 1, "generated": "<ISO date>", "skills": {} }
        * Return { created: true/false } indicating whether the directory was newly created
        * Must complete in < 1s (NFR: performance)
    — Replace fetch() stub with real implementation:
        * Read remote URL from .github/context.yml (context.yml must be present; no hardcoded URL)
        * If remote URL is null or blank: return { error: "no remote configured" } — do not throw
        * Execute: git clone <url> <tmpdir> using child_process.execSync (or equivalent)
        * Copy fetched SKILL.md files to .github/skills/<skillname>/SKILL.md
        * Return { fetched: [list of skill names copied] }
    — Do NOT modify pin(), verify(), or any other function in this file (p11.4 owns those)
  tests/check-p11-init-fetch.js
    — Create test file covering all 10 tests in the test plan
    — Uses Node.js built-ins only (fs, path, os, child_process)
    — Mock execSync where needed to avoid real network calls in tests
    — Custom assert(condition, label) pattern; process.exit(failed > 0 ? 1 : 0)

Constraints:
  — No new npm dependencies; use Node.js built-ins only
  — Remote URL MUST come from context.yml (ADR-004) — no hardcoded strings
  — No credentials written to disk at any point (NFR: security)
  — init() must complete in < 1s (NFR: performance)
  — Lockfile path: .github/skills/skill-lockfile.json (ADR-016)
  — Architecture standards: read .github/architecture-guardrails.md before implementing
  — Open a draft PR when tests pass — do not mark ready for review
  — If ambiguity not covered by ACs or tests: add a PR comment and do not mark ready for review

Oversight level: Medium
```

---

## Sign-off

**Oversight level:** Medium
**Signed off by:** /definition-of-ready skill (Copilot) — 2026-04-28
**Tech lead notified:** [ ] Pending operator confirmation
