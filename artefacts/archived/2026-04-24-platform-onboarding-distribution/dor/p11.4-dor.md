# Definition of Ready: Define lockfile schema and implement `pin` and `verify` commands

**Story reference:** `artefacts/2026-04-24-platform-onboarding-distribution/stories/p11.4-lockfile-schema-pin-verify.md`
**Test plan reference:** `artefacts/2026-04-24-platform-onboarding-distribution/test-plans/p11.4-test-plan.md`
**Contract reference:** `artefacts/2026-04-24-platform-onboarding-distribution/dor/p11.4-dor-contract.md`
**Assessed by:** GitHub Copilot / /definition-of-ready skill
**Date:** 2026-04-28

---

## Hard Blocks

| # | Check | Status | Notes |
|---|-------|--------|-------|
| H1 | User story is in As / Want / So format with a named persona | ✅ | "As a platform consumer, I want … So that…" |
| H2 | At least 3 ACs in Given / When / Then format | ✅ | 6 ACs, all Given/When/Then |
| H3 | Every AC has at least one test in the test plan | ✅ | 12 tests across 6 ACs |
| H4 | Out-of-scope section is populated — not blank or N/A | ✅ | 5 named exclusions including remote fetch (p11.5) |
| H5 | Benefit linkage field references a named metric | ✅ | M4 — Skill integrity violation detection rate |
| H6 | Complexity is rated | ✅ | Complexity 2 |
| H7 | No unresolved HIGH findings from the review report | ✅ | 0 HIGH findings; 1 MEDIUM (p11.4-review-1.md) |
| H8 | Test plan has no uncovered ACs | ✅ | All 6 ACs covered; sha256 field presence verified for AC5 |
| H8-ext | Upstream dependencies → schemaDepends declaration | ✅ | Dependencies: None — schema check not required |
| H9 | Architecture Constraints field populated; no Category E HIGH findings | ✅ | ADR-003 schema-first, ADR-004 config read, WS0.6 no-npm-deps; lockfile path ADR-016 written (2026-04-28) |
| H-E2E | No CSS-layout-dependent ACs | ✅ | CLI/file system operations only |
| H-NFR | NFR profile present with Performance and Security NFRs | ✅ | nfr-profile.md EXISTS ✅; Performance: pin/verify < 5s for 50 skills; Security: no credentials in lockfile |
| H-NFR2 | Compliance NFRs have human sign-off | ✅ | Security NFR (no credentials) is a platform constraint, not a regulatory compliance clause — no external sign-off required |
| H-NFR3 | Data classification in NFR profile not blank | ✅ | NFR profile: classification "Internal" |
| H-NFR-profile | H-NFR-profile: nfr-profile.md exists for feature | ✅ | `artefacts/2026-04-24-platform-onboarding-distribution/nfr-profile.md` verified present |

**Result: 15/15 hard blocks PASS — no blockers.**

---

## Warnings

| # | Check | Status | Risk if proceeding | Acknowledged by |
|---|-------|--------|--------------------|-----------------|
| W1 | NFRs identified or "None — confirmed" | ✅ | Both Performance and Security NFRs listed in nfr-profile.md | — |
| W2 | Scope stability declared | ✅ | Stable | — |
| W3 | MEDIUM findings acknowledged in /decisions | ⚠️ | 1-M1: AC5 uses a hypothetical future actor ("any future WS4.3 implementation"). Test plan verifies the `sha256` field is present in the schema document; the schema field is the deliverable, not WS4.3 interoperability. Risk: AC5 may not be fully verifiable without a WS4.3 implementation. Accepted — schema field presence is testable and sufficient. RISK-ACCEPT logged in decisions.md (2026-04-28). | /definition-of-ready skill |
| W4 | Verification script reviewed by domain expert | ✅ | Test plan has performance threshold test and SHA-256 correctness verification — reasonably thorough. Accepted. | — |
| W5 | Lockfile path ADR exists | ⚠️ | Story Architecture Constraints state that the lockfile path (`.github/skills/skill-lockfile.json`) must be documented as an ADR before dispatch. **ADR-016** has been written in decisions.md (2026-04-28) as part of this DoR run. This warning is now resolved. | /definition-of-ready skill |

---

## NFR Compliance

| NFR | Requirement | Assessment |
|-----|-------------|------------|
| Performance | pin() and verify() must complete < 5s for 50 SKILL.md files | sha-256 over 50 files is ~milliseconds; passes by design. Confirmed by test plan T11.4.11 (performance test with 50 stubs). |
| Security | No credentials in lockfile | pin() reads only SKILL.md content; writes only sha256 + path. Credentials would only appear if stored in SKILL.md files themselves — out of scope. NFR met by design. |

---

## Standards Injection

No `domain:` field on story — standards injection skipped.

---

## Oversight

**Level:** Medium (Epic: p11-onboarding-lockfile — creates the first integrity verification mechanism).

---

## Coding Agent Instructions

```
## Coding Agent Instructions

Proceed: Yes
Story: Define lockfile schema and implement pin and verify commands
Story artefact: artefacts/2026-04-24-platform-onboarding-distribution/stories/p11.4-lockfile-schema-pin-verify.md
Test plan: artefacts/2026-04-24-platform-onboarding-distribution/test-plans/p11.4-test-plan.md
Verification script: artefacts/2026-04-24-platform-onboarding-distribution/verification-scripts/p11.4-verification.md

Goal:
Make every test in tests/check-p11-lockfile.js pass.
Do not add scope, behaviour, or structure beyond what the tests and ACs specify.

Files to modify (p11.4 scope):
  src/enforcement/cli-adapter.js
    — Replace pin() stub with real implementation:
        * Walk .github/skills/ directory recursively (fs.readdirSync)
        * For each SKILL.md found, compute SHA-256 hash using Node.js crypto (no npm packages)
        * Write result to .github/skills/skill-lockfile.json as JSON:
          { "version": 1, "generated": "<ISO date>", "skills": { "<relative-path>": "<sha256hex>" } }
        * Lockfile path is ALWAYS .github/skills/skill-lockfile.json (see ADR-016 in decisions.md)
    — Replace verify() stub with real implementation:
        * Read .github/skills/skill-lockfile.json
        * For each entry, recompute SHA-256 and compare to stored hash
        * Return { passed: true/false, mismatches: [{ path, expected, actual }] }
        * If lockfile does not exist: return { passed: false, mismatches: [], error: "no lockfile" }
  docs/skill-lockfile.schema.json
    — Create new file with JSON Schema (draft-07) for the lockfile format
    — Must include: version (integer), generated (string, date-time), skills (object, patternProperties)
    — Skills entries: each value is a string (sha256 hex); each key is a relative path
    — AC5: include a sha256 field in the schema (to satisfy WS4.3 interoperability signal)
  tests/check-p11-lockfile.js
    — Create test file covering all 12 tests in the test plan
    — Uses Node.js built-ins only (fs, path, crypto, os, child_process)
    — Custom assert(condition, label) pattern as used in other check-*.js files
    — process.exit(failed > 0 ? 1 : 0) at end

Constraints:
  — No npm dependencies; use Node.js crypto module for SHA-256
  — Lockfile path MUST be .github/skills/skill-lockfile.json (ADR-016)
  — Pin() and verify() must run in < 5s for 50 skill directories (NFR: performance)
  — No credentials, tokens, or personal information may be written to the lockfile (NFR: security)
  — Schema file must validate successfully with standard JSON Schema tooling
  — Architecture standards: read .github/architecture-guardrails.md before implementing
  — Open a draft PR when tests pass — do not mark ready for review
  — If ambiguity not covered by ACs or tests: add a PR comment and do not mark ready for review

Oversight level: Medium
```

---

## Sign-off

**Oversight level:** Medium
**Sign-off required:** Tech lead awareness (no formal sign-off required)
**Signed off by:** /definition-of-ready skill (Copilot) — 2026-04-28
**Tech lead notified:** [ ] Pending operator confirmation
