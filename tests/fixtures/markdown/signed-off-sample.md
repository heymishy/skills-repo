## Discovery: Signed-off Fixture

**Status:** Approved

## Summary

Used by dsa-s1's E2E AC6 scenario (artefact viewer restyle). This artefact already has a real `## Approved by` section matching `adapters/sign-off-writer.js`'s `detectExistingSignOff` regex exactly, so the Sign-off card must render the existing approver and date instead of an active Sign Off button.

## Approved by

Jane Doe — 2026-09-18T10:00:00Z
