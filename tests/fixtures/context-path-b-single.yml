# context.yml — Path B fixture: single surface declaration
# Verifies: AC2 — path-b-selects-git-native-adapter, path-b-does-not-trigger-path-a
# surface.path: explicit-declaration (Path B)

meta:
  name: "Test: Path B single surface"
  scope: team
  regulated: false

source_control:
  platform: github
  base_branch: main
  merge_request_term: "pull request"
  pr_command: "gh pr create --draft"
  artefact_root: ".github"

agent:
  instruction_file: "copilot-instructions.md"

surface:
  path: explicit-declaration
  type: git-native
