# context.yml — Both Path A and Path B declared (AC5 guard scenario)
# Verifies: AC5 — both paths present; Path B (explicit) wins; warning logged; no HTTP call
# surface: block has path: explicit-declaration → AC5 guard fires

registry_source: ea-registry
registry_url: "https://stub.ea-registry.test/apps"
app_id: "app-saas-gui-001"

surface:
  path: explicit-declaration
  type: saas-gui
