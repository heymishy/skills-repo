# context.yml — Path A fixture: EA registry lookup, saas hosting
# Verifies: AC1 — HTTP GET with app_id, AC2 — saas maps to saas-api
# No surface: block — Path A only; falls back to error if registry unavailable

registry_source: ea-registry
registry_url: "https://stub.ea-registry.test/apps"
app_id: "app-saas-001"
