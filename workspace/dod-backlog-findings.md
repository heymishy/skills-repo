# DoD backlog pass — findings tracker

Single consolidated list of every real finding (not pre-existing/already-known test failures) discovered during the 2026-08-17 retroactive DoD backlog pass. Each finding gets a follow-up story artefact; this file tracks status until the follow-up story reaches DoD, at which point its row moves to Resolved with the closing PR.

Update this file whenever a new finding surfaces during the backlog pass, and whenever a tracked follow-up story's stage changes.

---

## Open

| ID | Finding | Follow-up story | Stage | Owner / next action |
|----|---------|-----------------|-------|---------------------|
| F1 | Impersonation "Viewing as" banner does not render on `/dashboard` — `handleGetDashboard` never threads `req.session.impersonation` into its `renderShell()` call, unlike `settings.js`/older `dashboard.js`. Live-confirmed via Chrome (started real impersonation session, banner present on `/settings`, absent on `/dashboard`, reproduced twice). | `ibg-s1` — `artefacts/2026-08-17-impersonation-banner-dashboard-gap/` | Story written, not yet at `/test-plan` | operator/next session — run `/test-plan` for `ibg-s1` next |
| F2 | `b2`'s own AC1 requires a broken Scenario B to block merge, but the live master branch ruleset (`gh api repos/heymishy/skills-repo/rulesets/14979696`) does not list "Scenario B E2E (staging)" in `required_status_checks`, even though the job runs on every PR. `b2`'s own test (`check-b2-ci-gate-config.js`) already flags this as SKIPPED/inconclusive rather than failing, since it depends on live GitHub state. | `sbrc-s1` — `artefacts/2026-08-17-scenario-b-not-required-check/` | Story written, not yet at `/test-plan` | operator/next session — run `/test-plan` for `sbrc-s1` next |
| F3 | `r-canvas-render-and-story-extraction-fix`'s own AC3 self-documented two gaps at merge (2026-07-26), never followed up: (a) `extractStoryIdsFromDefinitionArtefact()` was only manually verified, never captured as an automated regression test; (b) an unexplained 400 on `POST /api/journey/:id/gate-confirm` immediately after a real `/definition` turn was observed but never root-caused. | `csgc-s1` — `artefacts/2026-08-17-canvas-story-extraction-gate-confirm-gap/` | Story written, not yet at `/test-plan` | operator/next session — run `/test-plan` for `csgc-s1` next |
| F4 | **Security-relevant.** `sec-perf-s3` (CSRF hardening, merged 2026-07-01) protected only 4 highest-value routes and explicitly deferred the rest ("Deferred to a follow-up story to keep this one bounded") — that follow-up was never created. Real, currently-unprotected server-rendered POST forms remain: `/journey/wizard`, `/api/journey` + sibling journey-flow forms, `/api/artefacts/:slug/:file/annotations`, `/api/skills/:name/sessions` (form path), `/api/skills/:name/sessions/:id/commit` (form path), `/products/confirm`, `/products/:id/features`, plus the legacy `renderLoginPage()` fallback shell. | `rcfc-s1` — `artefacts/2026-08-17-remaining-csrf-form-coverage/` | Story written, not yet at `/test-plan` | operator/next session — run `/test-plan` for `rcfc-s1` next; consider prioritizing given security relevance |
| F5 | `kfd1` (kanban card + detail page redesign, merged 2026-06-17) had its AC3/AC4 CSS-layout-dependent qualitative requirements accepted at DoR via a manual-verification plan (Scenarios 5-7 in its own verification-script) that was never actually executed — every result checkbox still blank, ~2 months later. No automated visual-regression test exists either, and no `decisions.md` RISK-ACCEPT was logged — violates CLAUDE.md's own B2 rule (every CSS-layout-dependent AC needs one or the other). Not believed to be an active defect (feature has been live ~2 months, no reported visual issues) — a process-verification gap, not a known bug. | `kvvg-s1` — `artefacts/2026-08-17-kfd1-visual-verification-gap/` | Story written, not yet at `/test-plan` | operator/next session — run `/test-plan` for `kvvg-s1` next |
| F6 | **Higher severity.** `bri-s3.3`'s (merged 2026-07-09, PR #483) Playwright spec is the story's own stated "primary regression guard against a role-boundary regression," but AC1's test never touches an admin-gated route (only checks both users can use shared product routes — no denial assertion), and AC3's test body is a literal unimplemented placeholder. `pipeline-state.json` recorded 6/6 tests passing and 4/4 ACs verified the whole time — a false sense of security-test coverage, not an active exploit (AC2/AC4 genuinely cover real-tenant isolation). | `rbg-s1` — `artefacts/2026-08-18-bri-s3-3-role-boundary-guard-gap/` | Story written, not yet at `/test-plan` | operator/next session — run `/test-plan` for `rbg-s1` next; higher priority than F5/F7 given it's a security-test-integrity gap |
| F7 | Two small, unrelated, already-diagnosed verification gaps bundled into one story: (a) `rpc-s1` (merged, PR #508+#510) promised a dedicated automated NFR-Accessibility test in its test plan but shipped none — markup itself manually confirmed compliant during the DoD pass, so no known defect. (b) `jrf-s1` (merged)'s AC4 regression test only calls the fixed handler twice in isolation, not the full documented 67/345-baseline suite run its own DoR contract required. | `vcb-s1` — `artefacts/2026-08-18-verification-coverage-backfill/` | Story written, not yet at `/test-plan` | operator/next session — run `/test-plan` for `vcb-s1` next |
| F8 | Two related staging-verification gaps from the same 2026-07-23 ideate-canvas-fix cluster, bundled into one story: (a) `icrh-s1`'s AC6 real-staging E2E verification shows as **skipped** (not passed) in CI, blocked by a precondition gate; a promised manual re-run was never posted. (b) `icv-s1`'s AC6 real-staging re-verification was explicitly left "pending" in `decisions.md` and never closed (`acVerified: 5` of `6`). Both stories' underlying fixes are confirmed correct via unit/behavioural tests — this is unclosed staging confirmation, not a known defect. | `csvg-s1` — `artefacts/2026-08-18-canvas-fix-staging-verification-gap/` | Story written, not yet at `/test-plan` | operator/next session — run `/test-plan` for `csvg-s1` next |
| F9 | **Higher severity, live production bug.** `cdpl-s1`'s (merged 2026-08-07) new "Maximise diagrams"/"Maximise canvas" buttons throw `ReferenceError: swToggleCanvasFs is not defined` on every read-only/historical resumed conversation — live-confirmed via browser console, twice. Root cause: `chat-view.js:156` suppresses the entire script block containing the toggle functions whenever `data.readOnly` is true, but the button markup is rendered unconditionally. This is a **pre-existing** defect (the older `swToggleArtefactFs` pattern cdpl-s1 was told to reuse has the identical bug, independently confirmed on the same page) — cdpl-s1 inherited it, didn't introduce it. Bundled with a minor, unrelated cosmetic bug also found live: `bmau-s1`'s "Assign to module" button label gets permanently stuck on "Assigning…" after the first successful use in a session (functionally harmless — repeat assigns still work correctly — just a missing state reset on the success path). | `cmba-s1` — `artefacts/2026-08-21-canvas-maximise-and-bulk-assign-button-fixes/` | Story written, not yet at `/test-plan` | operator/next session — run `/test-plan` for `cmba-s1` next; the AC1/AC2 maximise-button fix is the higher-priority half |
| F10 | UX complaint, live-reported by the operator on production (2026-08-21): `tpac-s1`'s Plan/Status control on `/admin/credits` packs two 3-option radio-button groups plus a submit button into one table cell per tenant row — cramped and hard to scan, worse as more tenants are added. Not a functional bug (the underlying `setPlanState`/`POST /api/admin/plan/set` mechanism works correctly, confirmed live this same session) — a presentation-only fix. | `apsc-s1` — `artefacts/2026-08-21-admin-credits-plan-status-ux/` | Story written, not yet at `/test-plan` | operator/next session — run `/test-plan` for `apsc-s1` next; low priority, presentation-only |
| F11 | **Highest severity found this session — live authorization gap, not a test-coverage issue.** Found while writing `rbg-s1`'s test plan: `bri-s3.3`'s own original AC3 ("viewer attempts any write action, denied") describes behaviour that was never implemented anywhere. `'viewer'` exists only as a stored value in `team-management.js`'s `VALID_ROLES` — confirmed via full codebase search that no middleware or route anywhere checks for or blocks it. The only real role gate anywhere is `requireAdmin` (admin vs. everyone else). A person assigned "viewer" (implying read-only) currently has full write access, identical to `engineer`/`product` roles. Unknown whether any real production tenant currently has a viewer-role person assigned (not yet checked). Routed through `/discovery` rather than a short-track story, since "which write actions specifically" is a real product decision, not a bounded bug fix. | `2026-08-21-viewer-role-no-enforcement/discovery.md` | Discovery drafted, awaiting human approval (not yet at `/benefit-metric`) | operator — review and approve the discovery, or reject/redirect; **recommend checking whether any real tenant currently has a viewer-role person assigned, to gauge urgency** |
| F12 | **Highest severity found this session — live, unconfirmed-exploitability privilege-escalation bug (not test-coverage-only).** Found while implementing `rbg-s1`'s own AC1 test for the first time against a real admin-gated route. `requireAdmin`'s live per-request role re-check (`sec-perf-s2`) calls `_getCurrentRole(tenantId)` with no per-person identity key; `server.js`'s wiring forwards none either. For a `TENANT_ORG_ALLOWLIST` shared-org tenant (2+ people, same `tenant_id`), the under-specified lookup falls back to an arbitrary `LIMIT`-less row for the tenant, resolving EVERY person's live re-check to whichever teammate's role was inserted first — live-confirmed: bob (engineer) got `200` (treated as admin) on `/admin/credits` purely because alice (admin) shares his tenant. Same bug class `tir-s9` already fixed for the login-time path; never applied to this separate live-recheck call site added later by `sec-perf-s2`. This wiring is unconditional in production (`if (DATABASE_URL)` branch), not test-only. Root cause and fix fully identified (thread `req.session.login` through, mirroring `tir-s9`'s existing pattern) — routed as a short-track story since it's a bounded, well-understood fix, not a design decision. **Not yet confirmed whether any real production tenant currently has 2+ people with different roles sharing a `TENANT_ORG_ALLOWLIST` tenant** (i.e. whether this is actively exploitable today vs. latent). `rbg-s1` itself is blocked on this shipping. | `lrtc-s1` — `artefacts/2026-08-21-live-role-recheck-tenant-collapse/` | Story written, not yet at `/test-plan` | operator — **recommend prioritizing above the remaining F1-F10 backlog given security severity and that it's actively blocking `rbg-s1`**; also recommend checking `team_memberships` for any tenant with 2+ distinct `person_id`s to gauge real exploitability |

---

## Resolved

| ID | Finding | Resolution | Closed |
|----|---------|-----------|--------|
| F13 | **CI-gate reliability finding, not a security/functional issue.** `lrtc-s1`'s PR #750 failed `/trace`'s `test_plan_coverage` check with 21 MISSING entries, unrelated to that PR's own scope. Split into two causes: (1) 10 false positives — `pipeline-state.json` never recorded `testPlan.artefact` for `fps-s1`, `spv-s1`, `dss-s1`, `dsn-s1`, `ftcg-s1`, `acv-s1`, `pss-s1`, `rlcc-s1`, `nis-s1`, `bjs-s1`. (2) 11 genuine gaps — already-shipped stories (`alrf-s1/s2/s3/s4/s5/s6/s8/s9/s10/s11`, `r-canvas-render-and-story-extraction-fix`) with no test-plan artefact under any naming convention. | Both causes fixed: (1) `testPlan.artefact` populated directly, commit `3348c3d2`. (2) `tpbg-s1` backfilled all 11 test-plan.md files reconstructed from each story's already-documented AC-to-test mapping, commit `2e8b211c`. `validate-trace.sh --ci` confirmed locally: 6 passed, 0 warnings, 0 failed (was 1 failed/21 missing). One genuine underlying gap surfaced during backfill (`r-canvas-render-and-story-extraction-fix` AC3, no automated test for `extractStoryIdsFromDefinitionArtefact`) — not fabricated as covered, correctly left routed to its own existing tracking, F3/`csgc-s1`. | `tpbg-s1` DoD complete, 2026-08-21 (`artefacts/2026-08-21-test-plan-backfill-gap/dod/tpbg-s1-dod.md`) |

---

## Backlog pass status

**Complete as of 2026-08-21.** All 55 stories originally identified in the 2026-08-16 backlog scan now have DoD artefacts (53 via automated re-verification, 2 — `cdpl-s1`/`bmau-s1` — via live Chrome verification per the depth policy below). 9 real findings (F1–F9) surfaced along the way, each closed out into a tracked, not-yet-started follow-up story. No further stories remain in the original scan; `node scripts/check-pipeline-state-integrity.js` shows only the same 3 pre-existing, already-accepted C3 partial-pass failures that existed before this pass began.

---

## Process notes

- **Depth policy for this backlog pass** (operator-set 2026-08-16/17): lightweight-by-default — confirm PR merged, re-run tests fresh, cite production longevity. Live Chrome verification reserved for stories flagged `hasLayoutDependentGaps`. Ordering: largest story-count clusters first.
- **Decision pattern on every real finding so far:** operator has consistently chosen "create a follow-up story" over "fix it directly in the DoD pass" — this keeps the DoD write-up itself honest (documents the gap, doesn't silently patch around it) and lets each fix go through its own short-track pipeline (`/test-plan` → `/definition-of-ready` → dispatch) rather than being rushed inside a bookkeeping session.
- **Distinct from `tests/known-baseline-failures.json`:** that file tracks pre-existing, already-accepted test failures encountered repeatedly across this backlog pass (not new findings, no action needed). This file tracks the opposite — genuinely new, real gaps that need resolution. Check a discovery against `known-baseline-failures.json` first; only add it here if it's not already known/accepted there.
- **Distinct from the original 161-story DoD-backlog note** in `workspace/state.json` `pendingActions` (logged 2026-08-16) — that entry tracks the overall *volume* of stories missing DoD write-ups; this file tracks the *substantive findings* uncovered while writing those DoDs, which is a much smaller, higher-signal list.
- When a follow-up story here reaches DoD, move its row to Resolved with: closing PR link, merge date, and one line on the actual fix. Do not delete the row — this file is the durable record of what the backlog pass surfaced, not just a todo list.
