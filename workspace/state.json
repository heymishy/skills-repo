{
  "currentPhase": "definition-of-done",
  "lastUpdated": "2026-09-12T00:00:00.000Z",
  "activeFeature": "2026-08-31-web-ui-navigation-legibility",
  "previousFeature": "2026-09-08-session-origin-badge",
  "cycle": {
    "discovery": {
      "status": "approved",
      "artefact": "artefacts/2026-08-31-web-ui-navigation-legibility/discovery.md",
      "completedAt": "2026-08-31T09:00:00.000Z",
      "note": "Clarified via /clarify (3 assumptions resolved: reuse existing UI patterns, sticky element not nav redesign, disk-canonical artefact reads). 3 architectural decisions logged in decisions.md. Approved by Hamish King, Platform Owner. UNTOUCHED since 2026-08-31 -- this session worked an entirely separate feature (session-origin-badge, now fully closed, see checkpoint below). Still ready for /benefit-metric whenever picked back up."
    },
    "benefitMetric": {
      "status": "not-started"
    }
  },
  "checkpoint": {
    "writtenAt": "2026-09-12",
    "contextAtWrite": "This session ran a full DoD-triage sweep, prompted by the operator asking 'is everything DoD Chrome-verified?' and then 'is there more to validate, all dod done?'. Closed the dsh-s3 session_turns root-cause gap as a new short-track story ntpg-s1 (PR #865, merged, DoD complete) -- the non-streaming JSON API turn handler was silently never persisting session_turns, disabling dsh-s3's chat-split view and drh-s1's diagram rendering for any journey advanced that way. Then worked the full 7-item ranked web-UI-cohesion list (cmba-s1/cdpl-s1, lsbm-s1, bee.3, tmc-s1, mtrr-s2, bmau-s1, ep1-s4) with real live-Chrome verification once GitHub OAuth staging auth was restored (existing GitHub session auto-authorized, no password ever entered by any agent). Then ran a full pipeline-state.json audit: found and fixed 0 missing-DoD cases (100% coverage confirmed) but found 66 dodStatus:complete stories sitting at amber/red health, most with a real, honest, self-documented reason in their own blocker text. Closed 22 of the 66 with real evidence this session (stale stage/health bookkeeping matching the same gate-advance field-omission gotcha found on ntpg-s1 itself; blockers whose named dependency/follow-up story had since shipped; RISK-ACCEPT/accessibility gaps closed via live DOM checks or direct source reads). 44 remain genuinely open for real reasons this agent should not act on unilaterally -- see pendingActions below for the full categorized breakdown. Separately, investigating the 'production not yet promoted' bucket surfaced a materially bigger finding: 31 separate pending promote-to-prod GitHub Actions approvals are queued back to 2026-09-03 (not just the 6 named in DoD text), and because each promote-to-prod job does a plain checkout pinned to its own commit SHA on a linear master history, approving only the single newest pending run deploys everything accumulated since 9/3 in one shot (verified via git merge-base --is-ancestor). Also found two GitHub Actions workflows (Deploy dashboards to GitHub Pages; Improvement Agent -- Scheduled Dreaming) failing on every single push this entire session -- pre-existing, unrelated to this sweep, not caused by any commit made today.",
    "resumeInstruction": "The DoD-triage sweep is done for real, close-able items -- do not re-run the same audit from scratch next session, read the pendingActions entries below first (dated 2026-09-12) for the current, accurate remaining-work picture. The single highest-leverage next action is the production-deploy decision (pendingActions entry 1) -- everything else in this backlog is smaller and lower-urgency. Before assuming any of the 44 remaining amber items are still open, check this session's own git log (commits c742b95c through 6d8896c0 plus the ones before) and the item's own current pipeline-state.json blocker text, since bookkeeping can drift again the same way it already did once.",
    "pendingActions": [
      "session-origin-badge feature: fully closed, no action needed. If the operator wants an /improve pass, two real findings from this session are worth scoping: (1) bin/skills advance's dot-notation writer has no awareness that `tasks` is an array indexed by id -- silently corrupts it via bogus top-level story.task-N keys, and check-pipeline-state-integrity.js doesn't catch the shape (SendFeedback drafted, not sent -- send it or fix the tool directly). (2) A PR can sit with zero CI dispatch when it has a merge conflict with its target branch, easy to misread as 'CI just hasn't started' -- worth adding an explicit gh pr view --json mergeable,mergeStateStatus check to /branch-complete/verify-completion's own routine, not just gh pr checks.",
      "NOT YET SCOPED: promote-to-prod's own missing version-stamp step. write-version-file.js and write-learnings-count-file.js both only run in staging-deploy.yml's deploy-staging job, never in promote-to-prod's own job -- so GET /version on production always returns the DEV_FALLBACK ({sha:null, shortSha:'dev', prNumber:null, commitSubject:null, deployedAt:null}) regardless of what's actually deployed. Found during the dcfx-s1 investigation (2026-09-05), explicitly deferred as a distinct, lower-severity root cause (a missing CI step, not a missing Dockerfile COPY). Fix direction: add the same two write-*-file.js steps to promote-to-prod's own job before its flyctl deploy step, mirroring deploy-staging's own pattern.",
      "STILL OPEN, watching for recurrence: the npm ci timing mystery (ncdv-s1). deploy-staging and post-deploy-e2e-confirm's own 'Install dependencies' steps have been observed taking 50s to 9m41s across different real runs, with an identical reported npm-cache hit every time -- genuinely intermittent, not fixed by cpco-s1's own PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD fix (confirmed: zero browser-download activity in every log checked). bcrypt (the only compiled/native dependency, node-gyp-build postinstall) is the leading but UNCONFIRMED hypothesis -- it ships a matching prebuilt Linux binary locally, which should resolve near-instantly on a standard glibc ubuntu-latest runner, in tension with a 'falls back to compiling from source' theory. ncdv-s1 added --foreground-scripts to the two affected npm ci steps (deploy-staging, post-deploy-e2e-confirm only) so the next real occurrence streams lifecycle-script output instead of staying silent. No fix attempted yet by design -- read the log the next time the slow pattern recurs before proposing one.",
      "Carried forward unchanged from the 2026-08-31 checkpoint -- see that entry's own detail below and workspace/capture-log.md's 2026-08-31 entries for full evidence: (1) listArtefacts local-vs-Postgres precedence bug, well-scoped, not yet fixed. (2) computeStep1Summary test-plan/DoR scanner per-story-subdirectory-vs-flat-file convention mismatch, needs an architectural decision first. (3) Fly.io auto_stop_machines='suspend' silent mid-request machine suspension, needs an operator infra/cost decision. NOTE: today's session (2026-09-04/05) did not re-investigate any of these three; do not assume resolved."
    ]
  },
  "pendingActions": [
    {
      "date": "2026-08-16",
      "action": "RESOLVED 2026-08-21. The 161-story DoD backlog identified here was fully processed across sessions 2026-08-17 through 2026-08-21: all stories from the 2026-08-16 scan now have DoD artefacts (53 via automated retroactive review, 2 -- cdpl-s1/bmau-s1 -- via live Chrome verification against real staging). check-pipeline-state-integrity.js shows only the same 3 pre-existing C3 partial-pass failures that predate this backlog pass -- no regressions introduced. 9 real findings (F1-F9) surfaced during the pass, each closed into its own tracked follow-up story (ibg-s1, sbrc-s1, csgc-s1, rcfc-s1, kvvg-s1, rbg-s1, vcb-s1, csvg-s1, cmba-s1) -- see workspace/dod-backlog-findings.md for the full consolidated list and status. None of F1-F9 have started /test-plan yet -- that is the actual remaining work, tracked as its own pendingActions entries below, not a continuation of this backlog note.",
      "owner": "closed -- superseded by workspace/dod-backlog-findings.md"
    },
    {
      "date": "2026-08-17",
      "action": "si-s3 (2026-08-17-settings-improvements, DoR signed off): AC1 and AC2 now CONFIRMED live on wuce-staging.fly.dev (2026-08-17) via Chrome, signed in as heymishy (GitHub OAuth, no credentials entered). AC1: bpe-s1/bse-s1 regression suites re-run clean (9/9, 7/7), no regression from si-s1/si-s2. AC2: clicking 'Manage billing' on heymishy's account (no Stripe customer ID) redirects to the Billing tab and shows the exact expected red banner 'You don't have a billing account set up yet.' -- scoped correctly to Billing, not Profile. Bonus confirmation: si-s1's relocated theme toggle and si-s2's locale-preference form are both visibly live on the Profile tab. AC3 (positive path -- an account WITH a real Stripe customer ID reaching the actual portal) is the only remaining gap: it requires either logging into the separate test@test.com account (needs a password, which is out of scope for the agent to enter) or upgrading heymishy's own account through the Stripe test-sandbox checkout (needs a card number, also out of scope for the agent to enter even though it's a test/sandbox card). Operator explicitly chose to leave AC3 for their own manual review rather than doing either handoff right now. Once done: click 'Manage billing' on an account with a real Stripe customer ID and confirm it reaches the actual Stripe-hosted portal, then record the full AC1-AC4 outcome in si-s3's DoD.",
      "owner": "operator (Hamish King) -- AC3 only, whenever convenient"
    },
    {
      "date": "2026-08-17",
      "action": "New short-track story ibg-s1 (2026-08-17-impersonation-banner-dashboard-gap) created: a real, live-confirmed AC1 gap found during the DoD backlog pass on d2 (2026-07-21-web-ui-experience-redesign) -- the persistent impersonation 'Viewing as' banner does not render on /dashboard (the Products landing page, likely the most-visited page in the app), because handleGetDashboard in src/web-ui/routes/products.js never threads req.session.impersonation into its renderShell() call, unlike dashboard.js's older handleDashboard (which does) and settings.js. Confirmed live via Chrome (started a real impersonation session as tenant-demo-1, banner present on /settings, absent on /dashboard, reproduced twice). Root-caused precisely; story artefact written with a bounded, single-file fix. Needs /test-plan -> /definition-of-ready -> dispatch to a coding agent -- not yet started.",
      "owner": "operator/next session -- run /test-plan for ibg-s1 next"
    },
    {
      "date": "2026-08-17",
      "action": "New short-track story sbrc-s1 (2026-08-17-scenario-b-not-required-check) created: a real gap found during the DoD backlog pass on b2 (2026-07-23-e2e-core-journey-coverage) -- b2's own AC1 requires 'a PR that breaks Scenario B... the PR's merge is blocked', but the live master branch ruleset (gh api repos/heymishy/skills-repo/rulesets/14979696) only lists 'Run assurance gate' and 'Scenario A E2E (staging)' in required_status_checks -- 'Scenario B E2E (staging)' is NOT required, even though the job runs on every PR. b2's own test file (check-b2-ci-gate-config.js) already flags this as SKIPPED/inconclusive rather than a hard failure, since it depends on live GitHub ruleset state the test can't assert confidently offline -- the live gh api check confirms it's a real, current gap. Story artefact written: adds one context entry to the existing ruleset (same pattern already used for Scenario A by a5). Needs /test-plan -> /definition-of-ready -> dispatch -- not yet started.",
      "owner": "operator/next session -- run /test-plan for sbrc-s1 next"
    },
    {
      "date": "2026-08-17",
      "action": "New short-track story csgc-s1 (2026-08-17-canvas-story-extraction-gate-confirm-gap) created: closes a gap self-documented (not newly discovered) by r-canvas-render-and-story-extraction-fix's own AC3 at merge time (2026-07-26, PR #613) -- extractStoryIdsFromDefinitionArtefact() was manually verified correct against the corrected fixture (finds [\"mock-fixture.1\"]) but this was never captured as an automated regression test, and a 400 on POST /api/journey/:id/gate-confirm observed immediately after a real /definition turn was left unconfirmed either way. Story artefact written with AC1 (regression test for the extraction function), AC2 (regression coverage for the downstream auto-skip-to-review logic that consumes it), and AC3 (root-cause or explicitly rule out the 400). Needs /test-plan -> /definition-of-ready -> dispatch -- not yet started.",
      "owner": "operator/next session -- run /test-plan for csgc-s1 next"
    },
    {
      "date": "2026-08-17",
      "action": "New short-track story rcfc-s1 (2026-08-17-remaining-csrf-form-coverage) created -- SECURITY-RELEVANT: found during the DoD backlog pass on sec-perf-s3 (2026-07-01-security-perf-hardening, PR #476, CSRF hardening). sec-perf-s3 protected only 4 highest-value routes (admin credits, team members, billing checkout, auth email) and explicitly deferred the rest 'to a follow-up story to keep this one bounded' -- that follow-up was never created, over 6 weeks later. Real, currently-unprotected server-rendered POST forms remain: /journey/wizard, /api/journey + sibling journey-flow forms, /api/artefacts/:slug/:file/annotations, /api/skills/:name/sessions (form path), /api/skills/:name/sessions/:id/commit (form path), /products/confirm, /products/:id/features, plus the legacy renderLoginPage() fallback shell. Mitigating factor: SameSite=Strict cookie policy already provides independent, real CSRF mitigation for all of these (same defence-in-depth framing sec-perf-s3 itself used) -- this is a hardening gap, not an actively-exploitable-in-all-browsers vulnerability. Story artefact written reusing sec-perf-s3's existing csrf.js mechanism exactly, AC1-AC5 covering all named routes plus the legacy fallback shell. Needs /test-plan -> /definition-of-ready -> dispatch -- not yet started. Logged in workspace/dod-backlog-findings.md as F4. NOTE (2026-08-31): this session found and fixed rssp-s1, sstr-s1, daep-s1, ssdo-s1 -- none overlap with rcfc-s1's scope, but confirms this repo's CSRF-field-gap pattern (D40/wsm precedent) is still a live, recurring class of bug worth checking against.",
      "owner": "operator/next session -- run /test-plan for rcfc-s1 next; consider prioritizing given security relevance"
    },
    {
      "date": "2026-08-17",
      "action": "New short-track story kvvg-s1 (2026-08-17-kfd1-visual-verification-gap) created: found during the DoD backlog pass on kfd1 (2026-06-17-kanban-feature-detail-cx, PR #388). kfd1's AC3/AC4 CSS-layout-dependent qualitative requirements (kanban detail pages 'reading as' the rest of the platform, markdown legibility) were accepted at DoR via a manual-verification plan (Scenarios 5-7 in its own verification-script) that was never actually executed -- every result checkbox still blank ~2 months later. No automated visual-regression test exists either, and no decisions.md RISK-ACCEPT was logged -- violates CLAUDE.md's own B2 rule (every CSS-layout-dependent AC needs one or the other, logged at DoR time). Not believed to be an active defect -- feature has been live ~2 months with no reported visual issues -- this is a process-verification gap, not a known bug. Story artefact written: AC1 execute the manual scenarios OR write an automated Playwright visual-regression spec, AC2 any newly-found defect gets its own separate triage, AC3 if manual path chosen, log the RISK-ACCEPT properly. Needs /test-plan -> /definition-of-ready -> dispatch -- not yet started. Logged in workspace/dod-backlog-findings.md as F5.",
      "owner": "operator/next session -- run /test-plan for kvvg-s1 next"
    },
    {
      "date": "2026-08-18",
      "action": "New short-track story rbg-s1 (2026-08-18-bri-s3-3-role-boundary-guard-gap) created -- HIGHER SEVERITY: found during the DoD backlog pass on bri-s3.3 (2026-07-09-beta-readiness-infra, PR #483). bri-s3.3's own story text calls its Playwright spec 'the primary regression guard against a role-boundary regression', but AC1's test never touches an admin-gated route (only checks both users can use shared product routes -- no denial assertion), and AC3's test body is a literal unimplemented placeholder. pipeline-state.json recorded 6/6 tests passing and 4/4 ACs verified the whole time -- a false sense of security-test coverage, not a confirmed active exploit (AC2/AC4 genuinely cover real-tenant isolation). Story artefact written: AC1 rewrite the test to assert real denial on an admin-gated route, AC2 implement AC3's placeholder using the already-seeded but unused VIEWER_PERSON_ID fixture, AC3 full suite passes as a genuine guard. Needs /test-plan -> /definition-of-ready -> dispatch -- not yet started. Logged in workspace/dod-backlog-findings.md as F6.",
      "owner": "operator/next session -- run /test-plan for rbg-s1 next; higher priority than kvvg-s1/vcb-s1 given it's a security-test-integrity gap"
    },
    {
      "date": "2026-08-18",
      "action": "New short-track story vcb-s1 (2026-08-18-verification-coverage-backfill) created: bundles two small, unrelated, already-diagnosed verification gaps found during the DoD backlog pass. (a) rpc-s1 (2026-07-19-product-repo-connect-ux, merged PR #508+#510) promised a dedicated automated NFR-Accessibility test in its test plan but shipped none -- markup itself manually confirmed compliant during the DoD pass, no known defect. (b) jrf-s1 (2026-07-19-new-feature-redirect-fix, merged)'s AC4 regression test only calls the fixed handler twice in isolation, not the full documented 67/345-baseline suite run its own DoR contract required. Needs /test-plan -> /definition-of-ready -> dispatch -- not yet started. Logged in workspace/dod-backlog-findings.md as F7.",
      "owner": "operator/next session -- run /test-plan for vcb-s1 next"
    },
    {
      "date": "2026-08-18",
      "action": "New short-track story csvg-s1 (2026-08-18-canvas-fix-staging-verification-gap) created: bundles two related, already-diagnosed staging-verification gaps found during the DoD backlog pass, both from the same 2026-07-23 ideate-canvas-fix cluster. (a) icrh-s1 (2026-07-23-ideate-canvas-resume-hydration-fix, merged PR #569)'s AC6 real-staging E2E verification shows as skipped (not passed) in CI, blocked by a precondition gate; a promised manual re-run/follow-up comment was never posted, though the deploy itself was confirmed live. (b) icv-s1 (2026-07-23-ideate-canvas-turn2-render-fix, merged)'s AC6 real-staging re-verification was explicitly left 'pending' in decisions.md and never closed -- acVerified 5 of 6 in pipeline-state.json confirms it. Both stories' underlying canvas-rendering fixes are already confirmed correct via unit/behavioural tests (15/15 and 3/3 respectively) -- this is unclosed staging confirmation, not a known defect. Needs /test-plan -> /definition-of-ready -> dispatch -- not yet started. Logged in workspace/dod-backlog-findings.md as F8.",
      "owner": "operator/next session -- run /test-plan for csvg-s1 next"
    },
    {
      "date": "2026-08-21",
      "action": "New short-track story cmba-s1 (2026-08-21-canvas-maximise-and-bulk-assign-button-fixes) created -- HIGHER SEVERITY, LIVE PRODUCTION BUG: found via live Chrome verification of the final 2 hasLayoutDependentGaps-flagged stories (cdpl-s1, bmau-s1), completing the full 55-story DoD backlog pass. cdpl-s1 (2026-08-07-canvas-diagram-panel-layout-fix, merged)'s new 'Maximise diagrams'/'Maximise canvas' buttons throw 'ReferenceError: swToggleCanvasFs is not defined' on every read-only/historical resumed /design, /definition, or /ideate conversation -- live-confirmed twice via browser console on a real staging session. Root cause: chat-view.js:156 suppresses the entire script block containing the toggle functions whenever data.readOnly is true, but the button markup renders unconditionally. Confirmed PRE-EXISTING, not introduced by cdpl-s1: the older swToggleArtefactFs pattern it was told to reuse has the identical bug, independently verified broken on the same page. Bundled with a minor, unrelated, live-confirmed cosmetic bug: bmau-s1 (2026-08-10-bulk-module-assignment-ui-gap, merged)'s 'Assign to module' button label gets permanently stuck on 'Assigning...' after the first successful use in a session (functionally harmless -- confirmed via two real POST /modules/bulk-assign round trips, both 200 -- just a missing success-path state reset). Story artefact written: AC1/AC2 fix the readOnly script-suppression for all 3 toggle functions (higher priority), AC3 no regression to live-session behaviour, AC4 fix the stuck button label. Needs /test-plan -> /definition-of-ready -> dispatch -- not yet started. Logged in workspace/dod-backlog-findings.md as F9. This closes the full DoD backlog pass -- see workspace/dod-backlog-findings.md 'Backlog pass status' for the final summary (55/55 stories, 9 findings, F1-F9 all tracked as follow-up stories not yet started).",
      "owner": "operator/next session -- run /test-plan for cmba-s1 next; AC1/AC2 (live production bug) is the higher-priority half. Also: review and prioritize F1-F9 as a batch -- the full DoD backlog pass is now complete."
    },
    {
      "date": "2026-08-21",
      "action": "Separately from the DoD backlog pass: fixed a real live production bug found and fixed directly (operator's explicit choice, not routed through the pipeline) -- products.js's 11 renderShell call sites never passed isAdmin, so the admin-only 'Admin credits' sidebar nav link never appeared anywhere in the app's primary UI (dashboard, product view, kanban x3, roadmap, guardrails, new-product, 2 error pages). Fixed by threading isAdmin through all 11 call sites and 5 render-helper function signatures (commit 57e1044c). Verified: 38 targeted regression tests + full 530-file suite, zero new regressions (33 pre-existing failures, 32 match tests/known-baseline-failures.json exactly, 1 -- rb-s5 -- independently reproduced as pre-existing via git stash). Pushed to origin/master with operator approval (bypassed branch-protection PR-required rule, operator has bypass permission). Operator then manually approved the stuck 'Staging Deploy' GitHub Actions run (32411846077) -- it had been queued in 'waiting' status since push, matching the known deploy-queue-stuck issue (see the earlier CI-triggering pendingActions entry above). All 4 jobs completed successfully including 'Promote to production' -- confirmed live via Chrome on skills-framework.fly.dev/dashboard: 'Admin credits' now appears in the sidebar. New short-track story apsc-s1 (2026-08-21-admin-credits-plan-status-ux) also created from this same live session: operator found tpac-s1's Plan/Status control on /admin/credits has a cramped, hard-to-scan UX (two 3-option radio-button groups + submit button per tenant row) -- not a functional bug (setPlanState/POST /api/admin/plan/set confirmed working correctly live), a presentation-only fix. Logged in workspace/dod-backlog-findings.md as F10.",
      "owner": "operator/next session -- run /test-plan for apsc-s1 next (low priority, presentation-only). Also worth understanding: the deploy queue being stuck required a MANUAL approval to unstick this one run -- if that's now a recurring need, worth investigating why runs land in 'waiting' by default rather than running automatically as staging-deploy.yml presumably intends. NOTE (2026-08-31): this session observed staging-deploy.yml's promote-to-prod jobs reaching manual-approval state reliably and completing normally once approved, across 4 separate merges -- the 'stuck in waiting indefinitely' symptom described here was not observed this session, though it was not explicitly re-tested either."
    },
    {
      "date": "2026-08-21",
      "action": "HIGHEST SEVERITY FINDING THIS SESSION -- live authorization gap, not a test-coverage issue. Found while writing rbg-s1's test plan (attempting to ground AC2's 'viewer denied on write action' assertion in real code): bri-s3.3's own original AC3 ('viewer attempts any write action, denied') describes behaviour that was NEVER implemented anywhere in the codebase. Confirmed via full search of src/web-ui/middleware/ and every route file -- 'viewer' exists only as a stored value in team-management.js's VALID_ROLES; no middleware or route anywhere checks for or blocks it. The only real role gate in the entire codebase is requireAdmin (admin vs everyone else -- does not distinguish engineer/product/viewer). A person assigned 'viewer' (implying read-only) currently has identical write access to engineer/product roles. Unknown whether any real production tenant currently has a viewer-role person assigned -- NOT yet checked, operator recommended to verify this to gauge urgency. rbg-s1 was corrected in place (AC2 narrowed to test the boundary that genuinely exists -- viewer correctly denied as non-admin via requireAdmin, same mechanism as AC1 -- rather than the non-existent 'any write action' behaviour). This new gap was NOT folded into rbg-s1 (operator's explicit decision, discussed and agreed) -- instead routed through a real /discovery artefact (artefacts/2026-08-21-viewer-role-no-enforcement/discovery.md, status: Draft, awaiting approval) since 'which write actions specifically should viewer block' is a real product/design decision, not a bounded bug fix. Logged in workspace/dod-backlog-findings.md as F11.",
      "owner": "operator -- review and approve (or reject/redirect) the discovery artefact; also recommended to directly check production's team_memberships table for any existing viewer-role assignments before treating this as low-urgency"
    },
    {
      "date": "2026-08-31",
      "action": "Four production dogfooding fixes shipped, merged, and DoD-complete this session, forming one investigation chain from a single operator bug report: rssp-s1 (/review SKILL.md selection-prompt removal, PR #799 -- COMPLETE WITH DEVIATIONS, AC4 verified by content-assertion only), sstr-s1 (SSE pre-first-chunk retry, PR #800 -- COMPLETE), daep-s1 (definition-artefact Format A story-ID parsing -- the ACTUAL root cause of the original bug report's specific URL, PR #801 -- COMPLETE), ssdo-s1 (SSE client-disconnect observability logging, PR #802 -- COMPLETE). All four independently verified merged via gh pr view before writing each DoD, per this repo's own standing rule. See each feature's own dod/*.md for full AC coverage and evidence.",
      "owner": "closed -- all four DoD-complete"
    },
    {
      "date": "2026-08-31",
      "action": "New standard-track feature 2026-08-31-web-ui-navigation-legibility created from 3 of 5 pieces of operator UX feedback (context-panel collapse, per-feature artefact browser, persistent next-stage action -- the feature-summary-page redesign item was explicitly deferred out, and the diagram-type-surfacing item was answered directly as expected-not-a-bug, both captured in capture-log.md 2026-08-31). Full /discovery -> /clarify (3 assumptions resolved) -> approval cycle run live with the operator this session. decisions.md created with 3 architectural decisions (reuse existing UI patterns, sticky element not nav redesign, disk-canonical artefact reads -- no new data store). Status: Approved, ready for /benefit-metric. Not yet started.",
      "owner": "operator/next session -- run /benefit-metric for web-ui-navigation-legibility when ready to continue"
    },
    {
      "date": "2026-08-31",
      "action": "Three NEW, unfixed production findings from the af17f555 dogfooding journey (separate from the four already-shipped fixes above) -- see checkpoint.pendingActions above for full detail on each: (1) listArtefacts local-vs-Postgres precedence bug (3-vs-8 artefact count), well-scoped, not yet fixed. (2) computeStep1Summary test-plan/DoR scanner expecting a per-story subdirectory convention that doesn't match what's actually written (flat feature-level files) -- causes the appearance of a journey 'reverting' from DoR back to test-plan; needs an architectural decision before fixing. (3) Fly.io auto_stop_machines='suspend' causing silent mid-request machine suspension -- strong evidenced root cause for silent SSE-turn hangs seen twice this session; needs an operator infra/cost decision, not a code fix. All three deliberately captured and NOT acted on this session, per the operator's own explicit prioritization choice ('capture for now, stop here').",
      "owner": "operator/next session -- decide which (if any) of the three to prioritize; none are blocking anything else"
    },
    {
      "date": "2026-09-04",
      "action": "Six short-track CI/deploy-infrastructure stories shipped, merged, and DoD-complete this session, all confirmed live in production via git-ancestor checks against real promote-to-prod deploys: daga-s1 (Docker image includes artefacts/+.github/ -- .dockerignore half only, PR #828), sdsb-s1 (staging deploy skips bookkeeping-only pushes, PR #829), cpco-s1 (CI install/cache optimization, real measured 75-85% improvement, PR #830), stcs-s1 (purge-e2e-tenants cold-start retry + scheduled backstop, PR #831), ncdv-s1 (npm timing diagnostic logging, AC4 still open/watching, PR #832), dcfx-s1 (Dockerfile COPY fix -- the other, load-bearing half of daga-s1's own original intent, PR #833). daga-s1's own DoD was found wrong via a live-page check a day after being marked COMPLETE, then amended with a CORRECTION section once dcfx-s1 fixed and re-verified it live. See checkpoint.contextAtWrite above for the full narrative and checkpoint.pendingActions for the two genuinely open follow-ups (promote-to-prod's own missing version-stamp step; the still-unconfirmed npm-timing/bcrypt mystery).",
      "owner": "closed -- all six DoD-complete and confirmed live; two small follow-ups noted in checkpoint.pendingActions, neither blocking"
    },
    {
      "date": "2026-09-12",
      "action": "DECISION NEEDED, HIGH LEVERAGE -- production deploy backlog. Found while investigating 6 DoD items blocked on 'promote-to-prod requires manual approval': there are actually 31 separate pending promote-to-prod GitHub Actions approval requests queued in .github/workflows/staging-deploy.yml, going back to 2026-09-03T19:28:58Z (not just the 6 named in DoD blocker text -- csdg-s1, pst-s1, pgft-s1, ppg-s1, aada-s1, prlf-s1). Confirmed via `gh run list --workflow=staging-deploy.yml`: 31 runs with status=waiting. Each promote-to-prod job does a plain `actions/checkout@v4` pinned to that specific run's own commit SHA (github.sha), with no ref override, and master's history is linear -- confirmed via `git merge-base --is-ancestor <oldest-waiting-sha> <newest-waiting-sha>` returning true. This means approving ONLY the single newest pending run (currently commit 916144ea, the ntpg-s1 merge -- check `gh run list --workflow=staging-deploy.yml --json status,headSha,createdAt` for the actual current newest before acting, since more may have queued since) deploys everything accumulated since 9/3 in one shot -- production is not '6 small fixes behind', it is about 8+ days and ~25 merged PRs behind staging. The other 30 pending approval requests will sit stale/orphaned afterward (harmless -- can be dismissed for tidiness in the Actions UI, or left alone). Recommend: review the newest waiting run's diff scope (it will include everything merged since 9/3, not just the 6 originally-flagged fixes) before approving, since this is a much bigger production change than any single one of those stories implied on its own.",
      "owner": "operator (Hamish King) -- review and approve (or reject) the newest waiting promote-to-prod run in GitHub Actions"
    },
    {
      "date": "2026-09-12",
      "action": "DECISION NEEDED -- 13 DoD gaps blocked on real external infrastructure/cost this agent should not touch unilaterally. Full list with the specific blocker: bri-s1.2 (live staging-vs-prod PostHog cross-contamination check, PAT-06 -- now structurally unblocked but never executed against real infra), bri-s1.5 (real comparison of staging vs prod PostHog flag-list mirroring, AC4 -- no automatable substitute), bri-s2.1 (real fly deploy + Fly dashboard build/billing confirmation, AC1/AC3), bri-s2.2 (real Neon schema/write-isolation/cold-start manual scenarios never executed, plus a known false-positive regex collision with bri-s1.2's own code), bri-s2.3 (AC3 Upstash usage-ceiling review, no automatable substitute, DoR-flagged as the highest-risk item in that whole epic), tir-s6 (AC1/AC2/AC4 need a real DATABASE_URL-backed run, RISK-ACCEPT covers the gap but does not close it), sdg.4 (AC5 manual smoke test -- model grounds >=2/5 questions in strategy -- needs a real, non-mocked model call), sdg.5 (AC2 same class, real model call), sdg.6 (real end-to-end confirmation producing a real strategy-metrics.json entry, plus a separate still-missing benefit-metric.md for the whole epic), a2-stripe-test-mode-plan-selection (AC1 requires entering a real Stripe test-mode card -- explicitly out of scope for an agent to type), s5-sequence-diagram-type (AC1/AC2 need real-model verification, requires toggling the mock gateway off for a real, costed call), mgar-s1 (the force-on step's wiring is confirmed correct, but its real-world effect is currently zero until a human runs `flyctl secrets set ADMIN_GITHUB_LOGINS=\"<existing-value>,e2e-test-admin@example.test\" --app wuce-staging` -- a live production-admin-authorization change), lab-s3.5 (AC5 pricing configurability end-to-end needs a real Stripe test-dashboard price change + a real Fly.io redeploy). Also obs-1 (NFR-PERF-1, pino <5ms SSE-open latency, unmeasured, RISK-ACCEPTed, low urgency, no specific blocker beyond 'nobody has measured it yet'). None of these are currently-known defects -- they are all honestly-recorded 'never independently verified against real infra' gaps. Decide per-item whether to authorize an agent to do the real-cost/real-infra action (mock gateway off, a real Stripe test card, a DATABASE_URL-backed run) or handle manually, or accept the RISK-ACCEPT as permanent.",
      "owner": "operator (Hamish King) -- decide per item; none are blocking anything else"
    },
    {
      "date": "2026-09-12",
      "action": "DECISION NEEDED -- 12 DoD gaps that are real, still-open code/scope gaps requiring actual follow-up work, not just verification. bri-s1.4: identifyTenantGroup() is fully built and unit-tested but never actually called from any live request path in src/ -- flag targeting itself still works via isEnabled()'s automatic group derivation, but PostHog dashboard group records are never populated; needs a real wiring task. wusl-s1: 5 of 9 handler call sites verified via code review (shared-helper delegation) rather than individual automated tests -- a real test-coverage gap, non-AC-blocking. csd-s5: AC2 only half-satisfied -- System Architecture as-built diagram generation was never built (no resolved decisions.md ARCH entry authorizing an extraction method); Program Design + Data Model as-built generation are complete. csd-s6: blocked on csd-s5's same gap -- its own comparison logic is implemented and tested but can't exercise its full real-usage path until csd-s5 closes. rb-s5 and scr-s1: both hit the same real, already-RISK-ACCEPT-re-affirmed ~24% performance-budget overage on --with-outer-loop (measured ~3722-4189ms against a 3000ms budget, re-confirmed multiple times, no fix attempted -- a real perf-optimization task if ever prioritized). ep1-s1: 2 NFRs not fully evidenced (perf timing, a stalled-feature test). ep1-s3: a session-header disclosure NFR literally not implemented (not just unverified -- not built). res-s2: audit-logging NFR (artefact-overwrite sub-flow) has no dedicated test and the log event omits journeyId/explicit timestamp fields. si-s1: AC4 required a new server route not in the original DoR contract touch-points (documented, reasoned, not urgent); its m2 metric also has no pre-relocation baseline to compare against (a metric-design gap, not a delivery defect). csd-s2 and csd-s3: both are honest, low-priority, cosmetic AC-wording mismatches (a labelling-asymmetry and a 'saved as part of the DoR artefact' wording gap respectively) with no functional impact -- lowest priority in this whole list, candidates for either a quick AC-wording fix or permanent accept. None of these are blocking release; all are legitimately open, not stale bookkeeping (already double-checked against this session's own re-verification pass).",
      "owner": "operator (Hamish King) -- prioritize which (if any) get a dedicated follow-up story; none are urgent"
    },
    {
      "date": "2026-09-12",
      "action": "FYI ONLY, no decision needed -- remaining amber items that are already correctly tracked elsewhere or are honest, accepted, permanent deviations, left as-is on purpose: wsm.2/wsm.4/wucp.1 (each references a real, currently-failing regression already tracked in tests/known-baseline-failures.json -- correctly amber, being watched, not silently ignored); vrne-s1 (AC4's literal wording named 4 roles, the shipped fix correctly adds a 5th documented default role 'user' -- a wording deviation already reasoned through in decisions.md's 2026-08-23 ARCH entry, fail-closed guarantee intact); icv-s1/cmtt-s1/csvg-s1 (each has a real, honestly-recorded partial-verification gap requiring either a real paid Anthropic API turn or a real staging billing/credit account fix that is itself a separate, already-tracked blocker -- not silently rounded up to complete, correctly left partial).",
      "owner": "none -- informational"
    },
    {
      "date": "2026-09-12",
      "action": "SEPARATE FINDING, not part of the DoD backlog -- two GitHub Actions workflows have been failing on every single push to master throughout this entire session (verified via `gh run list --limit 15`, all recent 'Deploy dashboards to GitHub Pages' and 'Improvement Agent -- Scheduled Dreaming' runs show status=failure). (1) 'Deploy dashboards to GitHub Pages': fails at the 'Setup Pages' step with 'Resource not accessible by integration -- Create Pages site failed' -- looks like GitHub Pages is not enabled for this repo, or the workflow's token lacks the pages:write permission/scope. (2) 'Improvement Agent -- Scheduled Dreaming': its own 'Commit agent outputs' step tries to `git push` trace files directly to master and gets rejected with 'GH013: Repository rule violations... Changes must be made through a pull request' -- this job's own token does not have the same branch-protection bypass this session's own git pushes had (a PAT/App with a configured bypass). Neither failure was caused by anything committed this session -- confirmed by checking failure history back through the session's first commit. Both are pre-existing infra/permission gaps; worth a look whenever there's a slow moment, not urgent.",
      "owner": "operator (Hamish King) -- decide whether to fix repo Pages settings and/or grant the dreaming-job's token a ruleset bypass (or change how it commits), or leave both failing as known noise"
    }
  ],
  "estimate": {
    "e1": null
  }
}
