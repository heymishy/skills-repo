**Checking for product context and reference materials...**

No `product/` directory or `artefacts/` reference folder found for this initiative yet. I'll surface the context through conversation.

No EA registry flag detected in `context.yml` — proceeding without blast-radius lookup.

---

"Make the API faster" tells me there's a performance problem worth solving — but I need more before I can write a useful discovery artefact.

Let's work through this one question at a time.

---

**What's actually happening now that shouldn't be — or not happening that should?**

Tell me about the problem itself, not the fix. For example:

- Which API (or which endpoints specifically)?
- What are users or systems experiencing — timeouts, slow page loads, job backlogs, SLA breaches?
- How often does it happen, and who notices it first?

> Reply: describe the problem